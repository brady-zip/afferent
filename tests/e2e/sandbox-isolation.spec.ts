import { test } from "@playwright/test";

test("keeps colliding two-user reads, writes, search, counts, and reactivity isolated", async () => {
  throw new Error("PHASE4_SANDBOX_ISOLATION_SUITE_IMPLEMENTATION_REQUIRED");
});
