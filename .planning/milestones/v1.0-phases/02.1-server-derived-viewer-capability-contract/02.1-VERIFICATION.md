---
phase: 02.1-server-derived-viewer-capability-contract
verified: 2026-07-21T18:06:18Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: passed
  previous_score: 6/6
  reason: "Test-only commit d9c4dec made the browser-authority validator slice non-vacuous."
  regressions: []
---

# Phase 02.1: Server-Derived Viewer Capability Contract Verification Report

**Phase Goal:** Copied UI can render exact vote and author-action state from server-derived, provider-neutral viewer truth without accepting browser identity or authority facts.

**Verified:** 2026-07-21T18:06:18Z

**Status:** passed

**Score:** 6/6 must-haves verified

**Re-verification:** Yes -- after test-only commit `d9c4dec`

## Final Verdict

Phase 02.1 passes. Fresh source, artifact, and behavioral evidence shows that browser-facing query arguments contain no actor or capability inputs; trusted host wrappers resolve an optional provider-neutral viewer on every post-bearing read; component queries resolve only an already-existing actor through the exact scope/external-key index and never write or scan; vote membership and author capabilities are computed from scope-complete indexes and predicates shared with mutation enforcement; and the explicitly versioned DTOs reach the headless and packed consumers without widening unrelated projections.

Desired-state vote optimism updates current-generation feed and detail caches atomically from the cached `viewerHasVoted` value, so repeated desired-state calls do not change counts. Existing generation fencing and Convex optimistic rollback semantics remain intact. Provider conformance, packed installation, scope isolation, type/lint, focused React behavior, and the complete Phase 2 regression all pass.

## Must-Have Accounting

The three ROADMAP success criteria and six plan truths were merged and deduplicated into six independently checkable truths.

| # | Truth | Status | Independent evidence |
| --- | --- | --- | --- |
| 1 | Every post-bearing host read derives an optional viewer in trusted query context; browser validators, bindings, and generation keys accept no identity or capability facts | VERIFIED | `src/client/internal.ts` invokes `resolveViewerActor` for `listPosts`, `listFeedback`, `getPost`, and `resolvePost`; the now non-vacuous static test extracts and scans all three public post-read validator declarations plus `src/react/bindings.ts`; adversarial conformance rejects forged authority. |
| 2 | Query-time viewer resolution finds only an existing actor through `actors.by_scope_external_key`, with no write or scan; anonymous and authenticated-no-row viewers receive all-false viewer fields | VERIFIED | `findExistingActor` is query-safe and scope-leading; the component matrix proves anonymous and no-row all-false results across all four reads and proves actor count is unchanged. |
| 3 | Vote membership is exact for scope/post/actor, while edit and withdraw projections use the same predicates as mutations | VERIFIED | `findVoteMembership` uses `votes.by_scope_post_actor`; scope isolation passes; the component matrix distinguishes actors and posts, then proves active-owner edit/withdraw, withdrawn edit rejection, and repeat-withdraw parity. |
| 4 | Widened post contracts are explicitly versioned and exact, while discovery, similar, roadmap, and changelog projections remain unchanged | VERIFIED | Types, public/component validators, generated references, fixtures, and packed consumer agree on `PostDto` 2, `FeedbackPostDto` 3, `PostPage` 2, `FeedbackPage` 3, and `PostLookup` 2; exact contract tests pass and minimal projections remain version 1. |
| 5 | Headless feed/detail state carries server fields verbatim; desired-state optimism updates membership and both counters only in current-generation caches without double increments or stale detail drift | VERIFIED | Mounted React test proves false-to-true, repeated true, and true-to-false values in feed and exact detail keys; generation replacement and stale result/error tests pass; full React suite passes 54/54. |
| 6 | Static, component, provider, scope, packed, type/lint, and full Phase 2 gates preserve the security contract without regressing completed Phase 2 | VERIFIED | The original focused and single full `npm run test:phase2` gates pass; after test-only `d9c4dec`, full static, focused component, and targeted React regressions also pass. No implementation file changed in the closure commit. |

## Artifacts, Wiring, and Commits

| Artifact / link | Status | Details |
| --- | --- | --- |
| Nine plan artifacts | VERIFIED | `src/client/internal.ts`, actor/capability/view models, public contracts, React feedback hook, focused component/static tests, and packed wrapper all exist and are substantive. |
| Trusted wrapper -> component reads | WIRED | Root/fixed/scoped server options expose `resolveViewerActor`; only internal component calls receive `viewerActor`. |
| Public reads -> actor resolver | WIRED | Posts and feeds resolve the optional viewer through `findExistingActor`; no public query calls `upsertActor`. |
| Views -> exact vote membership | WIRED | Both post mappers call `findVoteMembership` with the post scope, post ID, and resolved actor ID. |
| Views and participation -> shared capability policy | WIRED | Both paths import `canActorEditPost` and `canActorWithdrawPost` from `postCapabilities.ts`. |
| React -> versioned contracts | WIRED | Feed/detail hook state uses `FeedbackPostDto` fields directly and keys optimistic detail writes by post plus session generation. |
| Packed wrapper -> trusted auth | WIRED | `resolveTrustedViewer` derives Clerk identity from `ctx.auth`, returns `null` anonymously, and is never supplied from `App.tsx`. |

Automated artifact query: **9/9 present/substantive**.

Automated key-link query: **8/8 wired**.

Implementation/test commits `b9a42fe`, `f45fab5`, and `ddeecc4`, documentation commit `076b71b`, and test-coverage closure commit `d9c4dec` all exist. `git diff d9c4dec^ d9c4dec --name-only` contains only `tests/static/viewer-capabilities.test.ts`. The radio correction requested before continuation is separately present as `e4866eb` and is not part of the Phase 02.1 score.

## Fresh Behavioral Evidence

| Command | Result | Status |
| --- | --- | --- |
| `npm run test:static` after `d9c4dec` | 4 files, 28/28 tests passed | PASS |
| Direct post-read validator extraction probe | `listPosts` 133 bytes, `listFeedback` 482 bytes, `getPost` 72 bytes; no forbidden names | PASS, NON-VACUOUS |
| `npm exec -- vitest run tests/component/viewer-capabilities.test.ts` after `d9c4dec` | 2/2 tests passed | PASS |
| Targeted `tests/react/live-headless.test.tsx` generation/optimism checks after `d9c4dec` | 3/3 selected tests passed | PASS |
| Focused component/static matrix | 5 files, 27/27 tests passed | PASS |
| Focused React matrix | 2 files, 33/33 tests passed | PASS |
| `npm run test:auth-conformance` | 3 files, 25/25 tests passed | PASS |
| `npm run test:scope` | 3/3 tests passed | PASS |
| `npm run typecheck` | Exit 0 | PASS |
| `npm run lint` | Exit 0 | PASS |
| `node --test tests/integration/packed-artifact.test.mjs` | 3/3 tests passed; clean external packed consumer completed | PASS |
| `npm run test:phase2` | Build; model 32/32; component 40/40; backend and real Convex matrices; static 28/28; React 54/54; conformance 25/25; scope 3/3; typecheck; lint; packed consumer all passed | PASS, RUN ONCE |
| Dirty fingerprint | `09b5f7179dc749a197ec6a59c5ba188313b8c84536e0c35b6487ae7fcb058381` | PRESERVED |

The fresh aggregate output is captured by h5i object `b6cfba6dfccd9355`.

## Requirement Coverage

| Requirement | Status | Evidence |
| --- | --- | --- |
| UI-08 | SATISFIED | All post-bearing headless reads receive server-derived vote membership and author edit/withdraw capabilities without browser identity or authority inputs. |

Phase 3's copied UI can consume the three server fields as final presentation truth while trusted host wrappers remain the authority boundary.

## Disconfirmation Pass

| Required check | Investigation | Result |
| --- | --- | --- |
| Browser actor/capability input | Inspected every post-bearing browser binding and exact public intent validator; ran the repaired extraction test and an independent declaration-length probe; checked mounted generation arguments and forged-authority conformance | Rejected: all three declarations are present and non-empty, and browser inputs contain only intent data and cache generation, never actor, viewer, ownership, or capability facts. |
| Query-time actor writes or scans | Traced all four host reads through public component queries and `findExistingActor`; searched actor/vote paths for write calls and collection/filter scans | Rejected: reads use exact scope-leading indexes and do not insert, patch, upsert, collect, or filter membership rows. |
| Cross-scope, external-key, or vote-membership collision | Checked index definitions/call arguments, post-specific voter behavior, scope regression, and persisted vote partitions | Rejected: actor and vote lookups bind scope first; membership binds scope, post, and actor. |
| Read/mutation policy drift | Compared view mapping and edit/withdraw enforcement imports and ran active/withdrawn/repeat-withdraw transitions | Rejected: both paths call the same two shared predicates. |
| Anonymous and authenticated-no-row truth | Ran the four-read component matrix and three-provider conformance; inspected packed `null` resolver behavior | Rejected: no actor is created and all three fields are false. Better Auth uses `safeGetAuthUser` for anonymous queries. |
| Silent DTO widening/version mismatch | Compared TypeScript types, component/public validators, generated component references, exact fixtures, and packed result assertions | Rejected: all widened containers have explicit matching versions; minimal projections are unchanged. |
| Optimistic double increment or stale-generation/detail drift | Ran exact desired-state feed/detail assertions, identity-generation replacement, and stale callback tests | Rejected: delta derives from cached membership, repeated desired state is idempotent, and writes target current-generation feed/detail keys only. |
| Provider/packed anonymous behavior | Ran provider conformance anonymous public reads; inspected each provider resolver and packed host wrapper; installed and exercised the tarball in a clean Convex consumer configured in anonymous agent mode | Rejected: public anonymous reads remain available and return false capabilities; identity normalization stays server-side. |

The previous non-blocking test-quality concern is closed by `d9c4dec`. `tests/static/viewer-capabilities.test.ts` now locates `listPostsIntentValidator`, `listFeedbackIntentValidator`, and `getPostIntentValidator` separately, fails if any declaration is missing, fails if any extracted declaration is empty, and scans the resulting non-vacuous authority surface for every forbidden identity/capability name. The full static suite and an independent extraction-length probe both pass.

No unreferenced `TBD`, `FIXME`, `XXX`, `TODO`, `HACK`, or placeholder marker appears in Phase 02.1-owned implementation/test paths. `git diff --check` passes. The unrelated dirty files and their exact fingerprint remain unchanged.

## Human Verification

None. All behavior-dependent truths have fresh automated evidence.

---

_Verified: 2026-07-21T18:06:18Z_

_Verifier: independent generic-agent fallback using the installed gsd-verifier role_
