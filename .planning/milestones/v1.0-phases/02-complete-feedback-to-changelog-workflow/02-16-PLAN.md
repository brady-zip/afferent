---
phase: 02-complete-feedback-to-changelog-workflow
plan: "16"
type: execute
wave: 16
depends_on: ["02-15"]
files_modified:
  - src/component/model/mergedPagination.ts
  - src/component/public/comments.ts
  - src/component/admin/activity.ts
  - tests/model/merged-pagination.test.ts
  - tests/component/merge.test.ts
  - tests/component/moderation.test.ts
  - tests/static/schema-scope.test.ts
  - scripts/test-merge-backend.mjs
  - scripts/test-headless-backend.mjs
  - tests/integration/headless-backend.test.mjs
autonomous: true
gap_closure: true
requirements: [DISC-07, ADMN-10, UI-03, QUAL-01]
must_haves:
  truths:
    - "After a real duplicate merge, visitors can page the complete canonical-plus-source comment history in ascending (_creationTime, _id) order without omissions, duplicates, overlap, or page drift."
    - "After the same merge, authorized admins can page the complete combined activity history in existing index-native descending (occurredAt, _creationTime, _id) order with the same cursor-window guarantees."
    - "Both length-1 and length-2 reader sets honor paginationOpts.cursor and paginationOpts.endCursor through one opaque versioned envelope carrying order kind and stream-set signature; malformed or stale envelopes cleanly reset to a fresh maximal coherent prefix."
    - "Pinned windows survive source-row repoint during cleaning because cursor positions contain only repoint-invariant order keys, while single-to-merged cutover and merged-to-single completion reset rather than overlap."
    - "Deep merged pages remain bounded by scope/post/order indexes rather than scanning earlier post rows, and public DTO, scope, auth, merge-visibility, and headless-state boundaries remain unchanged."
    - "Mounted and disposable-real-Convex recorders accept every publication only when it is the exact prior/current canonical window or the exact maximal prefix paired with a typed fault, including load-more, insertion, deletion, recovery, and identity replacement."
  artifacts:
    - path: src/component/model/mergedPagination.ts
      provides: "Shared namespaced two-stream composite-cursor paginator over convex-helpers mergedStream"
      exports: ["paginateMergedPostStream"]
    - path: src/component/public/comments.ts
      provides: "Real merged comment reader using the shared ascending composite order"
    - path: src/component/admin/activity.ts
      provides: "Real merged activity reader using the shared descending composite order"
    - path: scripts/test-headless-backend.mjs
      provides: "Disposable real-product watch/load-more/fault/identity temporal oracle"
    - path: scripts/test-merge-backend.mjs
      provides: "Real merge-state, equal-key, pinned-window, cleanup, and bounded deep-page oracle"
  key_links:
    - from: src/component/public/comments.ts
      to: src/component/model/mergedPagination.ts
      via: "listComments passes mergeReadPostIds and the (_creationTime, _id) stream order into the common paginator"
      pattern: "paginateMergedPostStream"
    - from: src/component/admin/activity.ts
      to: src/component/model/mergedPagination.ts
      via: "listPostActivity passes the same merge reader truth and the existing descending (occurredAt, _creationTime, _id) index order"
      pattern: "paginateMergedPostStream"
    - from: src/component/admin/activity.ts
      to: src/component/schema.ts
      via: "listPostActivity retains by_scope_post_occurred and its native occurredAt,_creationTime,_id suffix"
      pattern: "by_scope_post_occurred"
    - from: scripts/test-headless-backend.mjs
      to: src/component/public/comments.ts
      via: "the disposable host wrapper watches the installed component's real listComments query after a real merge"
      pattern: "components.*public.*comments.*listComments"
    - from: scripts/test-merge-backend.mjs
      to: src/component/admin/activity.ts
      via: "the disposable merge harness invokes the installed component's real listPostActivity query through cutover and cleanup"
      pattern: "listPostActivity"
---

<objective>
Close the grouped 58/59 Phase 2 verifier gap by making merged comments and merged activity obey the same complete reactive pagination contract as their single-post paths.

Purpose: Ensure Phase 3 consumes a correct headless discussion/activity source rather than compensating for skipped ties, unpinned windows, or offset-scaling reads in copied UI.
Output: One shared bounded composite-cursor helper for both one- and two-stream reader sets, both real readers migrated to it without schema or writer changes, and red-first component plus disposable-real-product temporal evidence.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- including lossless duplicate merges and bounded comments/activity pagination -- through tested component APIs and framework-light headless React hooks, **so that** users and admins can traverse the complete feedback history while I retain ownership of identity, authorization, and presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-VERIFICATION.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-15-PLAN.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-15-SUMMARY.md
@src/component/schema.ts
@src/component/model/merge.ts
@src/component/model/activity.ts
@src/component/public/comments.ts
@src/component/admin/activity.ts
@src/react/query.ts
@tests/component/merge.test.ts
@tests/component/moderation.test.ts
@scripts/test-merge-backend.mjs
@scripts/test-headless-backend.mjs
@tests/integration/headless-backend.test.mjs

<interfaces>
`mergeReadPostIds(ctx, scopeId, postId)` is the only authority for selecting the canonical/source reader set and may legitimately change from one stream before cutover, to two during `cutover_done`/`cleaning`, and back to one after normalization. `convex-helpers/server/stream` already provides `mergedStream`; its normalized keys append `_creationTime` and `_id`, and `.paginate()` consumes both `cursor` and `endCursor` while producing `continueCursor`, `splitCursor`, and `pageStatus`. The shared helper must use comments' existing `by_scope_post` order `(_creationTime,_id)` and activity's existing `by_scope_post_occurred` order `(occurredAt,_creationTime,_id)` without changing schema or writers. `CommentPageDto`, `PostActivityPageDto`, `CommentDto`, and `PostActivityDto` remain contractVersion 1. `useComments` and `usePostActivity` already own generation-fenced all-descriptor publication; correct the component page source without adding a React cache or alternate read.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify merged-reader cursor correctness red-first through real product queries</name>
  <files>tests/model/merged-pagination.test.ts, tests/component/merge.test.ts, tests/component/moderation.test.ts, tests/static/schema-scope.test.ts, scripts/test-merge-backend.mjs, tests/integration/headless-backend.test.mjs</files>
  <read_first>src/component/public/comments.ts, src/component/admin/activity.ts, src/component/model/merge.ts, src/component/schema.ts, tests/component/merge.test.ts, tests/component/moderation.test.ts, scripts/test-merge-backend.mjs, tests/integration/headless-backend.test.mjs</read_first>
  <behavior>
    - A real small merge and a real staged merge both expose every canonical and source comment exactly once across multiple pages, in ascending `_creationTime` then opaque ID order, including rows sharing the primary timestamp.
    - The same merge exposes every activity row exactly once in existing index-native descending `(occurredAt,_creationTime,_id)` order, including multiple events with the same `occurredAt`.
    - A page requested with `endCursor` stays pinned through insertion before/inside/after the window, test-only deletion, and a cleanup batch that physically repoints a source row; adjacent boundaries remain equal because cursor positions exclude `postId` and offsets.
    - The same shared paginator preserves the old single-post page shape/order, then cleanly resets on single-to-merged cutover, merged-to-single completion, a source-redirect cursor, and stream-set changes without duplicate or overlapping publication.
    - A deep page with a small `maximumRowsRead` reads from its composite index lower/upper bound and returns the expected page rather than rescanning the preceding post history or reporting an incomplete required split.
    - Malformed, stale, wrong-reader, wrong-order, wrong-version, or wrong-stream-set cursor/endCursor envelopes fuzz to a clean fresh maximal-prefix reset; they never throw, silently overlap, or become a cross-scope/existence oracle.
  </behavior>
  <action>Create failing model, convex-test, static, integration-source, and disposable-real-merge assertions before production changes. Drive `api.admin.merge.mergePost`, `api.public.comments.listComments`, and `api.admin.activity.listPostActivity` (or host wrappers that invoke those exact references), not a substitute single-table paginator. Establish single-post parity first, then seed canonical and source rows on both sides, force equal primary keys, walk three or more pages, pin first and middle windows, and advance a staged merge through `ready`, `cutover_done`, individual cleanup repoints, and `done`. Explicitly cover 1-to-2 cutover reset, 2-to-1 completion reset, a cursor issued through a source redirect, and cleanup that moves a row between the two indexed streams without changing its order key. Fuzz cursor, endCursor, and splitCursor envelopes across malformed payloads and wrong versions/readers/orders/stream signatures and require a non-throwing fresh-prefix reset with no overlap. Record exact ordered opaque IDs, not Set membership or eventual counts. Add a static audit that both length-1 and length-2 readers delegate to one helper and no longer use post-prefix `.filter()` cursor scans. Keep test-only delete/fault controls inside disposable harness modules; do not add product deletion, generic CRUD, public cursor internals, schema migration, or alternate authority inputs.</action>
  <verify><automated>npm exec -- vitest run tests/model/merged-pagination.test.ts tests/component/merge.test.ts tests/component/moderation.test.ts tests/static/schema-scope.test.ts; node --test tests/integration/headless-backend.test.mjs; node scripts/test-merge-backend.mjs</automated></verify>
  <done>The real merged comments/activity paths fail against exact composite ordering, pinned-window, cursor-validation, cleanup, and bounded-deep-page expectations before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route every reader cardinality through one reset-safe composite-cursor stream</name>
  <files>src/component/model/mergedPagination.ts, src/component/public/comments.ts, src/component/admin/activity.ts, tests/model/merged-pagination.test.ts, tests/component/merge.test.ts, tests/component/moderation.test.ts, tests/static/schema-scope.test.ts</files>
  <read_first>tests/model/merged-pagination.test.ts, tests/component/merge.test.ts, tests/component/moderation.test.ts, src/component/model/merge.ts, src/component/public/comments.ts, src/component/admin/activity.ts, node_modules/convex-helpers/server/stream.ts</read_first>
  <action>Implement `paginateMergedPostStream` as the single internal seam for both one and two IDs from `mergeReadPostIds`; `mergedStream` with one member is the required degenerate case, not a native-paginator branch. Build each member from the existing scope/post equality index and call bounded `.paginate()` with validated `numItems`, `maximumRowsRead`, and `maximumBytesRead`. Comments retain `by_scope_post` ascending with the normalized `(_creationTime,_id)` suffix. Activity retains `by_scope_post_occurred` descending with its existing normalized `(occurredAt,_creationTime,_id)` suffix. Add no field, index, writer, backfill, migration, or merge mutation change. Wrap `continueCursor`, `splitCursor`, and both inbound `cursor`/`endCursor` in the same opaque versioned envelope carrying reader/order kind and a deterministic stream-set signature; the embedded helper position contains only repoint-invariant order fields, never scope/post IDs or offsets. Decode start/end symmetrically. When either boundary is malformed or its version, reader, order, or stream signature differs from the current reader set, execute the helper's explicit clean-reset path so the observable result becomes a fresh maximal coherent prefix; do not throw, reuse a mismatched position, or emit overlapping old/new rows. This reset contract must handle 1-to-2 cutover, 2-to-1 completion, source-redirect calls, and arbitrary stale cursor fuzz. Preserve DTO validators/mappers, the comments alias, auth/scope/visibility guards, `mergeReadPostIds`, page metadata, React bindings, and public exports unchanged.</action>
  <verify><automated>npm exec -- vitest run tests/model/merged-pagination.test.ts tests/component/merge.test.ts tests/component/moderation.test.ts tests/static/schema-scope.test.ts; npm run typecheck; npm run lint</automated></verify>
  <done>Single and merged comments/activity share one namespaced full-key paginator, preserve their existing index-native orders, reset safely across cursor/session mismatch and 1-to-2-to-1 reader transitions, remain bounded, and pass the red model/component/static matrix without schema or writer changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove mounted load-more, fault, identity, and boundedness against the installed product component</name>
  <files>scripts/test-merge-backend.mjs, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs</files>
  <read_first>scripts/test-merge-backend.mjs, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs, src/react/query.ts, src/component/public/comments.ts, src/component/admin/activity.ts, src/component/model/merge.ts</read_first>
  <behavior>
    - The disposable app installs the copied Afferent component, performs an actual merge, and watches the actual public comment and admin activity component queries through narrow host wrappers.
    - Multi-page and rapid `loadMore` publications are exact canonical composite windows; equal-key ties survive every boundary and the last settled result equals backend truth.
    - Pinned comment and activity windows remain exact through insert, test-only delete, a cleanup-batch row repoint, and mutations while a new tail is incomplete.
    - Single-post shared-paginator output matches the prior native shape/order; 1-to-2 cutover, 2-to-1 done, source-redirect, and stale/wrong/malformed envelope cases reset to one fresh maximal prefix without throwing or overlap.
    - First/middle/tail wrapper faults publish only the exact maximal contiguous prefix with the closed error, then recover live to exact product truth.
    - Generation replacement disposes every old comment/activity watch exactly once and no old result, error, or pending structural work appears under the new identity.
    - Late pages with small row budgets succeed from index-bounded cursors, while cross-scope and hidden canonical reads remain not-found-equivalent.
  </behavior>
  <action>Replace the synthetic comment-only acceptance seam with (or supplement it by) a disposable application that installs the copied component and exposes thin test host functions calling its real `listComments`, `listPostActivity`, merge, seed/write, and test-only fault/delete/step operations. Reuse `createPaginatedWatchStore`, the every-publication recorder, boundary inspector, exact prior/current temporal oracle, fault-prefix oracle, and exact-once disposal accounting from Plans 02-13 through 02-15. Prove unchanged single-post shape/order first. Then exercise both domains across actual merge states: at least three loaded windows, equal-key boundaries, pinned insert/delete, a cleanup batch physically repointing a pinned row, pending load-more mutation, 1-to-2 cutover reset, 2-to-1 done reset, source-redirect cursor use, first/middle/tail failure and recovery, A-to-B-to-A generation replacement, and malformed/stale/wrong reader/order/version/stream-signature fuzz. Require every reset to settle on the exact fresh maximal prefix with no thrown render error or mixed overlap. Retain hidden-canonical/cross-scope privacy and constrain a late page with `maximumRowsRead` to prove index-bounded work. Keep setup failures distinct from contract assertions. Finish with the existing regression commands; no schema/writer migration, repository-relative packed import, or client-side data repair may satisfy acceptance.</action>
  <verify><automated>npm run test:react && node --test tests/integration/headless-backend.test.mjs && node scripts/test-merge-backend.mjs && node scripts/test-headless-backend.mjs && npm run test:static && npm run typecheck && npm run lint && npm run build && npm run test:package && npm run test:phase2</automated></verify>
  <done>The installed component's real merged comment and activity readers satisfy exact temporal, cursor-window, identity, fault-recovery, privacy, and bounded deep-page oracles, and every Phase 2/packed regression gate passes.</done>
</task>

</tasks>

## Prohibitions

- Do not alter or replace completed Plans 02-01 through 02-15 or move this correction into Phase 3.
- Do not add a React/UI comment cache, activity cache, recursive discussion tree, raw Convex read, per-row follow-up query, offset cursor, or result deduplication layer.
- Do not accept browser `scopeId`, `userId`, `isAdmin`, actor facts, or cursor-decoded authority; trusted host and component guards remain authoritative on every call.
- Do not expose helper cursor payloads, scope, merge job state, internal order keys, schema documents, or provider records in public DTOs.
- Do not use post-index `.filter()` progression, unbounded `.collect()`, full-history sorting, or page-size-only assertions as boundedness evidence.
- Do not add a server/client revision, watermark, public transition token, schema-owned UI session, or alternate merge truth.
- Do not add `postActivity.orderId`, a new activity index, schema/backfill/migration work, activity-writer changes, or merge production mutations; the existing native order is canonical.
- Do not modify package dependencies, package metadata, the user's TypeScript experiment, copied Phase 3 UI, or unrelated Codex/h5i files.

## Source Coverage Audit

| Source | ID / constraint | Plan coverage | Status |
| --- | --- | --- | --- |
| GOAL | Complete provider-neutral lifecycle through tested headless APIs | Restores complete merged discussion/activity traversal before copied UI execution. | COVERED |
| REQ | DISC-07 | Real merges preserve and page every canonical/source comment without cursor loss or drift. | COVERED |
| REQ | ADMN-10 | Admin activity remains append-only and completely cursor-pageable after merge. | COVERED |
| REQ | UI-03 | Real component pages compose with the existing explicit load-more/error/recovery/identity store. | COVERED |
| REQ | QUAL-01 | Model, component, static, disposable-real-product, packed, and aggregate regression gates cover the gap. | COVERED |
| CONTEXT | D-02, D-12 | Stable index-native total orders and reader-atomic merge semantics hold across cursor windows, row repoints, and 1-to-2-to-1 stream transitions. | COVERED |
| CONTEXT | D-21, D-47 | Activity remains bounded/cursor-paginated and headless pagination owns exact accumulated windows. | COVERED |
| CONTEXT | D-53, D-54, D-58 | Scope/visibility/merge privacy and stable versioned DTO boundaries remain unchanged. | COVERED |
| RESEARCH | Indexed bounded feeds; do not hand-roll cursor pagination | Uses installed `mergedStream` and existing scope-leading indexes for both one- and two-stream readers instead of filters/offsets or schema changes. | COVERED |
| VERIFIER | Grouped 58/59 comments/activity blocker | Closes ignored endCursor, partial-key cursor, deep-page scan, and missing real-path oracle together. | COVERED |
| LIVE PEER | ASK #ea36ec76646a7986 | Shared helper preserves comments `(_creationTime,_id)` and activity `(occurredAt,_creationTime,_id)`, wraps all cursor roles with order/stream signatures, and resets cleanly across 1-to-2-to-1 transitions and malformed/stale inputs. | COVERED |
| PRIOR PLAN | 02-15 | Preserves optional `useComments`, closed flat DTOs, and the shared React watch store; corrects its upstream product source. | COVERED |

All actionable verifier and live-peer items map to Tasks 1-3. Deferred Phase 2 ideas and Phase 3 copied UI remain excluded.

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Browser cursor -> trusted host -> component | Cursor data is untrusted navigation state; it never supplies scope, actor, admin, or merge authority. |
| Merge reader truth -> two indexed streams | Only `mergeReadPostIds` selects canonical/source IDs, and every stream is scope/post constrained. |
| Native index order -> public page DTO | Comments retain `_creationTime,_id`; activity retains `occurredAt,_creationTime,_id`; post ownership is never part of cursor position. |
| Component page transition -> React store | Both cursor bounds and complete metadata must compose with exact all-descriptor publication. |
| Disposable harness -> acceptance result | Test-only delete/fault controls may perturb real product data/query paths but cannot become shipped component APIs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-02-73 | Tampering / Information Disclosure | composite cursor decoder | high | mitigate | Apply one opaque envelope to start/end/split cursors, validate version/order/stream signature, and clean-reset malformed or mismatched input without throwing or reusing its position. |
| T-02-74 | Information Disclosure | merged stream selection | critical | mitigate | Obtain IDs only from scope-checked `mergeReadPostIds`; retain canonical visibility/not-found equivalence and scope-leading equality ranges. |
| T-02-75 | Denial of Service | deep merged pages | high | mitigate | Narrow both streams by decoded composite index keys, propagate row/byte ceilings, and prove late-page reads with small budgets. |
| T-02-76 | Tampering | equal-key ordering | high | mitigate | Retain comments' normalized `_creationTime,_id` and activity's native `occurredAt,_creationTime,_id` keys and assert exact tie traversal without schema changes. |
| T-02-77 | Information Disclosure | generation replacement | critical | mitigate | Reuse generation-fenced descriptor identities and require exact-once disposal/no stale publication in the real-product watch oracle. |
| T-02-78 | Repudiation | synthetic acceptance | high | mitigate | Require installed component references after an actual merge and distinguish harness setup failure from behavioral assertion failure. |
| T-02-79 | Tampering / Denial of Service | merge lifecycle cursor reuse | high | mitigate | Sign the active stream set, exclude postId/offset from positions, preserve pinned repoints, and clean-reset on 1-to-2, 2-to-1, source-redirect, or stale-signature mismatch. |
| T-02-SC | Tampering | dependency surface | low | accept | No dependency or install task is allowed; existing audited convex-helpers runtime and packed gates remain unchanged. |

</threat_model>

<verification>
Execute the focused red model/component/static and disposable merge checks first, implement the shared reset-safe composite stream and both real readers, then run every-publication real-product comment/activity watches. Final acceptance requires exact arrays and boundaries for ties, multiple pages, pinned insert/delete and cleanup repoint, single-post parity, 1-to-2 cutover reset, 2-to-1 completion reset, source-redirect cursor use, malformed/stale/wrong envelope fuzz, load-more, faults/recovery, and identity replacement; a constrained deep page must prove index-bounded work. Run the complete React/static/type/lint/build/package/Phase 2 regression matrix only after focused gates pass.
</verification>

<success_criteria>

- `src/component/public/comments.ts` and `src/component/admin/activity.ts` contain no independent merged-pagination algorithms and both delegate one shared helper.
- Comment cursors encode the full ascending `_creationTime,_id` key; activity cursors encode the existing descending `occurredAt,_creationTime,_id` key; cursor, endCursor, continueCursor, and splitCursor use one opaque versioned order/stream envelope.
- Single-post and merged-post reads use the same helper and page shape; cleanup repoints preserve pinned positions, while 1-to-2 cutover, 2-to-1 completion, source redirects, and stale/wrong/malformed envelopes reset to one fresh maximal coherent prefix without throw or overlap.
- No public DTO, validator, capability, authority argument, React state union, or package export is widened.
- No `postActivity.orderId`, schema/index, backfill/migration, activity-writer, or merge production change is introduced.
- Focused tests fail before implementation, then pass against actual component queries with single-post parity, equal-key, multi-page, pinned-window/repoint, reset-fuzz, bounded deep-page, privacy, and cleanup cases.
- Every real watch publication satisfies the exact canonical/prior/current or exact prefix/error oracle through load-more, insert/delete, fault/recovery, and identity replacement.
- The clean packed consumer and aggregate Phase 2 gate pass before Phase 2 is marked complete again and Phase 3 Plan 03-02 resumes.

</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-16-SUMMARY.md` when done.
</output>
