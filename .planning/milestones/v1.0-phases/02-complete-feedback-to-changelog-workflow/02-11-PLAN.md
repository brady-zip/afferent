---
phase: 02-complete-feedback-to-changelog-workflow
plan: "11"
type: execute
wave: 11
depends_on: ["02-10"]
files_modified:
  - src/component/_generated/api.ts
  - src/component/_generated/component.ts
  - src/component/_generated/dataModel.ts
  - src/component/_generated/server.ts
  - src/component/schema.ts
  - src/component/validators.ts
  - src/component/model/merge.ts
  - src/component/model/changelog.ts
  - src/component/model/views.ts
  - src/component/model/visibility.ts
  - src/component/admin/merge.ts
  - src/component/admin/activity.ts
  - src/component/admin/changelog.ts
  - src/component/jobs/merge.ts
  - src/component/public/posts.ts
  - src/component/public/comments.ts
  - src/component/participation/votes.ts
  - src/component/participation/comments.ts
  - src/component/participation/subscriptions.ts
  - src/component/notifications/events.ts
  - src/component/feedback.ts
  - src/client/contracts.ts
  - src/client/internal.ts
  - tests/model/merge.test.ts
  - tests/component/merge.test.ts
  - tests/integration/merge-backend.test.ts
  - scripts/test-merge-backend.mjs
autonomous: true
requirements: [DISC-07, DISC-08, QUAL-01]
must_haves:
  truths:
    - "A merge with at most 50 total affected relation rows commits atomically in one transaction; larger merges never alter source relation truth while preparing (D-07, D-12, D-56)."
    - "A large merge advances through preparing, ready, cutover_done, cleaning, and done (or pre-cutover aborted), with one job row selecting the complete pre- or post-merge reader truth (D-12)."
    - "Exactly one ready-to-cutover_done transaction publishes the flattened tombstone, redirect, staged union, and precomputed counters; later cleanup cannot change any observable result (D-07..D-12, D-53, D-54)."
    - "Resume and abort are idempotent, concurrent votes/comments are neither lost nor doubled, and the real Convex matrix proves every crash boundary and final convergence (QUAL-01)."
  artifacts:
    - path: src/component/model/merge.ts
      provides: "Hidden staging, write fencing, atomic cutover, and observationally inert cleanup primitives"
    - path: src/component/jobs/merge.ts
      provides: "Idempotent state-machine continuation, resume, abort cleanup, and convergence"
    - path: scripts/test-merge-backend.mjs
      provides: "Disposable real Convex continuous-observation, kill/resume, abort, and concurrency matrix"
  key_links:
    - from: src/component/participation/comments.ts
      to: src/component/model/merge.ts
      via: "Every active-merge relation write records a fenced delta or routes through post-cutover canonical truth"
    - from: src/component/public/comments.ts
      to: src/component/model/merge.ts
      via: "One merge job state selects source/canonical originals before cutover or the staged canonical union after cutover"
    - from: src/component/jobs/merge.ts
      to: src/component/admin/merge.ts
      via: "Only the ready state may enter the single bounded public cutover transaction"
---

<objective>
Replace the visible batch-reparent path for large duplicate merges with a genuinely atomic reader cutover and resumable hidden staging.

Purpose: Correct the executed Plan 02-05 D-12 gap without rewriting its preserved-history, durable-redirect, host, or headless contracts.
Output: Atomic small merge, staged large-merge state machine, write fencing, idempotent recovery/abort, and a real Convex observation matrix.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- ranked, searchable, and filterable discovery with admin duplicate merges; authenticated participation; admin moderation with a status-driven roadmap; manually published changelog entries linked to feedback; and in-app notifications backed by a host-consumable delivery outbox -- through tested component APIs and framework-light headless React hooks with explicit async, auth, and error states, **so that** my users and admins can complete the entire feedback lifecycle while I retain ownership of identity, authorization, and my own presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-PATTERNS.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-05-PLAN.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-05-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-10-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make atomic merge visibility executable on a real Convex backend</name>
  <files>tests/model/merge.test.ts, tests/component/merge.test.ts, tests/integration/merge-backend.test.ts, scripts/test-merge-backend.mjs</files>
  <read_first>src/component/model/merge.ts, src/component/admin/merge.ts, src/component/jobs/merge.ts, src/component/public/posts.ts, src/component/public/comments.ts, scripts/test-fanout-backend.mjs</read_first>
  <behavior>
    - A total affected-relation cardinality of 50 or fewer returns done from one mutation; 51 or more creates one preparing job and leaves every source/canonical original and public counter unchanged.
    - A continuous observer that reads canonical plus source comments, votes, subscriptions, links, activity, tags, notifications, redirect, and counters sees only the complete pre-merge snapshot until cutover and only the exact union afterward; no sample is mixed.
    - Killing and restarting continuation at preparing, ready, cutover_done, and cleaning converges once; abort during preparing/ready restores the unchanged pre-merge view; repeated resume/abort is idempotent.
    - Concurrent source/canonical vote and comment writes immediately before and during cutover appear exactly once in the post-merge union and counters.
  </behavior>
  <action>Replace the static source-text probe with a disposable anonymous Convex deployment modeled on `scripts/test-fanout-backend.mjs`. Seed duplicate and overlapping memberships above the threshold, poll a combined canonical-plus-source observation query continuously, expose narrow probe-only step/pause/resume/abort controls, terminate and restart the backend at every state, and assert every recorded snapshot is byte-for-byte either the expected pre-state or expected post-state. Add cycle, chained-tombstone flattening, hidden target, two-scope, exact vote-union, concurrent vote/comment, repeated scheduler delivery, abort, and final physical-convergence cases. Keep model/component tests for fast deterministic failures, but require the real deployment script to fail against the visible `processMergePhase` reparent implementation.</action>
  <acceptance_criteria>The first task is red because the current large path moves live rows during its batches and the current backend script does not execute Convex; the new matrix distinguishes those failures from harness/setup failures.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/model/merge.test.ts tests/component/merge.test.ts tests/integration/merge-backend.test.ts && node scripts/test-merge-backend.mjs</automated></verify>
  <done>D-12 is represented by an end-to-end real-backend observation oracle before implementation changes begin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace live batch reparenting with hidden staged truth and one atomic cutover</name>
  <files>src/component/schema.ts, src/component/validators.ts, src/component/model/merge.ts, src/component/model/changelog.ts, src/component/model/views.ts, src/component/model/visibility.ts, src/component/admin/merge.ts, src/component/jobs/merge.ts, src/component/public/posts.ts, src/component/public/comments.ts, src/component/admin/activity.ts, src/component/admin/changelog.ts, src/component/feedback.ts, src/client/contracts.ts, src/client/internal.ts</files>
  <read_first>tests/component/merge.test.ts, tests/integration/merge-backend.test.ts, src/component/schema.ts, src/component/model/merge.ts, src/component/public/comments.ts, src/component/model/changelog.ts</read_first>
  <action>Count the complete affected relation set, including memberships, source history, projections, links/guards, and notification relations. Preserve a true single-transaction fast path only when that total is at most 50. For larger work replace `pending|complete` plus visible phase moves with the durable states `preparing -> ready -> cutover_done -> cleaning -> done`, and allow `aborted` only before cutover. Build scope-owned hidden stage/delta rows carrying `mergeJobId`, relation kind, original row identity, logical dedupe key, and preparation generation; preparation copies or points to union candidates without patching/deleting any source original. Serialize conflicting incoming/outgoing merges so every affected post has at most one active job truth. Precompute exact vote/comment counters from deduplicated membership/relation truth and reach `ready` only when all bounded cursors are exhausted and the fenced delta queue is empty. Make public/direct/comment/changelog/admin-activity projection helpers consult that one job row: preparing/ready read unchanged originals; cutover_done/cleaning read the deduplicated staged union; done reads normalized originals. Implement exactly one bounded transaction that requires `ready`, changes the job to `cutover_done`, writes the source history and flattened tombstone/redirect, switches source visibility, installs the precomputed canonical counters, and appends merge activity. It must not loop over relation rows. Keep hidden/cross-scope redirects not-found-equivalent and preserve the existing public/headless result shapes. Run `npm run codegen:component` and include generated outputs; never hand-edit them.</action>
  <acceptance_criteria>No scheduled preparation mutation changes an original relation or any reader-visible projection; one job-state read selects complete truth, and the sole public switch is the ready-to-cutover_done transaction.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/model/merge.test.ts tests/component/merge.test.ts tests/static/schema-scope.test.ts tests/static/contracts.test.ts && node scripts/test-merge-backend.mjs</automated></verify>
  <done>Small merges are genuinely atomic and large merges expose one instantaneous logical transition independent of physical row count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fence concurrent writes and prove inert cleanup, resume, and abort</name>
  <files>src/component/model/merge.ts, src/component/jobs/merge.ts, src/component/admin/merge.ts, src/component/participation/votes.ts, src/component/participation/comments.ts, src/component/participation/subscriptions.ts, src/component/admin/changelog.ts, src/component/notifications/events.ts, tests/model/merge.test.ts, tests/component/merge.test.ts, tests/integration/merge-backend.test.ts, scripts/test-merge-backend.mjs</files>
  <read_first>src/component/participation/votes.ts, src/component/participation/comments.ts, src/component/participation/subscriptions.ts, src/component/admin/changelog.ts, src/component/notifications/events.ts, tests/integration/merge-backend.test.ts</read_first>
  <action>Centralize merge-aware relation writes. While a job is preparing, each source/canonical mutation updates original truth and transactionally records an idempotent job delta; while ready it records the delta and demotes the same job row to preparing. The ready-to-cutover transaction and every concurrent writer must contend on that row, so a writer commits entirely before preparation rechecks or entirely after cutover. After cutover, resolve source-ID participation to the final canonical post and update the post-cutover truth/counters once. Make cleanup move/deduplicate at most 50 originals and remove the matching stage row in the same transaction, preserving an identical logical union through `cutover_done -> cleaning -> done`; delete the last stage metadata only in the atomic handoff to ordinary canonical reads. Add idempotent resume that schedules at most one continuation for every nonterminal non-aborted state and returns terminal state unchanged. Add idempotent abort for preparing/ready that atomically publishes `aborted`, then removes invisible stage/delta rows in bounded retries without touching originals; after cutover it must return a typed conflict and cannot reverse the merge. Exercise repeated/out-of-order scheduler calls and re-run the real matrix, component/model suites, discovery/moderation/changelog/notification regressions, schema/contracts audits, typecheck, lint, and build.</action>
  <acceptance_criteria>Concurrent relation writes are never lost or duplicated, cleanup cannot change reader output, resume cannot create competing chains, abort cannot expose or destroy data, and all runs converge to one terminal job state.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/model/merge.test.ts tests/component/merge.test.ts tests/component/discovery.test.ts tests/component/moderation.test.ts tests/component/changelog.test.ts tests/component/notifications.test.ts tests/static/schema-scope.test.ts tests/static/contracts.test.ts && node scripts/test-merge-backend.mjs && npm run typecheck && npm run lint && npm run build</automated></verify>
  <done>The corrected merge state machine is crash-safe, write-safe, scope-safe, abortable before cutover, and physically convergent without another public transition.</done>
</task>

</tasks>

## Artifacts this phase produces

- Complete modified-path inventory: `src/component/model/merge.ts`, `src/component/admin/merge.ts`, `src/component/jobs/merge.ts`, merge-aware relation writers/readers, `tests/model/merge.test.ts`, `tests/component/merge.test.ts`, `tests/integration/merge-backend.test.ts`, and `scripts/test-merge-backend.mjs`.
- Hidden scope-owned merge stage and delta storage keyed by `mergeJobId`, original relation identity, logical dedupe key, and preparation generation.
- `MergeJobState`: `preparing | ready | cutover_done | cleaning | done | aborted` with guarded transition helpers.
- Exact total-row `MERGE_ATOMIC_LIMIT = 50`, bounded `MERGE_BATCH_SIZE = 50`, precomputed union counters, and a row-free atomic cutover helper.
- Merge-aware reader projection helpers and centralized relation-write fencing/routing.
- Idempotent resume and pre-cutover abort operations with typed terminal/conflict results.
- A disposable real Convex harness that continuously records C+S reader observations and proves kill/resume, abort, concurrency, redirect, cycle, union, and convergence behavior.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Scheduled preparation -> public readers | Hidden stage rows must not leak or alter original relation truth before cutover. |
| Concurrent participation -> merge job | Votes/comments can race preparation and the sole public cutover. |
| Job state -> reader projection | One scoped row decides whether readers consume original or staged union truth. |
| Resume/abort/cleanup -> durable data | Repeated or out-of-order invocations must not duplicate work, reverse cutover, or delete originals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-39 | Information Disclosure / Tampering | pre-cutover staging | critical | mitigate | Separate hidden stage/delta rows, scope-leading indexes, original rows untouched, and continuous pre/post-only reader assertions. |
| T-02-40 | Tampering | ready/cutover race | critical | mitigate | Writers and cutover touch one job row; ready demotion, empty-delta fence, and OCC serialization prevent lost writes. |
| T-02-41 | Tampering | vote/comment union | high | mitigate | Actor/logical-key dedupe, membership-derived counters, concurrent real-backend matrix, never arithmetic source-plus-canonical totals. |
| T-02-42 | Information Disclosure | source redirect and staged readers | high | mitigate | Scope recheck, canonical visibility predicate, one active job per affected post, and not-found equivalence. |
| T-02-43 | Denial of Service | preparation/cleanup | high | mitigate | Exact atomic threshold, 50-row indexed batches, one scheduled continuation, idempotent cursors/generations, and terminal convergence tests. |
| T-02-44 | Tampering / Repudiation | abort/resume | high | mitigate | Closed transition table, pre-cutover-only abort, bounded invisible cleanup, schedule guard, and repeated/out-of-order invocation tests. |
| T-02-45 | Tampering | physical cleanup handoff | high | mitigate | Move/dedupe plus stage removal in one transaction and delete final stage metadata only with the done-state reader handoff. |
</threat_model>

<verification>
Run the red-first model/component tests, the actual disposable Convex continuous-observation matrix, schema/contracts audits, affected lifecycle regressions, typecheck, lint, and build. Static source-text assertions cannot satisfy the real-backend gate.
</verification>

<success_criteria>
- DISC-07/08 preserve exact relation truth and durable flattened redirects without any half-merged reader state.
- D-12 is proven continuously across every large-job state, backend kill/resume boundary, abort, concurrent write, and cleanup batch.
- The <=50 path is one transaction; the >50 path has one row-free public cutover transaction and observationally inert cleanup.
- QUAL-01 runs the merge behavior on actual Convex rather than inspecting implementation strings.
</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-11-SUMMARY.md` when done.
</output>
