---
phase: 02-complete-feedback-to-changelog-workflow
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/component/_generated/api.ts
  - src/component/_generated/component.ts
  - src/component/_generated/dataModel.ts
  - src/component/_generated/server.ts
  - package.json
  - package-lock.json
  - src/component/schema.ts
  - src/component/validators.ts
  - src/component/model/content.ts
  - src/component/model/similarity.ts
  - src/component/public/search.ts
  - src/component/participation/posts.ts
  - src/component/participation/comments.ts
  - src/component/feedback.ts
  - src/client/contracts.ts
  - src/client/internal.ts
  - src/client/index.ts
  - src/react/hooks/feedback.ts
  - tests/model/content.test.ts
  - tests/model/similarity.test.ts
  - tests/component/search.test.ts
  - tests/integration/search-backend.test.ts
  - tests/react/feedback-search.test.tsx
  - scripts/test-search-backend.mjs
autonomous: true
requirements: [DISC-04, DISC-06, ADMN-09, UI-01, UI-02, UI-03, QUAL-01]
must_haves:
  truths:
    - "Visitors receive bounded relevance-only search results with honest truncation, optional board/status/single-tag filters, and no cursor claim."
    - "Composers receive deterministic cross-board similar-post suggestions but can always submit (D-05, D-06)."
    - "Stored post/comment/changelog Markdown is normalized, bounded, and rejects executable/unsafe constructs (D-13, D-14)."
  artifacts:
    - path: src/component/public/search.ts
      provides: "Bounded canonical and per-tag search plus deterministic similarity"
    - path: src/component/model/content.ts
      provides: "Parser-backed safe-Markdown and plain-text validation"
    - path: tests/integration/search-backend.test.ts
      provides: "Real-backend proof of bounded search and pagination boundary"
  key_links:
    - from: src/component/public/search.ts
      to: src/component/schema.ts
      via: "Native search indexes and per-tag materialized projections"
    - from: src/react/hooks/feedback.ts
      to: src/component/public/search.ts
      via: "Debounced bounded result contract with `hasMore`, never pagination cursors"
---

<objective>
Add safe relevance search and deterministic compose-time duplicate suggestions as a complete component-to-hook slice.

Purpose: Make discovery credible without promising unsupported component search pagination or unsafe stored content.
Output: Approved parser dependency, safe-content model, search projections, bounded search/similarity contracts, hooks, and real-backend proof.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- ranked, searchable, and filterable discovery with admin duplicate merges; authenticated participation; admin moderation with a status-driven roadmap; manually published changelog entries linked to feedback; and in-app notifications backed by a host-consumable delivery outbox -- through tested component APIs and framework-light headless React hooks with explicit async, auth, and error states, **so that** my users and admins can complete the entire feedback lifecycle while I retain ownership of identity, authorization, and my own presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-PATTERNS.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify bounded search, deterministic suggestions, and safe content</name>
  <files>tests/model/content.test.ts, tests/model/similarity.test.ts, tests/component/search.test.ts, tests/integration/search-backend.test.ts, tests/react/feedback-search.test.tsx, scripts/test-search-backend.mjs</files>
  <read_first>src/component/schema.ts, src/component/participation/posts.ts, src/react/hooks/feedback.ts, .planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md</read_first>
  <behavior>
    - Search uses bounded relevance with `items` plus `hasMore`, supports explicit board/status/single-tag shapes, and never exposes a cursor (D-03, D-04).
    - Similar suggestions rerank a bounded cross-board candidate pool deterministically and omit raw engine scores (D-05, D-06).
    - Markdown allows headings/emphasis/lists/blockquote/code and hardened HTTP(S)/mailto links while rejecting HTML, images/media, scripts/styles/iframes, and unsafe schemes (D-13, D-14).
  </behavior>
  <action>Write red model/component/React tests plus a disposable real-Convex integration that proves `.take(HARD_LIMIT + 1)` search works while native component and helper search pagination remain unsupported. Freeze `SEARCH_HARD_LIMIT=50`, `SIMILAR_CANDIDATE_LIMIT=30`, default suggestions 5, hard suggestions 10, debounce 250ms, and max 20 tags per post. Exercise two scopes and hidden rows so search metadata cannot leak existence.</action>
  <acceptance_criteria>Tests fail at missing content/search/similarity contracts, and the real-backend contract explicitly distinguishes ordinary cursor feeds from bounded relevance search.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/model/content.test.ts tests/model/similarity.test.ts tests/component/search.test.ts tests/react/feedback-search.test.tsx</automated></verify>
  <done>Red evidence covers DISC-04/06 and ADMN-09 without revisiting the verified search boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement safe content and scope-complete search projections</name>
  <files>package.json, package-lock.json, src/component/schema.ts, src/component/validators.ts, src/component/model/content.ts, src/component/model/similarity.ts, src/component/public/search.ts, src/component/participation/posts.ts, src/component/participation/comments.ts, src/component/feedback.ts</files>
  <read_first>tests/model/content.test.ts, tests/model/similarity.test.ts, tests/component/search.test.ts, .planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md</read_first>
  <action>Install exact approved `mdast-util-from-markdown@2.0.3` as a runtime dependency from the RESEARCH package-legitimacy audit. Implement normalized plain text (titles/names max 160) and safe Markdown (post/comment/changelog bodies max 10,000) via a closed AST allowlist; consumers render an element tree and never raw HTML. Add canonical post search equality fields plus a scoped per-tag search-projection table/search index. Update create/edit/move/status/tag/visibility paths that exist so projections stay transactional and bounded. Implement `searchFeedback` and `suggestSimilarPosts` with native search `.take`, deterministic lexical reranking `(score desc, createdAt desc, opaqueId)`, stable DTOs, honest `hasMore`, and no relevance score/cursor (D-03..D-06, D-13, D-14). Run `npm run codegen:component` and include generated outputs; never hand-edit them.</action>
  <acceptance_criteria>Every supported search filter is search-index served or a bounded projection intersection; unsafe content is rejected at write time; search/suggestion DTOs are versioned, bounded, and privacy-safe.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/model/content.test.ts tests/model/similarity.test.ts tests/component/search.test.ts && node scripts/test-search-backend.mjs</automated></verify>
  <done>Component search and suggestions work on the real backend without unsupported pagination or post-scan filtering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Carry bounded discovery through host capabilities and debounced hooks</name>
  <files>src/client/contracts.ts, src/client/internal.ts, src/client/index.ts, src/react/hooks/feedback.ts, tests/react/feedback-search.test.tsx</files>
  <read_first>src/component/public/search.ts, src/client/contracts.ts, src/react/hooks/feedback.ts, tests/react/feedback-search.test.tsx</read_first>
  <action>Add distinct `SearchResultDto`/`SimilarPostResultDto` contracts and narrow host reads. Extend feedback hooks with `useFeedbackSearch` and `useSimilarPosts`; debounce by default at 250ms with an override, expose `hasMore`, loading/ready/empty/error/unsupported states, and never fabricate `loadMore`. Similarity remains non-blocking and does not disable submission. Expected failures map to typed hook state rather than navigation/toasts.</action>
  <acceptance_criteria>A consumer can search/filter and request similar posts through injected refs with explicit async states, while hook typings make cursor pagination impossible for these two operations.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/react/feedback-search.test.tsx && npm run typecheck && npm run lint</automated></verify>
  <done>The safe bounded search/suggestion experience is complete through headless React.</done>
</task>

</tasks>

## Artifacts this phase produces

- Complete new-path inventory: `src/component/model/content.ts`, `src/component/model/similarity.ts`, `src/component/public/search.ts`, `tests/model/content.test.ts`, `tests/model/similarity.test.ts`, `tests/component/search.test.ts`, `tests/integration/search-backend.test.ts`, `tests/react/feedback-search.test.tsx`, `scripts/test-search-backend.mjs`.
- `src/component/model/content.ts`: `normalizePlainText`, `validateSafeMarkdown`, `SAFE_MARKDOWN_LIMITS`, and safe node/scheme constants.
- `src/component/model/similarity.ts`: `scoreSimilarity`, `rankSimilarCandidates`, and limits.
- `src/component/public/search.ts`: `searchFeedback`, `suggestSimilarPosts`.
- `SearchResultDto`, `SimilarPostResultDto`, their validators, intents, and host capability methods.
- `useFeedbackSearch`, `useSimilarPosts`.
- `tests/integration/search-backend.test.ts` and `scripts/test-search-backend.mjs` real-backend seam.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User Markdown -> component storage | Persisted untrusted content can reach conforming renderers. |
| Browser query -> search index | Search/filter input can probe scope and hidden records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-05 | Tampering / Elevation | safe Markdown | high | mitigate | Closed parser AST allowlist, hardened URL schemes, bounded source, and element-tree renderer contract. |
| T-02-06 | Information Disclosure | search projections | high | mitigate | Required scope/visibility equality fields, scoped tag projection, no post-filtering, two-scope tests. |
| T-02-07 | Denial of Service | search/similarity | medium | mitigate | Hard candidate/result caps, debounce, bounded tag cardinality, and no cursor loop. |
| T-02-SC | Tampering | npm install | high | mitigate | Install only audit-approved `mdast-util-from-markdown@2.0.3`; verify lockfile, repository, no lifecycle script, packed resolution. |
</threat_model>

<verification>
Run focused suites, the real-backend harness, typecheck/lint, then the packed artifact gate after building declarations.
</verification>

<success_criteria>
- DISC-04/06 and ADMN-09 are implemented through component, host, and hooks.
- Search/similarity remain bounded and non-cursor on Convex 1.42.2/convex-helpers 0.1.120.
- Safe Markdown cannot become stored script execution in conforming consumers.
</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-02-SUMMARY.md` when done.
</output>
