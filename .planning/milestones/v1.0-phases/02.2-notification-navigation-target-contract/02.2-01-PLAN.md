---
phase: 02.2-notification-navigation-target-contract
plan: "01"
type: execute
wave: 1
depends_on: ["02.1-01"]
files_modified:
  - src/client/contracts.ts
  - src/component/validators.ts
  - src/component/notifications/inbox.ts
  - src/component/model/merge.ts
  - src/component/_generated/api.ts
  - src/component/_generated/component.ts
  - src/component/_generated/dataModel.ts
  - src/component/_generated/server.ts
  - src/react/bindings.ts
  - src/react/hooks/notifications.ts
  - tests/component/notifications.test.ts
  - tests/component/merge.test.ts
  - tests/static/contracts.test.ts
  - tests/static/notification-targets.test.ts
  - tests/react/notifications.test.tsx
  - tests/react/live-headless.test.tsx
  - tests/integration/packed-artifact.test.mjs
  - fixtures/packed-vite-convex/src/App.tsx
  - fixtures/packed-vite-convex/src/App.test.tsx
  - fixtures/registry-vite/src/App.tsx
  - scripts/test-headless-backend.mjs
  - scripts/test-merge-backend.mjs
autonomous: true
requirements: [NOTF-08]
must_haves:
  truths:
    - "D-01: NotificationDto version 2 contains a required closed post-or-changelog target and exposes no public polymorphic entityId; D-03: every target has the exact server-authored human-readable accessible label."
    - "D-02: Status targets contain the canonical post ID; reply and mention targets add only a same-scope, same-post optional comment anchor; changelog targets contain the public slug and never the internal entry ID; D-06: every referent and relationship is scope-validated with safe optional-anchor degradation."
    - "D-05: Every target is resolved once inside the component from notification source records; D-09: headless hooks surface it verbatim; D-10: copied UI owns href construction with no direct/N+1 reads; D-11: packed and registry consumers use the exported type."
    - "D-07: Merge normalization always canonicalizes event.postId, rewrites entityId only for status events, preserves comment/changelog identity, and degrades legacy-invalid anchors; D-08: both small and resumable paths preserve all other notification behavior."
    - "D-04: NotificationDto and NotificationPageDto move explicitly from version 1 to 2 in types, validators, generated references, fixtures, and packed consumption while DeliveryEventDto remains version 1 and unchanged."
    - "D-12: RED matrices cover exact event/relation/version/merge behavior; D-13: static guards reject authority, ID, scan, and N+1 regressions; D-14: focused, packed, build/type/lint, complete Phase 2, and fingerprint gates pass before Phase 3 Plan 03-03 resumes."
  artifacts:
    - path: src/client/contracts.ts
      provides: "Closed NotificationTarget union and explicitly bumped notification/page DTOs"
      exports: ["NotificationTarget", "NotificationDto", "NotificationPageDto"]
    - path: src/component/notifications/inbox.ts
      provides: "Scope-checked source-record target resolver used by inbox and mark-read projection"
    - path: src/component/model/merge.ts
      provides: "Event-type-aware notification relation normalization for both merge paths"
    - path: src/react/hooks/notifications.ts
      provides: "Verbatim target surface with existing pagination, mark-read optimism, and generation fencing"
    - path: tests/static/notification-targets.test.ts
      provides: "No-entityId, no-internal-ID, no-browser-authority, no-N+1, and no-scan contract guards"
    - path: fixtures/packed-vite-convex/src/App.tsx
      provides: "Clean installed-artifact consumption of closed navigation targets"
  key_links:
    - from: src/component/notifications/inbox.ts
      to: src/client/contracts.ts
      via: "Each inbox row maps its trusted event source into exactly one closed public target"
      pattern: "target|kind|commentId|slug|label"
    - from: src/component/notifications/inbox.ts
      to: src/component/schema.ts
      via: "Post, comment, and changelog referents are loaded and relationship-checked in the event scope"
      pattern: "notificationEvents|postId|entityId"
    - from: src/component/model/merge.ts
      to: src/component/notifications/inbox.ts
      via: "Merge cutover preserves the type-specific source identity needed by target projection"
      pattern: "status_changed|entityId|postId"
    - from: src/react/hooks/notifications.ts
      to: src/client/contracts.ts
      via: "Headless notification state carries server target truth without reconstructing routes or referents"
      pattern: "NotificationDto|target"
    - from: fixtures/packed-vite-convex/src/App.tsx
      to: src/react/hooks/notifications.ts
      via: "Packed consumer renders target labels and dispatches by the closed kind"
      pattern: "target.kind|target.label"
---

<objective>
Replace the public polymorphic notification identifier with the exact accessible navigation target that copied notification UI needs, red-first, without adding browser authority or read amplification.

Purpose: Let Phase 3 render reliable post/comment/changelog links directly from versioned server truth while retaining the isolated component and trusted-host boundary.
Output: Closed target union, server source resolution, type-aware merge normalization, aligned DTO/page versions, verbatim headless consumption, and adversarial packed/full-regression proof.
</objective>

## Phase Goal

**As a** React integrator, **I want to** receive a closed accessible notification destination from Afferent, **so that** my UI can navigate without knowing component IDs or fetching each target separately.

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-notification-navigation-target-contract/02.2-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-08-PLAN.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-08-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-11-PLAN.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-11-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-12-PLAN.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-12-SUMMARY.md
@.planning/phases/03-source-owned-product-interface/03-CONTEXT.md
@.planning/phases/03-source-owned-product-interface/03-UI-SPEC.md
@.planning/phases/03-source-owned-product-interface/03-03-PLAN.md
@src/client/contracts.ts
@src/component/schema.ts
@src/component/validators.ts
@src/component/notifications/inbox.ts
@src/component/model/merge.ts
@src/react/hooks/notifications.ts

<interfaces>
From `src/client/contracts.ts` and `src/component/validators.ts`:
- `NotificationDto` and `NotificationPageDto` are version 1 and expose `entityId`. Replace that field with the locked `NotificationTarget`; bump both containers to version 2 and keep exact type/validator parity.
- `DeliveryEventDto.entityId` is a separate version 1 host delivery contract and remains unchanged.

From `src/component/schema.ts` and notification producers:
- `notificationEvents` contains `postId` and polymorphic `entityId`; status uses a post ID, reply/mention uses a comment ID, and changelog publication uses a changelog entry ID.
- Resolve post labels from the same-scope post title, comment anchors only after same-scope/same-post validation, and changelog slug/label from the same-scope entry.

From `src/component/model/merge.ts`:
- Existing relation cutover rewrites both `postId` and `entityId` to the canonical post for every event. Change both bounded merge paths so `postId` always canonicalizes, but `entityId` changes only for `status_changed`.
- Legacy invalid comment anchors are optional and must not make the whole post destination unreadable.

From `src/react/hooks/notifications.ts` and Phase 3 Plan `03-03`:
- Preserve the current paginated state, actor/auth behavior, mark-read key/pending/error/reset/optimism, and account-generation fencing.
- UI consumes `target.kind`, `target.postId`/`commentId` or `target.slug`, and `target.label`; it performs no component-record read and receives no internal changelog ID.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the navigation target and merge contract with failing tests</name>
  <files>tests/component/notifications.test.ts, tests/component/merge.test.ts, tests/static/contracts.test.ts, tests/static/notification-targets.test.ts, tests/react/notifications.test.tsx, tests/react/live-headless.test.tsx, tests/integration/packed-artifact.test.mjs, fixtures/packed-vite-convex/src/App.test.tsx</files>
  <read_first>.planning/phases/02.2-notification-navigation-target-contract/02.2-CONTEXT.md, src/client/contracts.ts, src/component/schema.ts, src/component/validators.ts, src/component/notifications/inbox.ts, src/component/model/merge.ts, src/react/hooks/notifications.ts, tests/component/notifications.test.ts, tests/component/merge.test.ts, tests/static/contracts.test.ts</read_first>
  <behavior>
    - Red event matrix expects exact accessible post, comment-anchor, and public-slug changelog targets for all five notification types and exact DTO/page version 2.
    - Cross-scope posts/comments/changelog entries, mismatched comment/post pairs, and forged target/browser fields never leak or redirect across scope.
    - Small and resumable source-to-canonical merges preserve valid comment and changelog source IDs while canonicalizing the destination post; legacy-clobbered comment identity yields no commentId.
    - Static guards reject NotificationDto.entityId, internal changelog IDs, target construction outside component projection, per-item direct reads, table scans, and any target authority input in bindings.
    - Headless and packed tests consume targets verbatim and prove mark-read optimism/generation behavior is unchanged.
  </behavior>
  <action>Add focused RED tests before production edits. Cover every event kind, readable label text, version literals, optional anchor degradation, source/canonical relations in both merge modes, and exact absence of internal IDs. Keep delivery outbox expectations at version 1/entityId. Run only the focused matrix, confirm failure is caused by the missing target/version/merge behavior, then commit exact test paths with `h5i capture commit -m "test(02.2-01): specify notification navigation targets" --agent codex --tests --audit`.</action>
  <acceptance_criteria>A committed RED specification fully distinguishes public navigation targets from delivery identities and fails only on the missing locked contract.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/notifications.test.ts tests/component/merge.test.ts tests/static/contracts.test.ts tests/static/notification-targets.test.ts && npm exec -- vitest run --config vitest.react.config.ts tests/react/notifications.test.tsx tests/react/live-headless.test.tsx</automated></verify>
  <done>All event, relationship, merge, static, headless, and packed expectations exist as a focused failing contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement server-resolved versioned targets and type-aware merge normalization</name>
  <files>src/client/contracts.ts, src/component/validators.ts, src/component/notifications/inbox.ts, src/component/model/merge.ts, src/component/_generated/api.ts, src/component/_generated/component.ts, src/component/_generated/dataModel.ts, src/component/_generated/server.ts, tests/component/notifications.test.ts, tests/component/merge.test.ts, tests/static/contracts.test.ts, tests/static/notification-targets.test.ts, scripts/test-merge-backend.mjs</files>
  <read_first>tests/component/notifications.test.ts, tests/component/merge.test.ts, tests/static/notification-targets.test.ts, src/client/contracts.ts, src/component/validators.ts, src/component/notifications/inbox.ts, src/component/model/merge.ts, src/component/schema.ts, scripts/test-merge-backend.mjs</read_first>
  <behavior>
    - NotificationTarget is a closed version 1 discriminated union, required on every NotificationDto version 2 row and NotificationPageDto version 2 page.
    - Projection resolves one trusted source event to one scope-complete target and emits the exact locked English accessible labels.
    - Comment anchors require a normalized same-scope comment whose post matches event.postId; invalid legacy anchors are omitted, not leaked or guessed.
    - Changelog projection emits only slug/title-derived label; it never emits entry ID or asks the browser to resolve it.
    - Merge cutover changes event.entityId only for status_changed while maintaining canonical postId and existing bounded/idempotent relation processing for every event type.
  </behavior>
  <action>Add the union/types and exact validators, replace inbox `entityId` projection with a dedicated scope-checking resolver, and bump notification/page versions everywhere they are validated or constructed. Update merge notification patching in both small and resumable paths to branch on event type. Preserve all delivery DTOs and outbox storage. Regenerate component references with `npm run codegen:component`, inspect generated diffs, take component/static/backend merge tests green, and commit owned paths with `h5i capture commit -m "feat(02.2-01): add server-resolved notification targets" --agent codex --tests --audit`.</action>
  <acceptance_criteria>All public notification rows are versioned closed destinations resolved within scope, and both merge paths retain the exact source identity each target needs.</acceptance_criteria>
  <verify><automated>npm run codegen:component && npm exec -- vitest run tests/component/notifications.test.ts tests/component/merge.test.ts tests/static/contracts.test.ts tests/static/notification-targets.test.ts && npm run test:merge-backend && npm run typecheck && npm run lint</automated></verify>
  <done>The component/types/validators/generated references are green with no public polymorphic or internal changelog identifier.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove verbatim headless and clean packed consumption</name>
  <files>src/react/bindings.ts, src/react/hooks/notifications.ts, tests/react/notifications.test.tsx, tests/react/live-headless.test.tsx, fixtures/packed-vite-convex/src/App.tsx, fixtures/packed-vite-convex/src/App.test.tsx, fixtures/registry-vite/src/App.tsx, tests/integration/packed-artifact.test.mjs, scripts/test-headless-backend.mjs, scripts/test-merge-backend.mjs</files>
  <read_first>src/react/bindings.ts, src/react/hooks/notifications.ts, tests/react/notifications.test.tsx, tests/react/live-headless.test.tsx, fixtures/packed-vite-convex/src/App.tsx, fixtures/packed-vite-convex/src/App.test.tsx, tests/integration/packed-artifact.test.mjs, .planning/phases/03-source-owned-product-interface/03-03-PLAN.md, package.json</read_first>
  <behavior>
    - Bindings and hooks carry target DTOs without target arguments, local route policy, referent joins, or event-type reconstruction.
    - Notification pagination, unread state, mark-read desired state, errors, and identity-generation resets retain existing observable behavior.
    - Packed/registry fixtures compile against version 2, render accessible target labels, and dispatch only by the closed target kind.
    - Focused headless/backend/merge/packed gates and the full Phase 2 regression pass with the unrelated dirty fingerprint unchanged.
  </behavior>
  <action>Make only contract-forced binding/hook/fixture changes; do not add a router or direct Convex reads. Update packed UI/tests to demonstrate post, anchored-comment, and changelog target consumption from the installed tarball. Run focused React/headless/merge/packed gates, then build/type/lint and `npm run test:phase2`. Require unrelated diff hash `09b5f7179dc749a197ec6a59c5ba188313b8c84536e0c35b6487ae7fcb058381`, inspect `git diff --check` and exact staged paths, and commit with `h5i capture commit -m "feat(02.2-01): surface notification targets to consumers" --agent codex --tests --audit`.</action>
  <acceptance_criteria>The real headless and packed paths consume the closed target without extra reads or authority, all regression gates pass, and 03-03 has an executable dependency.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run --config vitest.react.config.ts tests/react/notifications.test.tsx tests/react/live-headless.test.tsx && npm run test:headless-backend && npm run test:merge-backend && node --test tests/integration/packed-artifact.test.mjs && npm run build && npm run typecheck && npm run lint && npm run test:phase2 && test "$(git diff -- .codex/config.toml .codex/hooks.json package-lock.json package.json | shasum -a 256 | awk '{print $1}')" = "09b5f7179dc749a197ec6a59c5ba188313b8c84536e0c35b6487ae7fcb058381" && git diff --check</automated></verify>
  <done>The navigation target contract is proven through installed/headless consumers and Phase 3 Plan 03-03 may execute after independent verification.</done>
</task>

</tasks>

## Prohibitions

- Do not expose public `NotificationDto.entityId`, a changelog entry ID, component documents, provider records, or browser-supplied target/identity/scope/permission facts.
- Do not move target resolution into host wrappers, React hooks, or copied UI; do not add per-notification direct reads, unbounded queries, or client-side joins.
- Do not rewrite comment or changelog `entityId` during merge normalization; do not leave a source post destination after canonicalization.
- Do not silently widen a versioned DTO/container or change DeliveryEventDto/outbox semantics.
- Do not add route ownership, localization infrastructure, copied UI, new dependencies, or edits to completed Phase 2 planning evidence.
- Do not stage `.codex/config.toml`, `.codex/hooks.json`, `package.json`, `package-lock.json`, `.codex/hooks/**`, `.npmrc`, `.planning/debug/**`, or unrelated dirty work.

## Source Coverage Audit

Decision coverage index:

- D-01: Task 2 implements the closed target union and removes public entityId.
- D-02: Tasks 1-2 cover the exact event-to-post/comment/changelog mapping.
- D-03: Tasks 1-2 lock and implement the exact accessible label text.
- D-04: Tasks 1-3 align notification row/page v2 while preserving delivery v1.
- D-05: Tasks 1-2 require component-side source resolution and forbid consumer joins.
- D-06: Tasks 1-2 cover scope/relation validation and safe optional-anchor degradation.
- D-07: Tasks 1-2 implement type-aware post/entity merge normalization.
- D-08: Tasks 1-3 cover both merge modes and preserve all other notification behavior.
- D-09: Task 3 surfaces target truth verbatim through headless hooks.
- D-10: The plan and downstream dependency reserve href ownership for Phase 3 with no direct reads.
- D-11: Task 3 proves the exported contract in packed and registry fixtures.
- D-12: Task 1 starts with the exact RED event/relation/version/merge matrix.
- D-13: Task 1 adds every locked static authority, ID, scan, and N+1 guard.
- D-14: Task 3 runs focused, packed, build/type/lint, full regression, and fingerprint gates.

| Source | ID / constraint | Plan coverage | Status |
| --- | --- | --- | --- |
| GOAL | Host-owned authority, narrow versioned DTOs, no internal records | Server-only closed target resolution, explicit versions, and no browser target inputs preserve the boundary. | COVERED |
| REQ | NOTF-08 | All notification navigation fields, labels, public slugs, and no-internal-ID/N+1 guarantees are covered by Tasks 1-3. | COVERED |
| CONTEXT | Closed target and versioning | Tasks 1-2 lock exact shapes, labels, NotificationDto/Page v2, and unchanged DeliveryEventDto v1. | COVERED |
| CONTEXT | Source resolution and merge | Tasks 1-2 cover scope/relationship validation plus small/resumable type-aware merge normalization and legacy anchor degradation. | COVERED |
| CONTEXT | Headless/navigation boundary | Task 3 covers verbatim hooks, packed consumers, no router ownership, and no direct reads. | COVERED |
| DOWNSTREAM | Phase 03 Plan 03-03 | 03-03 depends on 02.2-01 and can construct injected links from the target alone. | COVERED |
| DEFERRED | Delivery URL/router/localization/copied UI | Explicitly prohibited and left to hosts or Phase 3. | EXCLUDED |

No source decision is missing. Phase 02.2 owns only the notification navigation prerequisite; Phase 02.3 owns admin reads and Phase 3 owns copied presentation.

## Artifacts this phase produces

- `NotificationTarget` version 1 closed post/changelog union with exact accessible labels.
- `NotificationDto` version 2 and `NotificationPageDto` version 2 with no public polymorphic entity ID.
- Scope-checked post/comment/changelog source resolution in the component.
- Type-aware notification merge normalization for small and resumable cutovers.
- Verbatim headless and clean packed-tarball consumption without N+1/direct reads.
- Adversarial version, relationship, scope, no-scan, no-authority, merge, and full Phase 2 regression evidence.

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Stored event -> public notification DTO | Polymorphic internal source identity is converted to one scope-checked closed public target. |
| Merge source -> canonical destination | Post destinations normalize without corrupting comment/changelog source identity. |
| Component DTO -> host/headless/UI | Consumers receive presentation-ready targets and cannot assert or resolve authority/referents. |
| Packed source -> consumer | Versioned target types and validators must survive tarball installation exactly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-02.2-01 | Information Disclosure | NotificationDto | critical | mitigate | Remove public entityId; emit only post/comment opaque public IDs or public slug after scope checks. |
| T-02.2-02 | Spoofing | browser bindings | critical | mitigate | No target/scope/identity inputs; static and runtime adversarial guards. |
| T-02.2-03 | Information Disclosure | cross-scope relation resolution | critical | mitigate | Validate every post/comment/changelog referent and relation in the event scope. |
| T-02.2-04 | Tampering | merge normalization | high | mitigate | Event-type-aware entity preservation in both merge paths with exact tests. |
| T-02.2-05 | Repudiation / Tampering | contract versioning | high | mitigate | Explicit DTO/page bumps, exact validators/generated refs, and packed compilation. |
| T-02.2-06 | Denial of Service | UI target resolution | high | mitigate | Resolve in inbox projection; static no-N+1/no-direct-read/no-scan guards. |
| T-02.2-07 | Tampering | legacy invalid anchors | medium | mitigate | Omit optional commentId and retain the validated canonical post destination. |
| T-02.2-SC | Tampering | dependencies | low | accept | No dependency change authorized; packed and full regression gates remain required. |

</threat_model>

<verification>
Preserve a committed RED target/merge contract, then take component types, validators, resolver, and both merge paths green before updating consumers. Finish with generated-diff inspection, focused React/headless/merge/packed gates, build/type/lint, complete `npm run test:phase2`, exact unrelated-diff fingerprint, `git diff --check`, and staged-path inspection. Every execution commit uses `h5i capture commit --agent codex` with `--tests --audit`; independent verification gates Plan 03-03.
</verification>

<success_criteria>

- Every in-app notification has an exact accessible post/comment or public-slug changelog target resolved in the component.
- No public polymorphic/internal changelog ID, browser target authority, cross-scope relation, direct read, N+1 join, or unbounded target query exists.
- Notification DTO/page version 2 is aligned across types, validators, generated refs, headless hooks, fixtures, and packed installation while delivery remains unchanged.
- Both merge modes preserve canonical navigation and type-specific source identity, with safe legacy comment degradation.
- Focused and complete Phase 2 gates pass with unrelated dirty work untouched.

</success_criteria>

<output>
Create `.planning/phases/02.2-notification-navigation-target-contract/02.2-01-SUMMARY.md` when done.
</output>
