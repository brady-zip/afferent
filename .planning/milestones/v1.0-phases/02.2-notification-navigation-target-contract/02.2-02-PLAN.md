---
phase: 02.2-notification-navigation-target-contract
plan: "02"
type: execute
wave: 2
depends_on: ["02.2-01"]
files_modified:
  - src/component/notifications/inbox.ts
  - tests/component/notifications.test.ts
autonomous: true
gap_closure: true
requirements: [NOTF-08]
must_haves:
  truths:
    - "A reply or mention target uses `View comment on feedback: {post title}` only when a validated same-scope, same-post commentId is present; every legacy-invalid, cross-scope, or mismatched optional comment degrades to a post-only target labelled `View feedback: {post title}`."
    - "Executable component tests prove cross-scope required post and changelog referents fail with exact invariant errors and leak no destination, while cross-scope and same-scope-wrong-post optional comments degrade to the exact safe post-only target."
    - "The original valid five-event target matrix remains unchanged and green; focused component/static gates, the complete Phase 2 regression, diff checks, and the protected unrelated-worktree fingerprint all pass."
  artifacts:
    - path: src/component/notifications/inbox.ts
      provides: "Anchor-dependent accessible label selection after optional comment validation"
    - path: tests/component/notifications.test.ts
      provides: "Executable required-referent invariant and optional-comment safe-degradation matrix"
  key_links:
    - from: src/component/notifications/inbox.ts
      to: tests/component/notifications.test.ts
      via: "Target label and optional commentId are derived from the same validated comment branch"
      pattern: "validComment|View feedback|View comment on feedback"
---

<objective>
Close only the two deterministic failures in the canonical Phase 02.2 verification: the post-only fallback label and the missing runtime scope/relation matrix.

Purpose: Make the already-shipped closed notification target contract satisfy its exact accessible-label and adversarial-proof requirements without reopening verified DTO, merge, headless, packed, or delivery behavior.
Output: One corrected conditional label branch plus executable cross-scope and mismatched-relation component tests, proven by focused and complete Phase 2 gates.
</objective>

## Phase Goal

**As a** notification consumer, **I want to** receive an accessible label that matches the destination actually present, **so that** invalid optional anchors degrade safely while required cross-scope referents fail without leaking data.

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.2-notification-navigation-target-contract/02.2-CONTEXT.md
@.planning/phases/02.2-notification-navigation-target-contract/02.2-VERIFICATION.md
@.planning/phases/02.2-notification-navigation-target-contract/02.2-01-PLAN.md
@.planning/phases/02.2-notification-navigation-target-contract/02.2-01-SUMMARY.md
@src/component/notifications/inbox.ts
@tests/component/notifications.test.ts
@tests/static/notification-targets.test.ts

<interfaces>
From canonical `02.2-VERIFICATION.md`:
- Gap 1 is exact: when `validComment` is absent, omit `commentId` and label the destination `View feedback: {post title}`. Use `View comment on feedback: {post title}` only when the validated `commentId` is emitted.
- Gap 2 is proof-only around existing checks: execute cross-scope required post, cross-scope required changelog, cross-scope optional comment, and same-scope comment attached to a different post.
- Required post/changelog failures must throw exact `NOTIFICATION_TARGET_POST_INVARIANT` / `NOTIFICATION_TARGET_CHANGELOG_INVARIANT` errors and return no target data. Optional comment failures must return the validated post-only target without `commentId` or foreign metadata.

From `src/component/notifications/inbox.ts`:
- `resolveNotificationTarget` already rejects missing/cross-scope required posts and changelog entries and computes `validComment` only for a same-scope comment whose `postId` matches the event post.
- The only implementation correction is to derive both `commentId` presence and accessible label from `validComment`; do not alter DTO versions, event mapping, error names, or persistence.

From `tests/component/notifications.test.ts`:
- Preserve the exact valid five-event matrix.
- Correct the legacy-invalid assertion, then add direct executable cases by inserting adversarial event/inbox records through `backend.run` and reading them through the real inbox query.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the exact failing fallback and adversarial relation matrix</name>
  <files>tests/component/notifications.test.ts</files>
  <read_first>.planning/phases/02.2-notification-navigation-target-contract/02.2-VERIFICATION.md, .planning/phases/02.2-notification-navigation-target-contract/02.2-CONTEXT.md, src/component/notifications/inbox.ts, tests/component/notifications.test.ts, tests/static/notification-targets.test.ts</read_first>
  <behavior>
    - Correct the legacy-invalid comment assertion to require no commentId and label `View feedback: Canonical destination`; this fails RED against the current anchored label.
    - A notification event whose required post belongs to another scope rejects with exactly `NOTIFICATION_TARGET_POST_INVARIANT`; the rejected result exposes no post title, ID, label, or target.
    - A changelog-published event whose required changelog entry belongs to another scope rejects with exactly `NOTIFICATION_TARGET_CHANGELOG_INVARIANT`; the rejected result exposes no slug, title, ID, label, or target.
    - A reply/mention event whose comment belongs to another scope returns the same-scope event post target only, with no commentId and label `View feedback: {post title}`.
    - A reply/mention event whose comment is same-scope but belongs to a different post returns the event post target only, with no commentId and label `View feedback: {event post title}`.
    - The original five valid events retain their exact anchored/non-anchored labels and destination fields.
  </behavior>
  <action>Extend only the component notification suite. Reuse its real `convexTest` installation/actor/post setup, insert second-scope and second-post referents directly through `backend.run`, then query through `api.notifications.inbox.listNotifications`. Assert required-referent failures by exact error message and assert the foreign slug/title/post/comment identifiers never appear in serialized rejection output. Assert optional-comment cases by full target equality and explicit absence of `commentId`. Run the focused component file and require RED only because the fallback label remains anchored; the existing required invariant branches may already pass. Commit only the test file with `h5i capture commit -m "test(02.2-02): cover notification relation fallbacks" --agent codex --tests --audit`.</action>
  <acceptance_criteria>The committed RED suite reproduces the wrong fallback label and executes every missing required/optional cross-scope or mismatched relation case with exact no-leak expectations.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/notifications.test.ts</automated></verify>
  <done>The canonical verifier gaps are represented by executable tests, and only the conditional fallback label remains red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Couple the accessible label to validated anchor presence and close regression gates</name>
  <files>src/component/notifications/inbox.ts, tests/component/notifications.test.ts</files>
  <read_first>tests/component/notifications.test.ts, src/component/notifications/inbox.ts, .planning/phases/02.2-notification-navigation-target-contract/02.2-VERIFICATION.md, package.json</read_first>
  <behavior>
    - The post target object chooses the anchored label if and only if it also emits `commentId`; all invalid optional relations use the post-only label.
    - Required post/changelog invariant error behavior and optional comment safe degradation pass the new executable matrix without changing any public shape or error name.
    - No DTO, validator, generated reference, merge, React, packed fixture, schema, package, completed Plan 02.2-01 artifact, or verification report changes.
    - Focused component/static target tests and the full `npm run test:phase2` gate pass with the unrelated diff fingerprint unchanged.
  </behavior>
  <action>Change only the return label in `resolveNotificationTarget` to branch on `validComment` exactly as the optional `commentId` spread does. Do not refactor or broaden the resolver. Take the focused notification/static target tests green, then run `npm run test:phase2` once as the authoritative aggregate. Require protected hash `09b5f7179dc749a197ec6a59c5ba188313b8c84536e0c35b6487ae7fcb058381`, run `git diff --check`, inspect exact staged paths, and commit only the two owned files with `h5i capture commit -m "fix(02.2-02): align notification fallback labels" --agent codex --tests --audit`.</action>
  <acceptance_criteria>Accessible wording always matches anchor presence, all adversarial relation cases are executable and green, the complete Phase 2 regression passes, and no verified surface or unrelated work changes.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/notifications.test.ts tests/static/notification-targets.test.ts && npm run test:phase2 && test "$(git diff -- .codex/config.toml .codex/hooks.json package-lock.json package.json | shasum -a 256 | awk '{print $1}')" = "09b5f7179dc749a197ec6a59c5ba188313b8c84536e0c35b6487ae7fcb058381" && git diff --check</automated></verify>
  <done>Both canonical verification gaps are closed by the smallest possible implementation/test delta and Phase 02.2 is ready for independent re-verification.</done>
</task>

</tasks>

## Prohibitions

- Do not modify `02.2-01-PLAN.md`, `02.2-01-SUMMARY.md`, `02.2-VERIFICATION.md`, or reinterpret already-verified Phase 02.2 behavior.
- Do not change target/DTO/page/delivery versions, validators, generated references, merge normalization, bindings, React hooks, fixtures, schema, package manifests, or dependencies.
- Do not change invariant error names, make required post/changelog referents optional, expose foreign identifiers in an error/target, or retain the anchored label without a validated commentId.
- Do not add static-only substitutes for the required executable runtime matrix or test implementation helpers instead of the public inbox query path.
- Do not stage `.codex/config.toml`, `.codex/hooks.json`, `package.json`, `package-lock.json`, `.codex/hooks/**`, `.npmrc`, `.planning/debug/**`, or unrelated dirty work.

## Source Coverage Audit

| Source | Gap / constraint | Plan coverage | Status |
| --- | --- | --- | --- |
| VERIFICATION | Wrong legacy-invalid fallback label | Task 1 corrects the assertion; Task 2 uses post-only wording iff commentId is absent. | COVERED |
| VERIFICATION | Missing cross-scope required post case | Task 1 executes exact post invariant/no-leak behavior. | COVERED |
| VERIFICATION | Missing cross-scope required changelog case | Task 1 executes exact changelog invariant/no-leak behavior. | COVERED |
| VERIFICATION | Missing cross-scope optional comment case | Task 1 executes exact safe post-only degradation. | COVERED |
| VERIFICATION | Missing same-scope wrong-post comment case | Task 1 executes exact safe event-post-only degradation. | COVERED |
| CONTEXT | D-03 exact accessible labels | Both tasks bind wording to actual anchor presence. | COVERED |
| CONTEXT | D-06 scope/relation validation | Task 1 proves existing required and optional semantics behaviorally. | COVERED |
| CONTEXT | D-12 executable adversarial matrix | Task 1 adds every verifier-named runtime case. | COVERED |
| REQ | NOTF-08 | Exact accessible target label and no-leak server resolution become independently proven. | COVERED |
| SCOPE | Preserve verified 02.2 behavior | Exact two-file boundary and full Phase 2 gate forbid reopening DTO, merge, headless, packed, or delivery work. | COVERED |

No additional Phase 02.2 gap is planned. Canonical verification explicitly marks the other four must-haves verified; this gap-closure plan leaves them untouched.

## Artifacts this gap closure produces

- Correct post-only accessible label for legacy-invalid, cross-scope, and wrong-post optional comment relations.
- Executable cross-scope required post/changelog invariant tests with explicit no-leak assertions.
- Executable cross-scope and same-scope-wrong-post optional comment degradation tests.
- Focused and complete Phase 2 regression evidence with the unrelated dirty fingerprint preserved.

<threat_model>

| Threat ID | Category | Severity | Mitigation |
| --- | --- | --- | --- |
| T-02.2-02-01 | Information Disclosure | critical | Cross-scope required post/changelog tests assert exact invariant rejection and absence of foreign target metadata. |
| T-02.2-02-02 | Information Disclosure | critical | Cross-scope and wrong-post comments omit commentId and foreign metadata, returning only the validated event post. |
| T-02.2-02-03 | Tampering / Accessibility | high | Label selection and commentId emission share the same `validComment` condition and exact full-object assertions. |
| T-02.2-02-04 | Regression | high | Preserve five-event matrix, focused target suite, full Phase 2 gate, diff check, and exact fingerprint. |

</threat_model>

<verification>
Commit the corrected/missing runtime expectations RED before changing production code. Then make the single conditional label correction, run focused component/static target tests, run the complete Phase 2 gate once, verify the protected dirty fingerprint and exact two-file implementation scope, and submit for independent re-verification against the unchanged canonical report.
</verification>

<success_criteria>

- Optional comment anchors use anchored wording iff a validated commentId is present; every invalid relation safely becomes a correctly labelled post-only target.
- Cross-scope required post and changelog relations fail with exact invariant errors and no foreign destination leak.
- Cross-scope and same-scope-wrong-post optional comments are executable and prove safe degradation.
- Valid targets and all already-verified Phase 02.2 behavior remain unchanged through the full regression gate.

</success_criteria>

<output>
Create `.planning/phases/02.2-notification-navigation-target-contract/02.2-02-SUMMARY.md` when done.
</output>
