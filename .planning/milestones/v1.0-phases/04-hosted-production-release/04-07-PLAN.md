---
phase: 04-hosted-production-release
plan: "07"
type: execute
wave: 7
depends_on: ["04-06"]
files_modified:
  - README.md
  - docs/.vitepress/config.ts
  - docs/index.md
  - docs/guide/local-demo.md
  - docs/guide/install.md
  - docs/guide/mount.md
  - docs/auth/convex-auth.md
  - docs/auth/clerk.md
  - docs/auth/better-auth.md
  - docs/ui/headless.md
  - docs/ui/registry.md
  - docs/ui/customization.md
  - docs/operations/testing.md
  - docs/operations/deployment.md
  - docs/operations/upgrades.md
  - scripts/test-docs.mjs
  - tests/integration/docs.test.mjs
  - package.json
autonomous: true
requirements: [COMP-01, QUAL-09]
must_haves:
  truths:
    - "The root README gives both a one-command local-demo path and a short accurate install-to-first-board path, then routes deeper work to one versioned documentation site."
    - "Convex Auth, Clerk, and Better Auth recipes derive identity and admin permission inside host wrappers on every call; no recipe accepts browser authority fields."
    - "Headless React, static registry installation, customization, testing, host-application deployment, and upgrade guides use the actual public exports and generated artifact paths."
    - "Local-demo documentation distinguishes credential-free anonymous Convex infrastructure from authenticated sandbox/admin authority and never instructs users to commit generated keys or environment files."
    - "Every executable documentation snippet is parsed, typechecked, built, or exercised against the exact packed package and current generated registry rather than trusted as prose."
  artifacts:
    - path: "README.md"
      provides: "Concise adopter entry point, compatibility contract, and release links"
    - path: "docs/.vitepress/config.ts"
      provides: "Versioned VitePress documentation site"
    - path: "docs/guide/local-demo.md"
      provides: "Clone-and-run anonymous local Convex demo prerequisites, command, security model, and recovery guide"
    - path: "docs/auth/convex-auth.md"
      provides: "Executable trusted-wrapper recipe for Convex Auth"
    - path: "docs/auth/clerk.md"
      provides: "Executable trusted-wrapper recipe for Clerk"
    - path: "docs/auth/better-auth.md"
      provides: "Executable trusted-wrapper recipe for Convex Better Auth"
    - path: "tests/integration/docs.test.mjs"
      provides: "Documentation export, snippet, link, and generated-artifact drift gate"
  key_links:
    - from: "docs/guide/install.md"
      to: "scripts/verify-package-release.mjs"
      via: "install commands are run against the exact packed tarball"
      pattern: "afferent"
    - from: "docs/auth"
      to: "fixtures"
      via: "provider snippets are extracted from or compiled as executable fixtures"
      pattern: "ctx.auth|identity"
    - from: "docs/ui/registry.md"
      to: "registry/registry.json"
      via: "documented item names and URLs are checked against generated registry metadata"
      pattern: "registry"
---

<objective>
Publish accurate, versioned adopter documentation whose local-demo, install, auth, UI, testing, host-deployment, and upgrade instructions are executable against the release candidate.

Purpose: QUAL-09 and COMP-01 require consumers to install and secure the component without guessing at provider boundaries, exports, or copy-owned UI mechanics.
Output: Root README, local-demo guide, VitePress site, three auth-provider recipes, headless/registry/operations guides, and an automated documentation gate.

Decisions: D-20, D-21, D-22, D-23, D-29, D-30.
</objective>

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hosted-production-release/04-CONTEXT.md
@.planning/phases/04-hosted-production-release/04-SCOPE-PIVOT.md
@.planning/phases/04-hosted-production-release/04-06-SUMMARY.md
@package.json
@src/client/index.ts
@src/react
@src/client
@fixtures
@registry
@ui/afferent

<interfaces>
Documentation contract:
- All normal-install examples use a fixed installation scope inside the trusted host wrapper and preserve the one-installation/one-product API.
- Provider adapters normalize only verified minimal actor facts; provider records never enter component calls.
- Admin authorization is re-derived server-side for every admin wrapper call.
- UI examples distinguish package headless imports from copy-owned static registry files.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the concise README, local-demo guide, and versioned VitePress spine</name>
  <files>README.md, docs/.vitepress/config.ts, docs/index.md, docs/guide/local-demo.md, docs/guide/install.md, docs/guide/mount.md, package.json</files>
  <read_first>
    - package.json
    - src/client/index.ts
    - src/react/index.ts
    - .planning/PROJECT.md
    - .planning/phases/04-hosted-production-release/04-CONTEXT.md
  </read_first>
  <behavior>
    - README states the product boundary, supported auth providers, package/UI distribution model, license, compatibility, one local-demo command, and docs/source links without claiming a hosted demo URL.
    - The local-demo guide names exact Node/npm/Convex/browser prerequisites, explains that no Convex login/deploy key is used, documents ephemeral JWT/JWKS behavior, and gives actionable recovery for bootstrap/readiness/port failures.
    - The guide explicitly states that signed-out showcase browsing is allowed while sandbox/admin requires a locally created Convex Auth account.
    - Install and mount guides go from package install through component mount, generated references, trusted wrapper, and first headless/copy-owned UI use.
    - Normal examples use one fixed server-owned scope and never introduce demo generation or multi-product tenancy into the public contract.
    - Documentation navigation and internal links build without dead destinations.
  </behavior>
  <action>Revalidate the exact current official VitePress release before pinning it (research baseline: 1.6.4). Create the documentation structure and theme-neutral navigation. Keep README short and lead with `npm run dev:demo` for evaluators, then route details to the local-demo and adopter guides. Derive the demo command, prerequisites, import names, and mount syntax from actual scripts, package exports, and the packed fixture; do not invent APIs. Document credential-free versus unauthenticated precisely and never suggest checking in generated auth/environment state. Use package/registry/docs release-link placeholders that Plan 04-08 can generate and Plan 04-09 must replace/verify; do not add a demo-domain placeholder.</action>
  <verify>
    <automated>npm run docs:build</automated>
  </verify>
  <done>The repository has one accurate entry path and a buildable versioned documentation site grounded in actual public exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Document executable provider, headless, registry, customization, and operations recipes</name>
  <files>docs/auth/convex-auth.md, docs/auth/clerk.md, docs/auth/better-auth.md, docs/ui/headless.md, docs/ui/registry.md, docs/ui/customization.md, docs/operations/testing.md, docs/operations/deployment.md, docs/operations/upgrades.md</files>
  <read_first>
    - fixtures/auth-convex-auth
    - fixtures/auth-clerk
    - fixtures/auth-better-auth
    - src/client/index.ts
    - src/client/server.ts
    - src/react
    - ui/afferent
    - registry/registry.json
    - scripts/generate-ui-artifacts.mjs
    - scripts/dev-demo.mjs
    - scripts/test-demo.mjs
  </read_first>
  <behavior>
    - Each provider guide authenticates in the host, rechecks admin permission per call, and passes only normalized actor facts into narrow component operations.
    - Examples explicitly reject client-supplied `userId`, `isAdmin`, and `scopeId` and explain why the component cannot access host `ctx.auth`.
    - Headless and registry guides cover generated function references, provider/hooks, copy ownership, updates, styling, and accessibility obligations.
    - Testing documents packed-consumer, local real-Convex, two-user isolation, and accessibility gates; deployment documents how adopters deploy their own host applications and never presents a hosted Afferent demo.
    - Upgrade guidance covers Changesets/changelog, package plus registry version synchronization, copied-source review, migrations, and rollback boundaries.
  </behavior>
  <action>Write provider recipes by extracting or mirroring the already executable fixtures, preserving exact provider-neutral identity rules. Document the canonical package/headless and registry workflows using current filenames/item names. Explain the one-product normal API separately from the local demo's server-only generated scopes. Include exact repo commands for packed verification, the clone-and-run demo, and Phase 4 tests. Keep deployment guidance explicitly adopter-owned: show how a consuming host app can separate preview/production Convex configuration and use its chosen frontend platform, but do not add Afferent demo infrastructure, account IDs, domains, or a Vercel-specific canonical path.</action>
  <verify>
    <automated>npm run docs:build</automated>
  </verify>
  <done>Adopters have security-correct executable guidance for every supported auth and UI path plus testing, deployment, and upgrade operations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce documentation snippets, exports, links, and generated references</name>
  <files>scripts/test-docs.mjs, tests/integration/docs.test.mjs, package.json</files>
  <read_first>
    - scripts/verify-package-release.mjs
    - scripts/prepare-demo-consumer.mjs
    - scripts/generate-ui-artifacts.mjs
    - docs
    - registry/registry.json
    - package.json
  </read_first>
  <behavior>
    - TypeScript snippets marked executable are extracted into clean fixtures and typecheck against the exact packed tarball.
    - Shell install/build/local-demo snippets run in temporary consumer fixtures or match a named tested command; adopter-owned account/deploy examples are syntax-validated but not executed.
    - Referenced package exports, registry items, paths, scripts, requirement links, and internal documentation links must exist.
    - Package, registry, docs, and repository placeholders are allowed only as explicit unresolved tokens and block the final Plan 04-09 release evidence gate; a demo domain or Convex/Vercel demo identifier is forbidden.
  </behavior>
  <action>Add metadata to code fences identifying provider/context and expected validation. Build a docs verifier that packs once, stages clean snippet fixtures, typechecks/builds safe snippets, runs or structurally validates the local-demo command without leaking owned processes, compares registry examples to generated metadata, validates internal links and command/script references, rejects forbidden authority parameters, and rejects any hosted-demo/Vercel/remote-mode claim. Add `verify:docs` and include it in `verify:phase4`. Keep adopter deploy and external package-publish commands non-mutating here and label their human prerequisites precisely.</action>
  <verify>
    <automated>npm run verify:docs &amp;&amp; npm run verify:phase4</automated>
  </verify>
  <done>Documentation drift and unsafe or non-executable recipes fail before release.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                | Description                                                                     |
| --------------------------------------- | ------------------------------------------------------------------------------- |
| Documentation -> adopter implementation | Copied auth and admin wrappers become real security boundaries.                 |
| Generated API -> prose snippets         | Export or registry drift must fail rather than silently mislead.                |
| Adopter deployment -> external accounts | Documentation may describe host-app deployment but must not fabricate credentials, IDs, or domains or imply Afferent operates the host. |

## STRIDE Threat Register

| Threat ID       | Category               | Component                    | Severity | Disposition | Mitigation Plan                                                                  |
| --------------- | ---------------------- | ---------------------------- | -------- | ----------- | -------------------------------------------------------------------------------- |
| T-04-29         | Spoofing               | provider recipes             | critical | mitigate    | Compile trusted host wrappers and statically reject browser authority inputs.    |
| T-04-30         | Elevation of Privilege | admin docs                   | critical | mitigate    | Require per-call host-owned admin reauthorization in all three provider recipes. |
| T-04-31         | Tampering              | stale exports/registry names | high     | mitigate    | Verify snippets and links against exact packed/generated artifacts.              |
| T-04-32         | Information Disclosure | provider records             | high     | mitigate    | Normalize only minimal actor facts and test forbidden provider-record flow.      |
| T-04-33         | Repudiation            | unverified instructions      | medium   | mitigate    | Mark executable fences and produce deterministic validation results.             |
| </threat_model> |

<verification>
`npm run verify:docs` must pack once, compile executable snippets in clean fixtures, validate the complete link/export/registry graph, and reject forbidden authority examples or unresolved final-release claims.
</verification>

<success_criteria>

- QUAL-09 documents the local demo, install, all three auth providers, headless and registry UI, customization, testing, adopter host deployment, and upgrades.
- COMP-01 docs install and compile against the exact package candidate.
- All examples preserve the host/component security boundary and one-product API.

</success_criteria>

<output>
Create `.planning/phases/04-hosted-production-release/04-07-SUMMARY.md` when done.
</output>
