---
phase: 02.3-admin-read-and-projection-completion
plan: "01"
type: execute
wave: 1
depends_on: ["02.2-02"]
files_modified:
  - src/client/contracts.ts
  - src/client/internal.ts
  - src/component/schema.ts
  - src/component/validators.ts
  - src/component/admin/posts.ts
  - src/component/admin/activity.ts
  - src/component/admin/changelog.ts
  - src/component/model/activity.ts
  - src/component/model/changelog.ts
  - src/component/_generated/api.ts
  - src/component/_generated/component.ts
  - src/component/_generated/dataModel.ts
  - src/component/_generated/server.ts
  - src/react/bindings.ts
  - src/react/hooks/admin.ts
  - src/react/hooks/changelog.ts
  - src/react/index.ts
  - tests/component/admin.test.ts
  - tests/component/activity.test.ts
  - tests/component/changelog.test.ts
  - tests/static/contracts.test.ts
  - tests/static/admin-reads.test.ts
  - tests/react/admin.test.tsx
  - tests/react/changelog.test.tsx
  - tests/react/live-headless.test.tsx
  - tests/integration/packed-artifact.test.mjs
  - fixtures/packed-vite-convex/convex/afferent.ts
  - fixtures/packed-vite-convex/src/App.tsx
  - fixtures/packed-vite-convex/src/App.test.tsx
  - fixtures/registry-vite/src/App.tsx
  - scripts/test-headless-backend.mjs
  - scripts/test-merge-backend.mjs
autonomous: true
requirements: [ADMN-11]
must_haves:
  truths:
    - "D-01: A closed AdminFeedbackPostDto/page projects FeedbackPostDto plus exact server-owned discussionLocked, archived, lifecycle, and merge disposition, and all moderation mutation returns carry the same current truth."
    - "D-02: Authorized admins page visible/hidden feedback through the scope-leading visibility index; D-03: direct scoped lookup retrieves archived/hidden records; D-12: every call re-authorizes in the trusted host and denial prevents component invocation."
    - "D-04: Browser bindings accept no admin/scope/truth authority; D-13: headless hooks surface server DTO/state verbatim without joins or enforcement inference."
    - "D-05: All changelog states page newest-first through by_scope_created; D-06: page v1, trusted capability/binding, and complete paginated headless states move together."
    - "D-07: AdminChangelogEntryDto v2 contains ordered canonical id/title/status links instead of postIds and performs no browser N+1 reads."
    - "D-08: Activity v2 exposes readable references and no relation IDs; D-09: append snapshots survive deletion/unlisting; D-10: legacy rows use same-scope resolution/fallback labels; D-11: row/page v2 aligns across every contract layer."
    - "D-14: RED G1-G5, scope/non-admin/spoofing/index/history matrices, focused packed/headless tests, full Phase 2 regression, and exact fingerprint pass before 03-04 resumes."
  artifacts:
    - path: src/client/contracts.ts
      provides: "Closed admin feedback/page, admin changelog page/entry, and readable activity DTO contracts"
      exports: ["AdminFeedbackPostDto", "AdminFeedbackPageDto", "AdminChangelogPageDto", "AdminChangelogEntryDto", "PostActivityDto", "PostActivityPageDto"]
    - path: src/component/admin/posts.ts
      provides: "Bounded visibility queue and direct hidden/archived admin post read"
    - path: src/component/admin/changelog.ts
      provides: "Bounded all-state administrative changelog pagination"
    - path: src/component/model/changelog.ts
      provides: "Canonical linked-feedback summary projection for admin entries"
    - path: src/component/model/activity.ts
      provides: "Snapshot-at-write and legacy-safe readable activity projection"
    - path: src/react/hooks/admin.ts
      provides: "Admin feedback queue/detail and activity headless read states"
    - path: src/react/hooks/changelog.ts
      provides: "Paginated all-state admin changelog hook"
    - path: tests/static/admin-reads.test.ts
      provides: "Trusted auth, no-spoof, indexed-query, no-internal-ID, and no-N+1 guards"
  key_links:
    - from: src/client/internal.ts
      to: src/component/admin/posts.ts
      via: "Every admin queue/detail read authorizes before invoking the component with server-resolved scope"
      pattern: "authorizeAdmin|listAdminFeedback|getAdminPost"
    - from: src/component/admin/posts.ts
      to: src/component/schema.ts
      via: "Visibility-filtered pagination uses posts.by_scope_visibility_created and never filters an unbounded scan"
      pattern: "by_scope_visibility_created|visibilityKey"
    - from: src/component/admin/changelog.ts
      to: src/component/model/changelog.ts
      via: "Each paginated entry projects ordered canonical linked summaries on the server"
      pattern: "by_scope_created|toAdminChangelogEntryDto"
    - from: src/component/model/activity.ts
      to: src/component/schema.ts
      via: "Activity append stores readable snapshots before referents can be deleted or unpublished"
      pattern: "fromBoardName|tagName|changelogTitle|changelogSlug"
    - from: src/react/hooks/admin.ts
      to: src/react/bindings.ts
      via: "Queue/detail/activity hooks accept only resource/filter/pagination intent plus local session generation"
      pattern: "listAdminFeedback|getAdminPost|listPostActivity"
    - from: src/react/hooks/changelog.ts
      to: src/react/bindings.ts
      via: "Admin changelog feed consumes the injected bounded all-state query"
      pattern: "listAdminChangelog"
---

<objective>
Close the five server-read and projection gaps blocking copied administration, red-first, while keeping all authority server-derived and all joins bounded inside the component.

Purpose: Give Phase 3 an honest admin queue/detail, changelog editor feed, linked feedback summaries, and readable activity history without public hidden-data leakage or UI N+1 queries.
Output: Versioned admin DTOs/pages, indexed authorized reads, snapshot-backed activity references, injected headless hooks, and adversarial packed/full-regression proof.
</objective>

## Phase Goal

**As an** Afferent administrator, **I want to** read complete server-authorized moderation, changelog, and activity state, **so that** the source-owned admin interface can present and restore records without guessing state or resolving internal IDs.

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-admin-read-and-projection-completion/02.3-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-06-PLAN.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-06-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-07-PLAN.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-07-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-08-PLAN.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-08-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-16-PLAN.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-16-SUMMARY.md
@.planning/phases/03-source-owned-product-interface/03-CONTEXT.md
@.planning/phases/03-source-owned-product-interface/03-UI-SPEC.md
@.planning/phases/03-source-owned-product-interface/03-04-PLAN.md
@src/client/contracts.ts
@src/client/internal.ts
@src/component/schema.ts
@src/component/admin/posts.ts
@src/component/admin/activity.ts
@src/component/admin/changelog.ts
@src/component/model/activity.ts
@src/component/model/changelog.ts
@src/react/bindings.ts
@src/react/hooks/admin.ts
@src/react/hooks/changelog.ts

<interfaces>
From `src/component/schema.ts`, `src/component/admin/posts.ts`, and `src/client/contracts.ts`:
- Posts already store `discussionLocked`, `archivedAt`, `lifecycleState`, `mergedIntoPostId`, and scope-leading visibility indexes. Add `AdminFeedbackPostDto` version 1 as `{ contractVersion: 1, feedback: FeedbackPostDto, moderation: { contractVersion: 1, discussionLocked, archived, disposition: "active" | "withdrawn" | "merged", mergedIntoPostId? } }` and `AdminFeedbackPageDto` version 1.
- Add bounded `listAdminFeedback({ visibility, paginationOpts })` over `by_scope_visibility_created` and `getAdminPost({ postId })`. Return this admin DTO from edit/move/status/lock/archive/tag mutations too, so lock/archive truth is never stale after an action.

From `src/client/internal.ts` and `src/react/bindings.ts`:
- Each new read must call `authorizeAdmin(ctx)` before `ctx.runQuery`, derive scope server-side, and expose no browser `scopeId`, actor, or `isAdmin` field.
- Bindings accept visibility/post ID/pagination plus `sessionGeneration`; non-admin tests must assert component query was not called.

From `src/component/schema.ts`, `src/component/admin/changelog.ts`, and `src/component/model/changelog.ts`:
- Add `changelogEntries.by_scope_created` over scope/createdAt/orderId and page all entry states newest-first with the existing 1..50 bound.
- Add `AdminChangelogPageDto` version 1. Change `AdminChangelogEntryDto` 1→2 and `postIds` to ordered `links: ChangelogLinkedPostDto[]`, resolving canonical posts and their current status/title on the server.

From `src/component/model/activity.ts` and activity producers:
- Change activity presentation fields to `fromBoard`/`toBoard` `{ contractVersion: 1, name, slug }`, `tag` `{ contractVersion: 1, name }`, and `changelog` `{ contractVersion: 1, title, slug }`.
- Add optional snapshot columns to `postActivity` and populate them in the common append path through same-scope referent reads. For legacy missing snapshots, resolve live same-scope records; if absent, return `Deleted board`, `Deleted tag`, or `Unavailable changelog entry` without an ID.
- Bump activity row/page contracts 1→2 and retain merge-composite pagination.

From `src/react/hooks/admin.ts`, `src/react/hooks/changelog.ts`, and Phase 3 Plan `03-04`:
- Add injected paginated hooks for admin feedback and changelog plus a direct post hook. Preserve explicit unsupported/auth/loading/not-authorized/empty/ready/loading-more/error states and generation fencing.
- The copied admin workspace consumes these DTOs directly; it may not reuse public feeds for hidden records or join relation IDs in React.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock all five admin read/projection gaps with failing tests</name>
  <files>tests/component/admin.test.ts, tests/component/activity.test.ts, tests/component/changelog.test.ts, tests/static/contracts.test.ts, tests/static/admin-reads.test.ts, tests/react/admin.test.tsx, tests/react/changelog.test.tsx, tests/react/live-headless.test.tsx, tests/integration/packed-artifact.test.mjs, fixtures/packed-vite-convex/src/App.test.tsx</files>
  <read_first>.planning/phases/02.3-admin-read-and-projection-completion/02.3-CONTEXT.md, src/client/contracts.ts, src/client/internal.ts, src/component/schema.ts, src/component/admin/posts.ts, src/component/admin/activity.ts, src/component/admin/changelog.ts, src/component/model/activity.ts, src/component/model/changelog.ts, src/react/bindings.ts, src/react/hooks/admin.ts, src/react/hooks/changelog.ts</read_first>
  <behavior>
    - G1 tests require exact lock/archive/disposition truth in admin reads and every moderation mutation return.
    - G2 tests page visible/hidden records and directly retrieve archived records, reject non-admin before component invocation, reject spoofed auth/scope fields, and isolate two scopes.
    - G3 tests page draft/published/unpublished entries in deterministic newest-first order through the binding/headless state matrix and exact scope-leading index.
    - G4 tests require ordered canonical id/title/status links and reject bare postIds or browser joins.
    - G5 tests require readable board/tag/changelog references after tag deletion and changelog unpublication, legacy fallbacks, exact activity/page v2, and absence of relation IDs.
  </behavior>
  <action>Add focused RED behavioral, static, React, and packed expectations for G1-G5 before editing production code. Include 51-row bounds/cursors, tie ordering, two scopes, non-admin short-circuit, malformed browser fields, merge-canonical changelog links, deleted tag, unpublished changelog, and legacy activity rows. Confirm focused failures are solely the missing read/projection contracts, then commit exact test paths using `h5i capture commit -m "test(02.3-01): specify admin read projections" --agent codex --tests --audit`.</action>
  <acceptance_criteria>A committed RED matrix covers every locked gap, trust boundary, pagination/index rule, historical referent, and version transition.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/admin.test.ts tests/component/activity.test.ts tests/component/changelog.test.ts tests/static/contracts.test.ts tests/static/admin-reads.test.ts && npm exec -- vitest run --config vitest.react.config.ts tests/react/admin.test.tsx tests/react/changelog.test.tsx tests/react/live-headless.test.tsx</automated></verify>
  <done>G1-G5 are specified as exact failing behavior with no remaining contract ambiguity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement indexed authorized reads and presentation-ready server projections</name>
  <files>src/client/contracts.ts, src/client/internal.ts, src/component/schema.ts, src/component/validators.ts, src/component/admin/posts.ts, src/component/admin/activity.ts, src/component/admin/changelog.ts, src/component/model/activity.ts, src/component/model/changelog.ts, src/component/_generated/api.ts, src/component/_generated/component.ts, src/component/_generated/dataModel.ts, src/component/_generated/server.ts, tests/component/admin.test.ts, tests/component/activity.test.ts, tests/component/changelog.test.ts, tests/static/contracts.test.ts, tests/static/admin-reads.test.ts, scripts/test-merge-backend.mjs</files>
  <read_first>tests/component/admin.test.ts, tests/component/activity.test.ts, tests/component/changelog.test.ts, tests/static/admin-reads.test.ts, src/client/contracts.ts, src/client/internal.ts, src/component/schema.ts, src/component/validators.ts, src/component/admin/posts.ts, src/component/admin/activity.ts, src/component/admin/changelog.ts, src/component/model/activity.ts, src/component/model/changelog.ts</read_first>
  <behavior>
    - Admin feedback list/detail and mutation returns use the exact nested v1 DTO; visibility pages are index-backed, bounded, deterministic, scope-complete, and preserve hidden lifecycle distinctions.
    - Trusted client methods authorize first on every read and never call the component after denial; component queries receive only server scope and resource/filter/pagination intent.
    - Admin changelog pages use by_scope_created and return all three states; entry v2 links are ordered canonical summaries with no bare ID array or per-link browser read.
    - Activity append snapshots same-scope labels/slugs, projection prefers snapshots, uses safe legacy live resolution/fallback labels, and emits only row/page v2 readable references.
    - Types, validators, all constructors, generated references, and test fixtures agree on every new/bumped version.
  </behavior>
  <action>Implement the exact DTOs and validators, admin post mapper, indexed list/direct queries, and mutation return changes. Put host authorization before each new component invocation. Add the changelog index/query/page and map canonical linked posts with `toChangelogLinkedPostDto`. Extend the activity schema/common append path with readable snapshots and projection fallback while retaining internal relation fields only in storage. Bump the locked contracts, update all exact constructors, run `npm run codegen:component`, inspect generated diffs, and take focused component/static/backend tests green. Commit owned implementation/tests with `h5i capture commit -m "feat(02.3-01): add authorized admin read projections" --agent codex --tests --audit`.</action>
  <acceptance_criteria>Every admin record needed by copied UI is available through a bounded authorized server read and presentation-ready closed DTO with aligned versions and no relation leak.</acceptance_criteria>
  <verify><automated>npm run codegen:component && npm exec -- vitest run tests/component/admin.test.ts tests/component/activity.test.ts tests/component/changelog.test.ts tests/static/contracts.test.ts tests/static/admin-reads.test.ts && npm run test:merge-backend && npm run typecheck && npm run lint</automated></verify>
  <done>G1-G5 backend/types/validators/generated references are green with exact authorization, indexing, historical readability, and canonical joins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose complete headless admin reads and prove installed consumption</name>
  <files>src/react/bindings.ts, src/react/hooks/admin.ts, src/react/hooks/changelog.ts, src/react/index.ts, tests/react/admin.test.tsx, tests/react/changelog.test.tsx, tests/react/live-headless.test.tsx, fixtures/packed-vite-convex/convex/afferent.ts, fixtures/packed-vite-convex/src/App.tsx, fixtures/packed-vite-convex/src/App.test.tsx, fixtures/registry-vite/src/App.tsx, tests/integration/packed-artifact.test.mjs, scripts/test-headless-backend.mjs, scripts/test-merge-backend.mjs</files>
  <read_first>src/react/bindings.ts, src/react/hooks/admin.ts, src/react/hooks/changelog.ts, src/react/index.ts, tests/react/admin.test.tsx, tests/react/changelog.test.tsx, tests/react/live-headless.test.tsx, fixtures/packed-vite-convex/convex/afferent.ts, fixtures/packed-vite-convex/src/App.tsx, .planning/phases/03-source-owned-product-interface/03-04-PLAN.md, package.json</read_first>
  <behavior>
    - Injected admin feedback/changelog reads and direct post lookup expose complete explicit state unions and reset all results on auth/session generation changes.
    - Hooks carry moderation, link, and activity projection truth verbatim; they perform no public-feed fallback, local lock/archive inference, relation lookup, or admin enforcement.
    - Packed wrapper authorizes and derives scope on the server, browser arguments contain no authority, and installed UI exercises hidden restore reachability plus all-state changelog/activity summaries.
    - Focused React/headless/packed/merge gates and complete Phase 2 regression pass with package experiments and other dirty work untouched.
  </behavior>
  <action>Add optional binding references and exported hooks using the established direct/paginated query adapters, with capability/auth handling and generation fencing consistent with existing admin hooks. Update the packed trusted wrapper and consumer to demonstrate admin-visible hidden feedback, current moderation flags, paginated changelog states/links, and readable activity. Do not add UI joins or treat capability display as enforcement. Run focused gates, build/type/lint, and `npm run test:phase2`; require unrelated hash `09b5f7179dc749a197ec6a59c5ba188313b8c84536e0c35b6487ae7fcb058381`, then inspect `git diff --check` and staged paths. Commit with `h5i capture commit -m "feat(02.3-01): expose complete admin read state" --agent codex --tests --audit`.</action>
  <acceptance_criteria>Headless and packed consumers can drive the entire planned admin workspace from server truth, every security/regression gate passes, and 03-04 has an executable dependency.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run --config vitest.react.config.ts tests/react/admin.test.tsx tests/react/changelog.test.tsx tests/react/live-headless.test.tsx && npm run test:headless-backend && npm run test:merge-backend && node --test tests/integration/packed-artifact.test.mjs && npm run build && npm run typecheck && npm run lint && npm run test:phase2 && test "$(git diff -- .codex/config.toml .codex/hooks.json package-lock.json package.json | shasum -a 256 | awk '{print $1}')" = "09b5f7179dc749a197ec6a59c5ba188313b8c84536e0c35b6487ae7fcb058381" && git diff --check</automated></verify>
  <done>The installed/headless admin path is complete and independently verifiable before Phase 3 Plan 03-04 resumes.</done>
</task>

</tasks>

## Prohibitions

- Do not accept browser `isAdmin`, actor/user identity, scope, lock/archive/lifecycle truth, or visibility authority; do not access host `ctx.auth` from the component.
- Do not use public feedback reads to retrieve hidden records, filter an unbounded table in memory, or allow non-admin calls to invoke component admin queries.
- Do not expose bare activity relation IDs or changelog post ID arrays, and do not join referents in React/host wrappers or issue N+1 browser queries.
- Do not hard-delete activity history, lose labels when tags are deleted/changelog entries unpublished, or silently widen versioned DTOs/pages.
- Do not add generic CRUD, bulk administration, workspace audit/export, configurable statuses, copied UI, new dependencies, or work for Plans 03-05/03-06.
- Do not stage `.codex/config.toml`, `.codex/hooks.json`, `package.json`, `package-lock.json`, `.codex/hooks/**`, `.npmrc`, `.planning/debug/**`, or unrelated dirty work.

## Source Coverage Audit

| Source | ID / constraint | Plan coverage | Status |
| --- | --- | --- | --- |
| GOAL | Host security boundary and presentation-ready narrow DTOs | Every admin read authorizes in the host, uses closed component DTOs, and exposes no browser authority. | COVERED |
| REQ | ADMN-11 | Tasks 1-3 cover all authorized moderation/changelog/activity reads and projections. | COVERED |
| AUDIT | G1 moderation truth | Exact admin DTO plus mutation returns expose lock/archive/disposition. | COVERED |
| AUDIT | G2 hidden/archived reachability | Indexed visibility queue and direct scoped read with pre-invocation denial are mandatory. | COVERED |
| AUDIT | G3 all-state changelog | Scope-leading pagination, page DTO, binding, and complete headless states are mandatory. | COVERED |
| AUDIT | G4 linked summaries | Admin entry v2 contains ordered canonical id/title/status links joined server-side. | COVERED |
| AUDIT | G5 readable activity | Snapshot-at-write, legacy fallback, readable references, no relation IDs, and activity/page v2 are mandatory. | COVERED |
| DOWNSTREAM | Phase 03 Plan 03-04 | 03-04 depends on both its normal predecessor and 02.3-01; every queue/editor/activity input is supplied. | COVERED |
| DOWNSTREAM | Plans 03-05 and 03-06 | Fresh audit found no additional backend/headless prerequisite gap. | NO CHANGE |
| DEFERRED | Generic CRUD/bulk/audit/export/configurable statuses/copied UI | Explicitly prohibited. | EXCLUDED |

No source decision is missing. One plan covers all five inseparable admin workspace contract gaps without changing the remaining Phase 3 scope.

## Artifacts this phase produces

- `AdminFeedbackPostDto` v1, moderation subprojection v1, and `AdminFeedbackPageDto` v1.
- Scope-indexed visibility queue, direct hidden/archived lookup, and admin DTO moderation mutation returns.
- `changelogEntries.by_scope_created`, `AdminChangelogPageDto` v1, and all-state paginated query/hook.
- `AdminChangelogEntryDto` v2 with ordered canonical `ChangelogLinkedPostDto` summaries instead of `postIds`.
- Activity snapshot columns plus readable board/tag/changelog reference DTOs; `PostActivityDto` v2 and page v2 with no relation IDs.
- Trusted bindings/headless state, packed consumption, adversarial authority/index/no-N+1 tests, and complete Phase 2 regression evidence.

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Browser intent -> trusted admin host wrapper | Resource/filter/pagination inputs are untrusted; admin permission and scope are derived on every call. |
| Trusted wrapper -> isolated component | Only authorized scoped operations cross; the component never reads host auth. |
| Hidden/admin records -> DTO | Non-public lifecycle and moderation truth is available only through the admin surface. |
| Internal relations -> presentation projection | Changelog and activity relations are joined/snapshotted server-side and IDs are not leaked as presentation work. |
| Server DTO -> headless/UI | Hooks surface truth and state but never infer authority or perform referent joins. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-02.3-01 | Spoofing / Elevation | admin bindings | critical | mitigate | Server-derived authorizeAdmin on every call; no isAdmin/scope inputs; denial-before-invocation tests. |
| T-02.3-02 | Information Disclosure | hidden feedback reads | critical | mitigate | Admin-only wrappers, scope-leading indexes, two-scope/non-admin matrix, no public-feed reuse. |
| T-02.3-03 | Information Disclosure | relation DTOs | high | mitigate | Closed linked/activity summaries and static absence of internal relation IDs. |
| T-02.3-04 | Denial of Service | feedback/changelog lists | high | mitigate | Scope-leading indexes, 1..50 pagination, deterministic cursors, no scans/in-memory filtering. |
| T-02.3-05 | Tampering | moderation truth | high | mitigate | One server mapper for reads and mutation returns; exact lock/archive/disposition tests. |
| T-02.3-06 | Tampering / Repudiation | deleted/unlisted activity referents | high | mitigate | Same-scope snapshot-at-write plus stable legacy fallback labels. |
| T-02.3-07 | Tampering | canonical changelog links | high | mitigate | Existing bounded canonical resolver and ordered summary tests; no browser join. |
| T-02.3-08 | Tampering | DTO version drift | high | mitigate | Exact type/validator/page/generated/packed version tests. |
| T-02.3-SC | Tampering | dependencies | low | accept | No package change authorized; clean packed and full regression gates remain required. |

</threat_model>

<verification>
Preserve one committed RED matrix for G1-G5, then take the indexed component reads, snapshots, closed DTOs/validators, and generated references green before adding bindings/hooks. Finish with focused React/headless/merge/packed gates, build/type/lint, complete `npm run test:phase2`, exact unrelated-diff fingerprint, `git diff --check`, and staged-path inspection. Execution commits use `h5i capture commit --agent codex --tests --audit`; independent verification gates Plan 03-04.
</verification>

<success_criteria>

- Authorized admins can page visible/hidden feedback and directly retrieve archived records with exact lock/archive/disposition truth; non-admins cannot invoke the component reads.
- Admins can page draft/published/unpublished changelog entries and receive ordered linked feedback id/title/status summaries without N+1 reads.
- Activity rows remain readable after deletion/unlisting and expose labels/slugs rather than relation IDs.
- Every new/bumped DTO/page is aligned across types, validators, generated refs, bindings, hooks, fixtures, and packed installation.
- All authority, scope, index, history, packed, and complete Phase 2 gates pass with unrelated dirty work untouched.

</success_criteria>

<output>
Create `.planning/phases/02.3-admin-read-and-projection-completion/02.3-01-SUMMARY.md` when done.
</output>
