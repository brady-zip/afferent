---
phase: 02.3-admin-read-and-projection-completion
plan: "02"
type: execute
wave: 2
depends_on: ["02.3-01"]
files_modified:
  - tests/component/moderation.test.ts
  - tests/component/changelog.test.ts
  - tests/component/activity.test.ts
  - tests/react/live-headless.test.tsx
  - fixtures/packed-vite-convex/src/App.tsx
  - fixtures/packed-vite-convex/src/App.test.tsx
  - tests/integration/packed-artifact.test.mjs
autonomous: true
gap_closure: true
requirements: [ADMN-11]
must_haves:
  truths:
    - "Executable component tests page exactly 51 visible and 51 hidden admin feedback rows across deterministic ties/cursors, exclude a second scope, and reject a direct cross-scope post read without exposing the foreign record."
    - "Executable component tests page one mixed draft/published/unpublished changelog feed newest-first across a deterministic tie and continuation cursor while excluding another scope."
    - "Executable activity tests prove board, tag, and changelog snapshots survive referent deletion/unpublication; legacy rows resolve live same-scope referents or exact safe missing/cross-scope fallbacks; every output omits relation ID fields."
    - "Mounted useAdminFeedback, useAdminPost, and useAdminChangelog tests prove unauthenticated suppression, identity-generation replacement, immediate old-value clearing, and rejection of stale prior-generation publications."
    - "The clean packed consumer reads a hidden archived post, consumes its exact restore/moderation truth, and asserts actual linked-feedback and readable activity values rather than checking hook names or status strings alone."
    - "The closure is test/fixture-only unless a new executable expectation exposes a concrete production defect; focused suites, packed artifact, full Phase 2 regression, diff checks, and the protected fingerprint must pass."
  artifacts:
    - path: tests/component/moderation.test.ts
      provides: "Visible/hidden 51-row tie/cursor/two-scope queue and direct cross-scope read matrix"
    - path: tests/component/changelog.test.ts
      provides: "All-state newest-first tie/cursor/scope admin changelog matrix"
    - path: tests/component/activity.test.ts
      provides: "Snapshot survival, legacy live/fallback, cross-scope, and no-ID activity matrix"
    - path: tests/react/live-headless.test.tsx
      provides: "Mounted auth, generation replacement, and stale-publication proof for all three new hooks"
    - path: fixtures/packed-vite-convex/src/App.tsx
      provides: "Installed consumer rendering hidden restore and server-projected changelog/activity values"
    - path: fixtures/packed-vite-convex/src/App.test.tsx
      provides: "Real packed host-wrapper value assertions and transcript evidence"
  key_links:
    - from: tests/component/moderation.test.ts
      to: src/component/admin/posts.ts
      via: "Real admin capabilities exercise indexed list cursors and scoped direct lookup"
      pattern: "listAdminFeedback|getAdminPost"
    - from: tests/component/changelog.test.ts
      to: src/component/admin/changelog.ts
      via: "Real all-state admin query exercises created-order cursor semantics"
      pattern: "listAdminChangelog"
    - from: tests/component/activity.test.ts
      to: src/component/model/activity.ts
      via: "Real activity reads distinguish write snapshots, live legacy resolution, and safe fallback"
      pattern: "fromBoard|toBoard|tag|changelog"
    - from: tests/react/live-headless.test.tsx
      to: src/react/hooks/admin.ts
      via: "Mounted providers drive auth and generation changes through actual admin feedback/detail hooks"
      pattern: "useAdminFeedback|useAdminPost"
    - from: tests/react/live-headless.test.tsx
      to: src/react/hooks/changelog.ts
      via: "Mounted provider drives generation-fenced all-state hook publications"
      pattern: "useAdminChangelog"
    - from: fixtures/packed-vite-convex/src/App.test.tsx
      to: fixtures/packed-vite-convex/convex/afferent.ts
      via: "Installed packed wrapper returns actual hidden moderation, linked changelog, and activity projection values"
      pattern: "listAdminFeedback|getAdminPost|listAdminChangelog|listPostActivity"
---

<objective>
Close only the behavioral-evidence gaps in canonical Phase 02.3 verification by executing the already-present queue, changelog, activity, mounted-hook, and packed projection contracts.

Purpose: Replace source/symbol confidence with deterministic runtime proof while preserving the verified authorization, DTO, index, and projection implementation.
Output: Backend boundary matrices, mounted generation-reset tests, and actual packed projection-value consumption; production changes occur only if those tests reveal a concrete defect.
</objective>

## Phase Goal

**As an** Afferent integrator, **I want to** see the complete admin read contract exercised at pagination, history, identity-reset, and installed-consumer boundaries, **so that** Phase 3 administration rests on behavior rather than inferred wiring.

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-admin-read-and-projection-completion/02.3-CONTEXT.md
@.planning/phases/02.3-admin-read-and-projection-completion/02.3-VERIFICATION.md
@.planning/phases/02.3-admin-read-and-projection-completion/02.3-01-PLAN.md
@.planning/phases/02.3-admin-read-and-projection-completion/02.3-01-SUMMARY.md
@tests/component/moderation.test.ts
@tests/component/changelog.test.ts
@tests/component/activity.test.ts
@tests/react/live-headless.test.tsx
@fixtures/packed-vite-convex/src/App.tsx
@fixtures/packed-vite-convex/src/App.test.tsx
@tests/integration/packed-artifact.test.mjs

<interfaces>
From canonical `02.3-VERIFICATION.md`:
- No production defect is currently established. Existing authorization order, indexes/bounds, DTOs, canonical links, activity snapshot/live/fallback branches, hook wiring, and packed wrapper compile are substantive and remain unchanged unless stronger tests disconfirm them.
- Gap 1 requires executable visible and hidden 51-row pagination, deterministic tie/cursor behavior, two-scope list isolation, and direct cross-scope rejection.
- Gap 2 requires draft/published/unpublished entries together in the admin feed with newest-first, deterministic tie, continuation cursor, and scope isolation.
- Gap 3 requires board/tag/changelog snapshot survival, legacy same-scope live resolution, exact absent/cross-scope fallbacks, and no relation IDs.
- Gap 4 requires actual mounted new-hook auth/generation/stale-publication behavior plus packed hidden-restore and projection-value consumption.

From current component contracts:
- `listAdminFeedback` and `listAdminChangelog` accept 1..50 pagination options, order descending through scope-leading indexes, and return Convex continuation cursors. Seed controlled `createdAt`/`orderId` values directly where needed so ties are deterministic and setup is not rate-limit dependent.
- `getAdminPost` must surface a same-scope hidden post and reject a foreign-scope ID as `NOT_FOUND` without serializing foreign fields.
- Activity projection prefers stored snapshots, otherwise reads a same-scope live referent, otherwise emits exact fallbacks: `Deleted board`/`deleted-board`, `Deleted tag`, and `Unavailable changelog entry`/`unavailable-changelog-entry`.

From current headless and packed contracts:
- `useAdminFeedback`, `useAdminPost`, and `useAdminChangelog` all use shared direct/paginated watch adapters with `sessionGeneration`. The mounted test must publish through recorded actual query callbacks before and after identity changes; mapper-only assertions do not count.
- The packed fixture already injects the trusted admin functions. Change its consumer from visible/status-only rendering to hidden restore and projection-field consumption, then assert those values through the installed tarball's real wrapper operations and captured transcript.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute bounded feedback and all-state changelog pagination boundaries</name>
  <files>tests/component/moderation.test.ts, tests/component/changelog.test.ts</files>
  <read_first>.planning/phases/02.3-admin-read-and-projection-completion/02.3-VERIFICATION.md, src/component/admin/posts.ts, src/component/admin/changelog.ts, src/component/schema.ts, tests/component/moderation.test.ts, tests/component/changelog.test.ts</read_first>
  <behavior>
    - Seed 51 visible and 51 hidden alpha-scope feedback rows plus beta-scope distractors with controlled createdAt/orderId ties; page each alpha visibility as 50 then 1, assert exact descending composite order, first/second cursor behavior, no duplicates/gaps, and zero beta IDs.
    - Alpha direct admin read of one beta hidden post rejects with NOT_FOUND and serialized error data contains no beta title/body/board/moderation truth.
    - Seed 51 alpha changelog rows spanning draft, published, and unpublished plus beta distractors; force at least one createdAt tie resolved by orderId.
    - Page alpha changelog as 50 then 1, assert exact descending composite order, exact three-state presence, cursor/isDone behavior, no duplicates/gaps, and zero beta IDs/slugs/titles.
    - Use real `client.admin` capabilities for reads; direct database inserts are setup only and must satisfy the actual schema/state invariants.
  </behavior>
  <action>Add the missing executable matrices to the existing owning component suites. Build explicit expected sequences from the controlled index keys rather than asserting only lengths or sets. Exercise the returned continuation cursor for page two and assert complete equality of the concatenated IDs. For the direct foreign read, assert the closed NOT_FOUND shape and explicit absence of foreign content. Run both files with static admin-read guards. These are coverage-first tests and are expected to pass against the verified source; if a behavior fails because production violates the locked contract, stop, record the exact counterexample, and amend this gap plan before editing production. Commit only the two test files with `h5i capture commit -m "test(02.3-02): prove bounded admin pagination" --agent codex --tests --audit`.</action>
  <acceptance_criteria>Visible/hidden feedback and all-state changelog behavior are proven at the 50-row boundary, deterministic ties/cursors and two scopes are exact, and direct foreign records cannot be read or inferred.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/moderation.test.ts tests/component/changelog.test.ts tests/static/admin-reads.test.ts</automated></verify>
  <done>The first two canonical coverage gaps are closed without production changes, or execution stops with a concrete defect requiring an explicit plan amendment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute activity snapshot, legacy resolution, fallback, and no-ID behavior</name>
  <files>tests/component/activity.test.ts</files>
  <read_first>.planning/phases/02.3-admin-read-and-projection-completion/02.3-VERIFICATION.md, src/component/model/activity.ts, src/component/schema.ts, src/component/admin/activity.ts, src/component/admin/tags.ts, src/component/admin/changelog.ts, tests/component/activity.test.ts</read_first>
  <behavior>
    - Produce real board-move, tag-change, and changelog publication activity; remove/mutate/unpublish their referents after append and prove the original snapshot name/title/slug remains exact.
    - Insert legacy activity rows without snapshots whose board, tag, and changelog referents still exist in the same scope; prove current live name/title/slug resolution.
    - Insert legacy rows whose referents are deleted/missing and rows pointing to another scope; prove the exact non-identifying board/tag/changelog fallback labels/slugs.
    - Serialize every tested v2 activity row and prove it contains none of `fromBoardId`, `toBoardId`, `tagId`, or `changelogEntryId`, including live, snapshot, missing, and cross-scope cases.
    - Read all cases through the real authorized `listPostActivity` path, preserving bounded merged pagination behavior.
  </behavior>
  <action>Expand the focused activity suite with one controlled matrix. Use real admin mutations to create snapshot-bearing activity, then database setup only to remove/mutate referents and insert legacy no-snapshot rows that public operations cannot create. Keep every activity/post/event scope explicit. Assert full nested presentation objects and property absence per row, not substring/source checks. Run the component activity and static contract suites. The existing implementation is expected to pass; on a genuine product failure, stop and amend the plan before any production edit. Commit only the activity test with `h5i capture commit -m "test(02.3-02): prove readable activity history" --agent codex --tests --audit`.</action>
  <acceptance_criteria>All snapshot, live legacy, missing, and cross-scope history branches are executable with exact readable values and zero relation-ID output.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/activity.test.ts tests/static/contracts.test.ts</automated></verify>
  <done>The canonical activity evidence gap is closed without production changes, or execution stops with a reproducible defect and explicit plan amendment requirement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mount generation-fenced hooks and consume actual packed admin values</name>
  <files>tests/react/live-headless.test.tsx, fixtures/packed-vite-convex/src/App.tsx, fixtures/packed-vite-convex/src/App.test.tsx, tests/integration/packed-artifact.test.mjs</files>
  <read_first>.planning/phases/02.3-admin-read-and-projection-completion/02.3-VERIFICATION.md, src/react/hooks/admin.ts, src/react/hooks/changelog.ts, src/react/query.ts, tests/react/live-headless.test.tsx, fixtures/packed-vite-convex/src/App.tsx, fixtures/packed-vite-convex/src/App.test.tsx, fixtures/packed-vite-convex/convex/afferent.ts, tests/integration/packed-artifact.test.mjs, package.json</read_first>
  <behavior>
    - Mount all three new hooks under the real provider: unauthenticated state starts no admin query and exposes no prior values; authenticated identity A publishes exact queue/detail/changelog DTOs.
    - Switching to identity B immediately clears A's queue/detail/changelog values, creates new query records with a new sessionGeneration, and ignores callbacks/publications retained from A; only current-generation B publications become ready.
    - Switching back to unauthenticated clears B values and returns exact not-authorized states with no active authoritative result.
    - Packed HeadlessWorkflow requests the hidden queue, consumes direct moderation `discussionLocked`/`archived`/`disposition` as restore eligibility, consumes changelog linked-feedback id/title/status, and consumes readable activity board/tag/changelog labels/slugs.
    - The packed fixture test creates and reads actual hidden archived feedback, linked all-state changelog, and readable activity through installed trusted wrappers; the outer packed-artifact test asserts these concrete transcript values, not source symbol occurrence.
    - Focused React and packed tests, the complete Phase 2 gate, exact dirty fingerprint, and diff checks pass with no production or completed planning/report changes.
  </behavior>
  <action>Replace the export-existence-only coverage with a mounted lifecycle test using the existing live watch harness and retained query callbacks. Assert values before and after auth/generation boundaries for each hook. In the packed fixture, add `usePostActivity`, switch admin feedback to hidden, and render/derive explicit restore/moderation, linked-feedback, and readable activity values. Extend the fixture's real backend test and captured transcript so `packed-artifact.test.mjs` checks those exact values from the installed tarball path. Do not change trusted wrapper authority or result validators. Run the focused React and packed tests, then `npm run test:phase2` once. Require protected hash `09b5f7179dc749a197ec6a59c5ba188313b8c84536e0c35b6487ae7fcb058381`, verify completed `02.3-01` and canonical report remain unchanged, inspect `git diff --check` and exact staged paths, and commit with `h5i capture commit -m "test(02.3-02): prove installed admin read behavior" --agent codex --tests --audit`.</action>
  <acceptance_criteria>All new hooks are behaviorally generation-fenced when mounted, and a clean packed consumer demonstrably uses real hidden restore, changelog-link, and readable activity values through the trusted server path.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run --config vitest.react.config.ts tests/react/live-headless.test.tsx && node --test tests/integration/packed-artifact.test.mjs && npm run test:phase2 && test "$(git diff -- .codex/config.toml .codex/hooks.json package-lock.json package.json | shasum -a 256 | awk '{print $1}')" = "09b5f7179dc749a197ec6a59c5ba188313b8c84536e0c35b6487ae7fcb058381" && git diff --exit-code -- .planning/phases/02.3-admin-read-and-projection-completion/02.3-01-PLAN.md .planning/phases/02.3-admin-read-and-projection-completion/02.3-01-SUMMARY.md .planning/phases/02.3-admin-read-and-projection-completion/02.3-VERIFICATION.md && git diff --check</automated></verify>
  <done>Every canonical behavioral-evidence gap is closed through focused, mounted, installed, and full-regression proof, ready for independent Phase 02.3 re-verification.</done>
</task>

</tasks>

## Prohibitions

- Do not alter `02.3-01-PLAN.md`, `02.3-01-SUMMARY.md`, `02.3-VERIFICATION.md`, or reinterpret the three must-haves already verified.
- Do not modify production code merely to make setup easier or broaden the admin API, DTOs, indexes, snapshots, bindings, hooks, authorization, or wrapper surface.
- If a stronger test exposes a genuine production defect, do not hide it with a weaker assertion or ad hoc fixture branch; stop and amend the plan with the exact causal production file before fixing it.
- Do not substitute counts, source regexes, mapper calls, export existence, or status strings for exact rows, cursors, scopes, mounted lifecycle publications, or packed projection values.
- Do not leak foreign IDs/content in direct-read errors or activity DTOs; do not use public feedback reads as an admin hidden-data path.
- Do not stage `.codex/config.toml`, `.codex/hooks.json`, `.codex/skills/radio/SKILL.md`, `package.json`, `package-lock.json`, `.codex/hooks/**`, `.npmrc`, `.planning/debug/**`, or unrelated dirty work.

## Source Coverage Audit

| Source | Gap / constraint | Plan coverage | Status |
| --- | --- | --- | --- |
| VERIFICATION | Visible+hidden 51-row/tie/cursor/two-scope queue | Task 1 executes exact ordered two-page matrices for both visibility values. | COVERED |
| VERIFICATION | Direct cross-scope admin post read | Task 1 requires closed NOT_FOUND and explicit no-foreign-content proof. | COVERED |
| VERIFICATION | Three-state newest-first/tie/cursor/scope changelog | Task 1 executes one exact mixed-state two-page sequence with beta distractors. | COVERED |
| VERIFICATION | Board/tag/changelog snapshot survival | Task 2 mutates/deletes/unpublishes referents after real activity append and asserts original snapshots. | COVERED |
| VERIFICATION | Legacy live resolution and safe fallback | Task 2 inserts same-scope live, missing, and cross-scope no-snapshot rows with exact outputs. | COVERED |
| VERIFICATION | Activity relation-ID absence | Task 2 asserts absence on snapshot, live, missing, and cross-scope rows. | COVERED |
| VERIFICATION | Mounted auth/generation/stale publication | Task 3 mounts all three hooks and exercises identity A, identity B, retained A callbacks, and unauthenticated clearing. | COVERED |
| VERIFICATION | Packed hidden restore/projection values | Task 3 consumes and transcripts actual moderation, link, and activity DTO values through installed wrappers. | COVERED |
| CONTEXT | D-02/D-03/D-05/D-06/D-08..D-14 | Tasks 1-3 provide the exact missing behavioral proof while preserving verified implementation decisions. | COVERED |
| REQ | ADMN-11 | Bounded authorized reads and no-ID/N+1 projections receive executable backend, hook, and packed proof. | COVERED |
| SCOPE | Test-only unless defect proven | Planned files are tests/fixtures only; a production defect requires explicit plan amendment. | COVERED |

No additional Phase 02.3 work is planned. Canonical verification identifies behavioral proof as the blocker and records no confirmed production defect.

## Artifacts this gap closure produces

- Exact 51-row visible/hidden admin feedback pagination and two-scope/direct-read isolation evidence.
- Exact all-state changelog chronology, tie, cursor, and scope evidence.
- Complete snapshot/live/fallback/no-ID activity evidence.
- Mounted three-hook auth/generation/stale-publication evidence.
- Clean packed hidden restore plus real changelog/activity projection-value evidence.
- Focused and complete Phase 2 regression proof with protected dirty work unchanged.

<threat_model>

| Threat ID | Category | Severity | Mitigation |
| --- | --- | --- | --- |
| T-02.3-02-01 | Information Disclosure | critical | Two-scope list/direct tests require zero foreign rows/content and closed NOT_FOUND behavior. |
| T-02.3-02-02 | Denial of Service / Integrity | high | Exact 50+1 cursor/tie matrices prove bounded complete traversal with no duplicates/gaps. |
| T-02.3-02-03 | Information Disclosure | critical | Missing/cross-scope activity referents emit only stable non-identifying fallbacks and no relation IDs. |
| T-02.3-02-04 | Tampering | high | Snapshot mutation/deletion/unpublication tests prove historical labels cannot drift with current referents. |
| T-02.3-02-05 | Information Disclosure | critical | Mounted identity change clears prior admin values and stale callbacks cannot republish them. |
| T-02.3-02-06 | Verification Integrity | high | Packed assertions require concrete installed DTO values rather than symbol/status proxies. |

</threat_model>

<verification>
Add exact executable assertions before considering any implementation change. Take component pagination and activity suites green, then mounted hook and real packed value tests green. If any new test establishes a production defect, stop and amend the plan rather than weakening the expectation. Finish with the complete Phase 2 gate, packed external consumer, protected fingerprint, immutable completed-plan/report check, diff check, and exact staged-path inspection before independent re-verification.
</verification>

<success_criteria>

- Both admin feedback visibility queues and the all-state changelog feed traverse exact 51-row/tie/cursor boundaries without scope leakage.
- Direct foreign reads reject without disclosure, and every activity history representation remains readable and ID-free across snapshot/live/fallback branches.
- Mounted hooks clear and fence admin values across auth identity generations and stale callbacks.
- The clean packed consumer demonstrates actual hidden restore, linked feedback, and readable activity projection values.
- All focused/full gates pass with production, completed 02.3-01 artifacts, canonical verification, and unrelated dirty work unchanged.

</success_criteria>

<output>
Create `.planning/phases/02.3-admin-read-and-projection-completion/02.3-02-SUMMARY.md` when done.
</output>
