---
phase: 02-complete-feedback-to-changelog-workflow
plan: "04"
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/component/_generated/api.ts
  - src/component/_generated/component.ts
  - src/component/_generated/dataModel.ts
  - src/component/_generated/server.ts
  - src/component/schema.ts
  - src/component/validators.ts
  - src/component/model/tags.ts
  - src/component/admin/tags.ts
  - src/component/jobs/tag-cleanup.ts
  - src/component/feedback.ts
  - src/client/contracts.ts
  - src/client/internal.ts
  - src/react/hooks/admin.ts
  - tests/component/tags.test.ts
  - tests/integration/tag-cleanup-backend.test.ts
  - tests/react/tags.test.tsx
  - scripts/test-tag-cleanup-backend.mjs
autonomous: true
requirements: [ADMN-03, ADMN-04, ADMN-10, UI-01, UI-02, UI-03, QUAL-01]
must_haves:
  truths:
    - "Admins can create, rename, delete, assign, and remove stable scope-owned tags; visitors see names and filter by one tag (D-18)."
    - "Rename follows ID references, while widely used deletion cleans joins/search projections in bounded resumable batches and records activity."
    - "Tag hooks expose explicit list/mutation states without browser authority or a generic CRUD API."
  artifacts:
    - path: src/component/admin/tags.ts
      provides: "Narrow tag management and assignment intents"
    - path: src/component/jobs/tag-cleanup.ts
      provides: "Durable bounded tag deletion continuation"
    - path: tests/integration/tag-cleanup-backend.test.ts
      provides: "Crash/resume and scope-isolation proof"
  key_links:
    - from: src/component/admin/tags.ts
      to: src/component/public/search.ts
      via: "Canonical membership changes update the per-tag search projection"
    - from: src/component/jobs/tag-cleanup.ts
      to: src/component/schema.ts
      via: "Scoped job cursor deletes bounded join/projection batches"
---

<objective>
Deliver admin-owned public tag organization from stable records through bounded cleanup and headless controls.

Purpose: Complete the one-tag discovery contract without unbounded arrays, scans, or generic CRUD.
Output: Tag schema/model/admin/job APIs, host/hooks, activity integration, and bounded cleanup tests.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- ranked, searchable, and filterable discovery with admin duplicate merges; authenticated participation; admin moderation with a status-driven roadmap; manually published changelog entries linked to feedback; and in-app notifications backed by a host-consumable delivery outbox -- through tested component APIs and framework-light headless React hooks with explicit async, auth, and error states, **so that** my users and admins can complete the entire feedback lifecycle while I retain ownership of identity, authorization, and my own presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-PATTERNS.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify tag lifecycle and bounded cleanup</name>
  <files>tests/component/tags.test.ts, tests/integration/tag-cleanup-backend.test.ts, tests/react/tags.test.tsx, scripts/test-tag-cleanup-backend.mjs</files>
  <read_first>src/component/schema.ts, src/component/public/search.ts, src/component/model/activity.ts, src/react/hooks/admin.ts</read_first>
  <behavior>
    - Tags have stable opaque IDs and mutable bounded names; only authorized admins manage memberships (D-18).
    - A post has at most 20 tags; assignment is idempotent; rename updates displayed names through ID lookup; delete removes joins/projections and appends activity.
    - Cleanup is resumable, scope-complete, and invisible publicly until coherent completion (D-56).
  </behavior>
  <action>Write red component, React, and disposable-backend tests for create/rename/list/delete/assign/remove, public DTO/search propagation, two scopes, repeated calls, worst-case 20-tag posts, and crash/resume at each cleanup boundary. Assert admin wrappers derive authorization and tags never become browser-owned taxonomy configuration.</action>
  <acceptance_criteria>Tests fail at missing tag/job APIs and demonstrate boundedness, activity, projection correctness, and identical cross-scope/not-found behavior.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/tags.test.ts tests/react/tags.test.tsx</automated></verify>
  <done>The red slice fully specifies ADMN-03/04 and D-18/D-56.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical tag memberships and resumable deletion</name>
  <files>src/component/schema.ts, src/component/validators.ts, src/component/model/tags.ts, src/component/admin/tags.ts, src/component/jobs/tag-cleanup.ts, src/component/feedback.ts</files>
  <read_first>tests/component/tags.test.ts, tests/integration/tag-cleanup-backend.test.ts, src/component/public/search.ts, src/component/model/activity.ts</read_first>
  <action>Use the scoped `tags`, canonical `postTags`, and feed/search projection rows established by discovery; add tag-cleanup job state/indexes and complete their management invariants. Implement narrow create/rename/list/assign/remove/delete intents with normalized unique names, stable IDs, the fixed 20-tag ceiling, scope checks, idempotency, and D-22 activity. Inline small deletes; otherwise atomically create a guarded job, process 50 join/projection rows per scheduled mutation with transaction headroom, and finalize only when no scoped membership remains (D-18, D-56). No unbounded arrays, collect, or post-search filtering. Run `npm run codegen:component` and include generated outputs; never hand-edit them.</action>
  <acceptance_criteria>All tag mutations and cleanup batches are scope-leading, bounded, idempotent, activity-producing, and keep feed/search DTOs coherent.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/tags.test.ts tests/static/schema-scope.test.ts && node scripts/test-tag-cleanup-backend.mjs</automated></verify>
  <done>Admins can safely manage tags of any supported usage cardinality.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose tag admin capabilities and hooks</name>
  <files>src/client/contracts.ts, src/client/internal.ts, src/react/hooks/admin.ts, tests/react/tags.test.tsx</files>
  <read_first>src/component/admin/tags.ts, src/react/hooks/admin.ts, tests/react/tags.test.tsx</read_first>
  <action>Add branded `TagId`, versioned `TagDto`, browser intent validators, authorized host methods, `useTags`, and `useTagManagement`. Preserve per-entity/action pending/error/reset, duplicate-submit guards, unsupported/not-authorized distinctions, and server-truth completion; never optimistically mutate tag membership or accept scope/admin facts from the browser.</action>
  <acceptance_criteria>A React admin can manage and assign tags through injected refs with explicit async states, while public feedback DTOs and filters use the same stable tag IDs/names.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/react/tags.test.tsx && npm run typecheck && npm run lint</automated></verify>
  <done>The tag slice is complete from component storage to provider-neutral headless controls.</done>
</task>

</tasks>

## Artifacts this phase produces

- Complete new-path inventory: `src/component/model/tags.ts`, `src/component/admin/tags.ts`, `src/component/jobs/tag-cleanup.ts`, `tests/component/tags.test.ts`, `tests/integration/tag-cleanup-backend.test.ts`, `tests/react/tags.test.tsx`, `scripts/test-tag-cleanup-backend.mjs`.
- `src/component/model/tags.ts`: membership lookup, DTO projection, limits, and name normalization.
- `src/component/admin/tags.ts`: `listTags`, `createTag`, `renameTag`, `setPostTag`, `deleteTag`.
- `src/component/jobs/tag-cleanup.ts`: `continueTagCleanup` internal mutation/job state.
- `TagId`, `TagDto`, tag intents/results, host capability methods.
- `useTags`, `useTagManagement` and named test/backend harness paths.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser tag intents -> admin wrapper | Untrusted IDs/names require fresh authorization and scope checks. |
| Scheduled cleanup -> canonical joins | Retried batches must not cross scope or expose partial projection state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-12 | Elevation | tag admin intents | high | mitigate | Fresh host authorization, scope-normalized IDs, narrow validators. |
| T-02-13 | Information Disclosure | tag search projection | high | mitigate | Required scope/visibility equality and shared visibility predicate. |
| T-02-14 | Denial of Service | tag deletion | medium | mitigate | 20 tags/post, durable guarded job, 50-row batches, transaction headroom. |
| T-02-15 | Tampering | retrying cleanup | medium | mitigate | Idempotent membership/projection deletion and cursor/state updates in one transaction. |
</threat_model>

<verification>
Run component/React suites, real-backend cleanup harness, schema/contracts audits, typecheck/lint.
</verification>

<success_criteria>
- ADMN-03/04 and tag activity are complete.
- D-18 and D-56 are proven under retry/crash and two-scope conditions.
- Search and feed filters remain honest and projection-driven.
</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-04-SUMMARY.md` when done.
</output>
