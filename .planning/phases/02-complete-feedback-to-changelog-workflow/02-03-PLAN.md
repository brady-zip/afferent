---
phase: 02-complete-feedback-to-changelog-workflow
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/component/_generated/api.ts
  - src/component/_generated/component.ts
  - src/component/_generated/dataModel.ts
  - src/component/_generated/server.ts
  - package.json
  - package-lock.json
  - src/component/convex.config.ts
  - src/component/schema.ts
  - src/component/validators.ts
  - src/component/model/activity.ts
  - src/component/model/errors.ts
  - src/component/model/visibility.ts
  - src/component/participation/posts.ts
  - src/component/participation/comments.ts
  - src/component/participation/votes.ts
  - src/component/admin/posts.ts
  - src/component/admin/activity.ts
  - src/component/feedback.ts
  - src/client/contracts.ts
  - src/client/internal.ts
  - src/react/hooks/admin.ts
  - tests/component/moderation.test.ts
  - tests/component/rate-limits.test.ts
  - tests/integration/rate-limiter-backend.test.ts
  - tests/react/admin.test.tsx
  - scripts/test-rate-limiter-backend.mjs
autonomous: true
requirements: [ADMN-01, ADMN-02, ADMN-05, ADMN-06, ADMN-07, ADMN-08, ADMN-10, UI-01, UI-02, UI-03, QUAL-01]
must_haves:
  truths:
    - "An authorized admin can edit/move posts, freely set any built-in status, lock discussion, archive, restore, and inspect append-only activity."
    - "Status, lifecycle, and discussion lock remain orthogonal; visibility and notification behavior follow D-15..D-17 and D-55."
    - "Participation limits charge once per logical request and return stable actionable RATE_LIMITED errors (D-19, D-20)."
  artifacts:
    - path: src/component/admin/posts.ts
      provides: "Narrow admin moderation/status/lifecycle intents"
    - path: src/component/admin/activity.ts
      provides: "Admin-only append-only paginated activity"
    - path: tests/integration/rate-limiter-backend.test.ts
      provides: "Real transaction/retry accounting proof"
  key_links:
    - from: src/component/admin/posts.ts
      to: src/component/model/activity.ts
      via: "Each admin intent appends one typed activity row in the same transaction"
    - from: src/component/participation/comments.ts
      to: rate-limiter child component
      via: "Committed mutation consumes actor/scope buckets before semantic validation"
---

<objective>
Deliver the authenticated moderation slice with fixed statuses, lifecycle controls, activity history, and transaction-correct participation limits.

Purpose: Give admins safe workflow control while preserving host-owned authorization and predictable user errors.
Output: Rate-limiter child installation, moderation/activity contracts, admin hooks, and component/real-backend tests.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- ranked, searchable, and filterable discovery with admin duplicate merges; authenticated participation; admin moderation with a status-driven roadmap; manually published changelog entries linked to feedback; and in-app notifications backed by a host-consumable delivery outbox -- through tested component APIs and framework-light headless React hooks with explicit async, auth, and error states, **so that** my users and admins can complete the entire feedback lifecycle while I retain ownership of identity, authorization, and my own presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-PATTERNS.md
@.planning/phases/01-secure-installable-feedback-board/01-04-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock moderation, activity, and rate-limit behavior</name>
  <files>tests/component/moderation.test.ts, tests/component/rate-limits.test.ts, tests/integration/rate-limiter-backend.test.ts, tests/react/admin.test.tsx, scripts/test-rate-limiter-backend.mjs</files>
  <read_first>src/component/participation/posts.ts, src/component/participation/comments.ts, src/component/admin/installation.ts, src/client/internal.ts, .planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md</read_first>
  <behavior>
    - Admin authority is re-derived per call; all six statuses transition freely and update `currentStatusSince`; Closed remains visible/votable and does not lock or publish (D-15, D-55).
    - Lock blocks only non-admin comments/replies; archive/withdraw hide uniformly; restore emits activity but no catch-up notification (D-16, D-17).
    - Activity is admin-only, append-only, bounded/cursor-paginated, uses opaque actor IDs, and records D-22 taxonomy with edit field names only (D-21..D-23).
    - Rate limits consume once under validation failures/OCC retries and return operation plus retryAfterMs (D-19, D-20).
  </behavior>
  <action>Create red two-scope component/React tests and a disposable real-backend concurrency test. Freeze library-owned limits: post creation 5/hour/actor and sharded 100/hour/scope; comments/replies/edits 30/10-min/actor and sharded 600/10-min/scope; votes and subscribe/unsubscribe 120/min/actor. Admin intents bypass participation buckets.</action>
  <acceptance_criteria>Tests fail on missing moderation/activity/rate contracts, not harness setup, and cover authorization, visibility, counter/search projection updates, and actionable errors.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/moderation.test.ts tests/component/rate-limits.test.ts tests/react/admin.test.tsx</automated></verify>
  <done>The red matrix specifies ADMN-01/02/05/06/07/08/10 and D-15..D-23.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transactionally coherent moderation, activity, and limits</name>
  <files>package.json, package-lock.json, src/component/convex.config.ts, src/component/schema.ts, src/component/validators.ts, src/component/model/activity.ts, src/component/model/errors.ts, src/component/model/visibility.ts, src/component/participation/posts.ts, src/component/participation/comments.ts, src/component/participation/votes.ts, src/component/admin/posts.ts, src/component/admin/activity.ts, src/component/feedback.ts</files>
  <read_first>tests/component/moderation.test.ts, tests/component/rate-limits.test.ts, src/component/model/visibility.ts, src/component/model/content.ts</read_first>
  <action>Install audit-approved `@convex-dev/rate-limiter@0.3.2`, register the child component, and consume fixed per-intent actor plus sharded scope buckets inside committing mutations with `reserve:false`. Return expected post-consumption validation failures so the charge commits once; map denial to versioned `RATE_LIMITED { operation, retryAfterMs }`. Add archived/discussionLocked/status/currentStatusSince fields and scoped activity rows/indexes. Implement explicit admin `editPost`, `movePost`, `setPostStatus`, `setDiscussionLock`, `setArchived`, and `listPostActivity`; update feed/search/tag projections atomically, append D-22 events, store only changed-field names/typed metadata, resolve current actor display at read time, and apply one visibility predicate everywhere (D-15..D-23, D-53, D-55, D-57, D-58). Run `npm run codegen:component` and include generated outputs; never hand-edit them.</action>
  <acceptance_criteria>Every admin call independently authorizes in the host, every component intent is scope-checked/bounded, status/lifecycle/lock remain orthogonal, and real-backend retries neither bypass nor multiply rate charges.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/moderation.test.ts tests/component/rate-limits.test.ts tests/static/schema-scope.test.ts tests/static/contracts.test.ts && node scripts/test-rate-limiter-backend.mjs</automated></verify>
  <done>Admins can moderate through stable APIs and users receive predictable limits without visibility or history leaks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose trusted admin gating and moderation mutation state</name>
  <files>src/client/contracts.ts, src/client/internal.ts, src/react/hooks/admin.ts, tests/react/admin.test.tsx</files>
  <read_first>src/component/admin/posts.ts, src/component/admin/activity.ts, src/react/provider.tsx, tests/react/admin.test.tsx</read_first>
  <action>Extend admin host capabilities and create `useAdminCapability`, `usePostModeration`, and `usePostActivity`. Admin affordance comes only from an injected trusted host query with loading state; never accept `isAdmin` from props (D-45). Expose per-post/per-action pending/error/reset state, duplicate-submit guards, not-configured vs not-authorized, typed result unions, delayed rate-limit retry metadata, and no optimistic changes for moderation/status/archive (D-46, D-48, D-50, D-51).</action>
  <acceptance_criteria>Admin controls can be gated without becoming authority, each mutation reports explicit async/error state, and the component still authorizes every operation on the server.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/react/admin.test.tsx && npm run typecheck && npm run lint</automated></verify>
  <done>The full moderation slice is usable through provider-neutral headless hooks.</done>
</task>

</tasks>

## Artifacts this phase produces

- Complete new-path inventory: `src/component/model/activity.ts`, `src/component/admin/posts.ts`, `src/component/admin/activity.ts`, `src/react/hooks/admin.ts`, `tests/component/moderation.test.ts`, `tests/component/rate-limits.test.ts`, `tests/integration/rate-limiter-backend.test.ts`, `tests/react/admin.test.tsx`, `scripts/test-rate-limiter-backend.mjs`.
- `src/component/model/activity.ts`: activity taxonomy, append/map helpers, metadata types.
- `src/component/admin/posts.ts`: `editPost`, `movePost`, `setPostStatus`, `setDiscussionLock`, `setArchived`.
- `src/component/admin/activity.ts`: `listPostActivity`.
- Stable `RATE_LIMITED` public error variant and operation metadata.
- Admin host capability methods and `useAdminCapability`, `usePostModeration`, `usePostActivity`.
- Named component/React/rate-backend test paths and `scripts/test-rate-limiter-backend.mjs`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser admin intent -> host wrapper | Untrusted admin-shaped actions require fresh host authorization. |
| Participation mutation -> rate child | Actor/scope keys and transaction outcome control abuse accounting. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-08 | Spoofing / Elevation | admin host capabilities | critical | mitigate | Resolve scope and independently authorize every call; component rechecks entity scope. |
| T-02-09 | Information Disclosure | activity/archive projections | high | mitigate | Admin-only activity plus shared visibility/not-found equivalence across all public projections. |
| T-02-10 | Denial of Service | rate-limit transaction | high | mitigate | Server-derived keys, fixed caps/shards, consume-on-commit semantics, OCC real-backend test. |
| T-02-11 | Repudiation | append-only activity | medium | mitigate | Typed immutable rows with occurrence time and opaque initiating actor ID. |
| T-02-SC | Tampering | npm install | high | mitigate | Install only audited `@convex-dev/rate-limiter@0.3.2`; verify exact lock/runtime packing and no lifecycle script. |
</threat_model>

<verification>
Run focused component/React suites, real-backend rate harness, schema/contracts audits, typecheck/lint, and packed artifact regression.
</verification>

<success_criteria>
- ADMN-01/02/05/06/07/08/10 work end to end.
- D-15..D-23 are enforced transactionally and server-authorized.
- No status transition becomes hidden automation.
</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-03-SUMMARY.md` when done.
</output>
