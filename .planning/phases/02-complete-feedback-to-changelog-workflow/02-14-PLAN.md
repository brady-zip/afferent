---
phase: 02-complete-feedback-to-changelog-workflow
plan: "14"
type: execute
wave: 14
depends_on: ["02-13"]
files_modified:
  - src/react/query.ts
  - tests/react/live-headless.test.tsx
  - scripts/test-headless-backend.mjs
  - tests/integration/headless-backend.test.mjs
autonomous: true
gap_closure: true
requirements: [UI-03, QUAL-01]
must_haves:
  truths:
    - "Every committed paginated-store notification contains either the exact prior canonical loaded window or the exact current canonical loaded window; a notification never combines page results from different Convex client transitions (D-47)."
    - "Each page-watch callback is only a dirty signal: the store synchronously rereads every descriptor in the current committed or replacement page set before deciding whether one atomic snapshot may be published (D-47)."
    - "A structurally inconsistent, loading, or partially available descriptor set retains the last coherent result array; a coherent set publishes its exact ordered concatenation once, while a query error publishes only the maximal same-transition coherent prefix plus its typed error (D-46, D-50, D-51)."
    - "Append, split, collapse, load-more, error recovery, and identity-generation replacement preserve atomic page-set swaps and reject stale reads or publications without adding a public revision, watermark, schema field, or DTO member (D-47, D-52, D-58)."
    - "Mounted and real Convex acceptance tests observe every store publication across insertion, deletion, sort movement, split, collapse, pending load-more, failure, recovery, and generation change; settled exact-canonical checks remain in addition to the temporal oracle (QUAL-01)."
  artifacts:
    - path: src/react/query.ts
      provides: "All-descriptor synchronous reread and last-coherent-snapshot publication gate"
    - path: tests/react/live-headless.test.tsx
      provides: "Controlled same-transition listener-interleaving and structural/error/generation notification matrix"
    - path: scripts/test-headless-backend.mjs
      provides: "Disposable Convex every-publication prior-or-current canonical oracle and client-atomicity probe"
    - path: tests/integration/headless-backend.test.mjs
      provides: "Release audit requiring temporal notification coverage without a public transition token"
  key_links:
    - from: node_modules/convex/src/browser/sync/optimistic_updates_impl.ts
      to: src/react/query.ts
      via: "Convex installs the complete local query-result map before changed-query listeners run, so one callback can synchronously reread all active watches"
    - from: src/react/query.ts
      to: tests/react/live-headless.test.tsx
      via: "Controlled multi-page updates fire listeners one at a time after all record values change and assert that no partial cache mutation is published"
    - from: scripts/test-headless-backend.mjs
      to: src/react/query.ts
      via: "Every real store listener notification is captured and compared to the mutation's exact prior and current canonical arrays"
    - from: tests/integration/headless-backend.test.mjs
      to: scripts/test-headless-backend.mjs
      via: "Static release audit requires notification capture, prior/current equality, client-atomicity evidence, and settled canonical assertions"
---

<objective>
Make the end-cursor-pinned paginated store revision-coherent at every publication by treating watch callbacks as dirty signals and synchronously rereading the complete descriptor set before composing a snapshot.

Purpose: Close the sole remaining 38/39 D-47/QUAL-01 gap: Plan 02-13 is exact when settled, but its per-page cache assignment can briefly publish a deletion from one updated page beside stale sibling pages.
Output: A no-token all-page coherence gate, red-first mounted and real every-notification oracles, unchanged public contracts, and a final 39/39 Phase 2 verification.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- ranked, searchable, and filterable discovery with admin duplicate merges; authenticated participation; admin moderation with a status-driven roadmap; manually published changelog entries linked to feedback; and in-app notifications backed by a host-consumable delivery outbox -- through tested component APIs and framework-light headless React hooks with explicit async, auth, and error states, **so that** my users and admins can complete the entire feedback lifecycle while I retain ownership of identity, authorization, and my own presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-PATTERNS.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-DISCUSSION-LOG.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-VERIFICATION.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-13-PLAN.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-13-SUMMARY.md
@src/react/query.ts
@tests/react/live-headless.test.tsx
@scripts/test-headless-backend.mjs
@tests/integration/headless-backend.test.mjs
@node_modules/convex/src/react/client.ts
@node_modules/convex/src/browser/sync/client.ts
@node_modules/convex/src/browser/sync/optimistic_updates_impl.ts

<interfaces>
From `src/react/query.ts`:
- `Watch<T>.onUpdate(listener)` reports that a watched result changed; `Watch<T>.localQueryResult()` synchronously returns the current locally cached result, `undefined`, or throws the current query error.
- `PageDescriptor<T>` owns the fixed `cursor`/optional `endCursor`, watch, last result/error, epoch, and structural-replacement state.
- `StructuralOperation<T>` atomically replaces one contiguous descriptor range for append, split, or collapse.
- `PaginatedWatchSnapshot<T>` is the unchanged public `{ results, status, error?, loadMore, retry }` contract.

From `node_modules/convex/src/react/client.ts` and `node_modules/convex/src/browser/sync/{client,optimistic_updates_impl}.ts`:
- `notifyOnQueryResultChanges` first calls `ingestQueryResultsFromServer`, which replaces the complete local `queryResults` map and reapplies optimistic updates, and only then calls the transition handlers.
- `ConvexReactClient.handleTransition` receives all changed query tokens and invokes their listeners synchronously in order; any listener may therefore call `localQueryResult()` on every subscribed sibling and observe the already-installed transition.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add red every-publication coherence oracles and prove the client transition premise</name>
  <files>tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs</files>
  <read_first>src/react/query.ts, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs, node_modules/convex/src/react/client.ts, node_modules/convex/src/browser/sync/client.ts, node_modules/convex/src/browser/sync/optimistic_updates_impl.ts</read_first>
  <behavior>
    - The controlled client applies one logical revision to all matching page records before invoking their listeners individually. Every resulting store publication must equal the exact prior or current ordered array; the existing implementation fails because the first callback publishes a changed page with stale siblings.
    - The real harness subscribes one recorder for the lifetime of each scenario and validates every emitted result array, not only snapshots selected by `waitFor`. For ordinary mutations, only the exact pre-mutation and post-mutation canonical cursor-window arrays are allowed; for fault transitions, only the exact expected coherent prefix with typed error is allowed.
    - A direct real-client probe watches at least three page queries changed by one backend mutation. Inside every underlying listener it synchronously rereads all page watches and proves they expose one revision before the store architecture is changed.
    - Settled `assertCanonicalWindow`, active-boundary, exact-once disposal, native split-threshold, and packed/full checks remain mandatory; the temporal oracle supplements rather than replaces them.
  </behavior>
  <action>Extend `ControlledWatchClient` with a transaction-style update helper that writes all matching record values first and only then invokes listeners in deterministic order. Record every `createPaginatedWatchStore` notification and add exact prior/current assertions for cross-window insertion, deletion, and sort movement, including the verifier's `[a,b,e,back,f]` to `[e,a,b,back,f]` movement that currently exposes `[a,b,back,f]`. In the disposable backend harness, bracket every mutation with exact previous/current canonical label arrays, capture every store callback until the exact current state settles, and fail immediately on any third array even if a later tick converges. Add equivalent allowed-state rules for pending append/split/collapse, where results must stay at the last coherent array until the full replacement is available, and for typed page failure/recovery, where the exact maximal prefix is the only error result. Add the direct Convex-client atomicity probe described in the behavior block. If that probe demonstrates that sibling `localQueryResult()` calls themselves span multiple transitions, stop this plan and replan through the required live-radio checkpoint; only then may a private fallback scoped to one `(scope, domain)` be considered. Do not introduce a global or public transition value. Strengthen the integration source audit so selected settled snapshots cannot satisfy QUAL-01 without per-notification capture and prior/current exact equality.</action>
  <acceptance_criteria>The new mounted and real temporal tests fail on the existing per-page cache publication with the mixed `[a,b,back,f]` reorder tick, while the direct-client probe passes and establishes that an all-watch synchronous reread can observe one installed transition.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run --config vitest.react.config.ts tests/react/live-headless.test.tsx && node --test tests/integration/headless-backend.test.mjs && node scripts/test-headless-backend.mjs</automated></verify>
  <done>The remaining defect has a deterministic red oracle that observes every notification and cannot be satisfied by eventual convergence, status-only checks, or selected `waitFor` snapshots.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate publication on one synchronous all-descriptor reread</name>
  <files>src/react/query.ts, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs</files>
  <read_first>tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, src/react/query.ts, node_modules/convex/src/react/client.ts, node_modules/convex/src/browser/sync/client.ts, node_modules/convex/src/browser/sync/optimistic_updates_impl.ts</read_first>
  <behavior>
    - Any committed-page callback rereads every descriptor in the captured committed page array synchronously and stages all values/errors before changing descriptor caches or publishing.
    - A reread publishes the exact concatenation only when the captured descriptor array is still current, its epoch is current, every descriptor is available, and each adjacent pair satisfies `endCursor[i] === cursor[i+1]`. Otherwise it retains the last coherent results.
    - A page error publishes the exact maximal available, boundary-contiguous prefix from the same reread plus the mapped typed error; recovery performs another complete reread and restores the exact full chain without remounting.
    - Append, split, and collapse candidates use the same all-candidate staging rule. A new loading descriptor or incomplete replacement retains the prior coherent results; a complete validated replacement swaps the descriptor set synchronously and the next all-current-page reread is the only publication path.
    - Epoch, page-array identity, operation identity, and disposal checks fence the start, completion, cache commit, descriptor swap, and publication of each reread.
  </behavior>
  <action>Refactor the paginated store's `read`/`refreshSnapshot` seam so an `onUpdate` callback never assigns only its triggering descriptor and immediately composes cached siblings. Capture the current epoch plus exact descriptor-array and operation identities, synchronously call `localQueryResult()` for every descriptor in the relevant committed or replacement set inside guarded try/catch reads, and stage results, `undefined`, and mapped errors in temporary entries. Recheck all fences after the loop and before any cache mutation or publication. Validate descriptor boundaries before accepting staged data. When any descriptor is unavailable, the chain is structurally inconsistent, or a replacement is incomplete, keep the exact last coherent result array while reporting the appropriate loading state; do not publish a newly changed prefix beside old siblings. When all descriptors are coherent, atomically update their cached states and publish the exact ordered concatenation once. If descriptor `i` throws, atomically publish only the staged coherent results from descriptors `0..i-1` plus the typed error and leave every watch live for recovery. Candidate append, split, and collapse reads must stage the whole replacement set; when complete, synchronously replace `pages`, then route through the same all-current-page reread before publishing. Preserve one unbounded tail, boundary pinning, structural serialization, required-split behavior, invalid-cursor restart, and all Plan 02-12/02-13 generation fences. Keep the public store, component schema, host bindings, validators, and DTOs unchanged: Convex's already-atomic local client transition is the coherence source, so no revision or watermark field is added.</action>
  <acceptance_criteria>Every publication is derived from one complete synchronous reread of one captured descriptor set, and the red every-notification matrix turns green without a public contract or schema change.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run --config vitest.react.config.ts tests/react/live-headless.test.tsx && node scripts/test-headless-backend.mjs && npm run typecheck && npm run lint</automated></verify>
  <done>The common paginated store cannot expose a one-page-new/sibling-old result array; loading, errors, structural replacement, recovery, and generation changes all pass through one atomic coherence gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stress every transition and close the packed Phase 2 gate</name>
  <files>src/react/query.ts, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs</files>
  <read_first>src/react/query.ts, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs, tests/integration/packed-artifact.test.mjs, package.json</read_first>
  <behavior>
    - Repeated real insert, delete, cross-window move, recommended split, required split, empty collapse, and error/recovery scenarios reject every mixed intermediate publication and still settle on exact canonical arrays.
    - A mutation while `loadMore` is pending and two rapid load requests retain the exact prior window until the complete new descriptor set is coherent; no partial append, duplicate boundary, orphan watch, or second unbounded tail appears.
    - Generation replacement during committed-page reread and during every structural operation immediately exposes the new generation's cold state; no old callback changes its results, error, status, descriptors, or listeners.
    - All public domain paginators retain their state vocabulary, declarations, packed runtime behavior, and non-throwing direct-query guarantees.
  </behavior>
  <action>Run the mounted controlled matrix with listener order permutations and repeat the real backend mutation matrix enough times to exercise changed-query callback ordering. Cover front/middle/back insertion, front/middle deletion, cross-window moves in both directions, opportunistic and required split, empty first/middle/tail collapse, mutation during pending `loadMore`, rapid `loadMore`, first/middle/tail errors, recovery, invalid cursor, retry, unsubscription, disposal, and identity-generation replacement. For each ordinary backend mutation, retain the every-notification prior-or-current oracle and then run the settled exact-canonical assertion; for fault scenarios, require the exact typed prefix/error and exact recovered canonical view. Keep direct-client atomicity probing as executable evidence. Record the rejected alternative in test/docs expectations: a server watermark is not the primary design because an unchanged sibling query would not necessarily advance with the changed page, causing false loading or over-invalidation. The only permitted contingency is a private value scoped to one `(scope, domain)`, and it is outside this plan unless the empirical client probe fails and the live-radio replanning gate accepts it. Run every React suite, integration audit, real backend oracle, static contract, typecheck, lint, build, clean packed tarball consumer, and aggregate Phase 2 gate. Re-run verification and require 39/39 with UI-03 and QUAL-01 satisfied.</action>
  <acceptance_criteria>The complete temporal and settled matrices pass repeatedly, public/package contracts are unchanged, all release gates are green, and Phase 2 re-verification reaches 39/39.</acceptance_criteria>
  <verify><automated>npm run test:react && node --test tests/integration/headless-backend.test.mjs && node scripts/test-headless-backend.mjs && npm run test:static && npm run typecheck && npm run lint && npm run build && npm run test:package && npm run test:phase2</automated></verify>
  <done>D-47 is exact at every real store publication, prior non-throwing/generation/structural guarantees remain green, the packed consumer passes, and no temporal pagination gap remains.</done>
</task>

</tasks>

## Source Coverage Audit

| Source | ID / constraint | Plan coverage | Status |
|---|---|---|---|
| GOAL | Complete provider-neutral lifecycle through tested headless APIs | This additive plan changes only the last incorrect publication seam; all completed domain behavior remains a regression gate. | COVERED |
| REQ | UI-03 | Exact prior/current pagination results and coherent typed loading/error states at every notification. | COVERED |
| REQ | QUAL-01 | Mounted, disposable-real-backend, integration-source, packed, and full-phase temporal proofs. | COVERED |
| CONTEXT | D-47 | All-page synchronous reread preserves reactive accumulated cursor windows without a parallel page cache. | COVERED |
| CONTEXT | D-43, D-46, D-50, D-51, D-52, D-58 | No provider coupling, thrown render error, public token, schema/DTO widening, or generation regression is introduced. | COVERED |
| RESEARCH | Pattern 8 and Pitfall 7 | The installed client result store is treated as canonical; the library only stages a snapshot from one synchronous read of it. | COVERED |
| PATTERNS | Headless React and test layers | The common store plus mounted/real/packed gates remain the assigned files and proof boundaries. | COVERED |
| VERIFICATION | Sole 38/39 mixed-revision publication gap | Every notification must be exact prior/current; the known shortened reorder tick is the red acceptance case. | COVERED |
| PRIOR PLAN | 02-13 contiguous page-window state machine | End-cursor pinning, one tail, structural serialization, exact settled arrays, errors, and generation fences are preserved and strengthened. | COVERED |

No source item is missing. All other D-01..D-58 decisions and Phase 2 requirement IDs remain owned by completed Plans 02-01 through 02-13 and are exercised by the final regression gate; this plan neither reopens deferred ideas nor changes another phase boundary.

## Artifacts this phase produces

- One all-descriptor synchronous reread path that stages a complete committed or replacement page set before cache mutation or publication.
- A last-coherent result gate for unavailable pages and structural swaps, plus exact same-transition prefix/error publication and recovery.
- Mounted and real-backend recorders that inspect every store notification while retaining exact settled canonical and boundary checks.
- Executable evidence that the Convex local query store applies a complete transition before listeners run.
- Full React/static/type/lint/build/packed/Phase 2 regressions and a final 39/39 verification result.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Convex local transition -> page-watch listener | All changed query results are installed before individual listeners report dirtiness. |
| Page-watch callback -> published React snapshot | One callback must not publish its page beside sibling caches from another transition. |
| Structural replacement -> committed descriptor chain | Loading candidates and synchronous swaps must retain the last coherent public window. |
| Query error -> coherent public prefix | Error state must not include later pages disconnected from the same-transition prefix. |
| Auth generation -> synchronous reread/operation | Old page arrays and callbacks must not commit after replacement or disposal. |
| Test recorder -> release decision | Settled-only checks can repudiate evidence of a falsifying intermediate publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-60 | Tampering / Information Disclosure | multi-page published snapshot | critical | mitigate | Stage one synchronous reread of every current watch, recheck identity/epoch fences, and atomically publish only exact concatenation. |
| T-02-61 | Tampering | per-descriptor result cache | high | mitigate | Do not mutate one descriptor before sibling reads complete; commit staged cache states as one accepted set. |
| T-02-62 | Denial of Service / Tampering | append/split/collapse loading | high | mitigate | Retain the last coherent array until every descriptor in the replacement set is available and boundary-valid. |
| T-02-63 | Information Disclosure / Tampering | page query error | high | mitigate | Derive only the maximal same-transition coherent prefix, map the typed error, keep all watches live, and require exact recovery. |
| T-02-64 | Information Disclosure / Tampering | stale generation reread | critical | mitigate | Fence epoch, descriptor-array identity, operation identity, cache commit, swap, and publish; dispose old watches exactly once. |
| T-02-65 | Repudiation | real-backend oracle | high | mitigate | Capture every publication and allow only exact prior/current canonical arrays, with separate exact error-prefix rules and settled checks. |
| T-02-66 | Denial of Service | revision-watermark alternative | medium | accept | Keep it out of the primary design because unchanged queries can over-invalidate; require empirical client failure plus live-radio replanning before any scoped contingency. |
</threat_model>

<verification>
Run the red-first controlled and disposable real Convex every-publication matrices, prove the local client transition is atomically visible to sibling reads, implement the all-descriptor coherence gate, and then run the complete React/static/type/lint/build/packed and Phase 2 suites. Re-run Phase 2 verification and require 39/39. Eventual convergence, selected `waitFor` snapshots, exact settled arrays alone, status labels, source inspection, or a public transition field cannot satisfy the gate.
</verification>

<success_criteria>
- Every ordinary store notification is exactly the prior or current canonical cursor-bounded loaded window; no third mixed array is observable.
- Every accepted snapshot comes from one synchronous staged reread of one structurally contiguous descriptor set.
- Loading or incomplete structural sets retain the last coherent results; errors expose only the exact same-transition prefix plus typed error and recover in place.
- Append, split, collapse, load-more, retry, invalid-cursor restart, disposal, and generation change remain serialized and fenced.
- The component schema, host bindings, validators, DTOs, public hook types, and packed consumer contract gain no transition token or watermark.
- All prior Phase 2 guarantees and the full release gate pass; re-verification reports 39/39.
</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-14-SUMMARY.md` when done.
</output>
