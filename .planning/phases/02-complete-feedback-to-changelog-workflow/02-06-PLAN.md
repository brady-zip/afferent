---
phase: 02-complete-feedback-to-changelog-workflow
plan: "06"
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - src/component/_generated/api.ts
  - src/component/_generated/component.ts
  - src/component/_generated/dataModel.ts
  - src/component/_generated/server.ts
  - src/component/schema.ts
  - src/component/validators.ts
  - src/component/public/roadmap.ts
  - src/component/feedback.ts
  - src/client/contracts.ts
  - src/client/internal.ts
  - src/react/hooks/roadmap.ts
  - src/react/index.ts
  - tests/component/roadmap.test.ts
  - tests/react/roadmap.test.tsx
autonomous: true
requirements: [RMAP-01, RMAP-02, RMAP-03, UI-01, UI-02, UI-03, QUAL-01]
must_haves:
  truths:
    - "Visitors can independently page Planned, In Progress, and recent Complete roadmap groups, optionally for one board (D-24)."
    - "Rows order by currentStatusSince, creation time, and opaque ID; roadmap has no independent record/manual order (D-25)."
    - "The fixed Complete window and shared visibility predicate prevent hidden or stale projection leaks (D-26, D-53)."
  artifacts:
    - path: src/component/public/roadmap.ts
      provides: "Three independently paginated status-derived roadmap queries"
    - path: src/react/hooks/roadmap.ts
      provides: "Headless grouped roadmap state and per-column pagination"
    - path: tests/component/roadmap.test.ts
      provides: "Projection, order, visibility, and two-scope matrix"
  key_links:
    - from: src/component/public/roadmap.ts
      to: src/component/model/visibility.ts
      via: "Every roadmap row uses the same public visibility key/predicate"
    - from: src/react/hooks/roadmap.ts
      to: src/component/public/roadmap.ts
      via: "Independent helper pagination state per status group"
---

<objective>
Deliver the public status-derived roadmap from indexed component projections through a grouped headless hook.

Purpose: Expose current product direction without creating an independent roadmap entity or status automation.
Output: Roadmap indexes/query/contracts, host wrapper, grouped hook, and projection tests.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- ranked, searchable, and filterable discovery with admin duplicate merges; authenticated participation; admin moderation with a status-driven roadmap; manually published changelog entries linked to feedback; and in-app notifications backed by a host-consumable delivery outbox -- through tested component APIs and framework-light headless React hooks with explicit async, auth, and error states, **so that** my users and admins can complete the entire feedback lifecycle while I retain ownership of identity, authorization, and my own presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-PATTERNS.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the grouped roadmap projection</name>
  <files>tests/component/roadmap.test.ts, tests/react/roadmap.test.tsx</files>
  <read_first>src/component/public/feeds.ts, src/component/model/visibility.ts, src/component/admin/posts.ts, src/react/provider.tsx</read_first>
  <behavior>
    - Exactly Planned/In Progress/Complete appear; Closed/Open/Under Review never do; each group pages independently (D-24).
    - Status transitions update `currentStatusSince`; newest transition wins with creation/ID ties (D-25).
    - Complete is limited to the planner-selected 90-day window; older complete posts remain in feedback/changelog history; board/visibility filtering is scope-safe (D-26).
  </behavior>
  <action>Create red component and React tests with uneven group sizes, board filters, transitions into/out of groups, archived/withdrawn/merged rows, authenticated-read policy, two scopes, and independent load-more state. Prove status changes do not publish changelog, lock comments, disable votes, or remove links (D-55).</action>
  <acceptance_criteria>Tests fail at missing roadmap API/hook and cover RMAP-01/02/03 plus D-24..D-26/D-53/D-55.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/roadmap.test.ts tests/react/roadmap.test.tsx</automated></verify>
  <done>The roadmap user path is defined without an independent table or combined-starvation query.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scope-complete independently paginated roadmap groups</name>
  <files>src/component/schema.ts, src/component/validators.ts, src/component/public/roadmap.ts, src/component/feedback.ts, src/client/contracts.ts, src/client/internal.ts</files>
  <read_first>tests/component/roadmap.test.ts, src/component/public/feeds.ts, src/component/model/visibility.ts, src/client/internal.ts</read_first>
  <action>Add only the real scope/status[/board]/visibility/currentStatusSince/createdAt/ID index shapes. Implement `listRoadmapGroup` for one of the three fixed statuses per call with helper pagination, a fixed 90-day Complete cutoff, max 50 rows/page, stable versioned `RoadmapItemDto`, board option, and shared read-policy/visibility enforcement (D-24..D-26, D-53). Expose the narrow host read and validators; do not add roadmap writes, manual ordering, or status-triggered publication (D-55). Run `npm run codegen:component` and include generated outputs; never hand-edit them.</action>
  <acceptance_criteria>Each group is independently bounded/indexed, filtered before pagination, and returns only currently visible canonical posts with preserved helper metadata.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/roadmap.test.ts tests/static/schema-scope.test.ts tests/static/contracts.test.ts</automated></verify>
  <done>The component/host roadmap is a faithful current-status projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose grouped roadmap state through headless React</name>
  <files>src/react/hooks/roadmap.ts, src/react/index.ts, tests/react/roadmap.test.tsx</files>
  <read_first>src/client/contracts.ts, src/react/hooks/feedback.ts, tests/react/roadmap.test.tsx</read_first>
  <action>Create `useRoadmap` that runs the three injected paginated refs independently and returns a fixed keyed group object with per-group items/loading/empty/error/loadMore/isLoadingMore/canLoadMore plus shared board/auth state. Use helper results as canonical, reset on board/auth-generation changes, report unsupported when roadmap refs are omitted, and perform no navigation/toasts.</action>
  <acceptance_criteria>A consumer can render and page each roadmap column independently through provider-neutral refs with explicit states and no copied UI.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/react/roadmap.test.tsx && npm run typecheck && npm run lint</automated></verify>
  <done>The roadmap slice is complete through the headless contract.</done>
</task>

</tasks>

## Artifacts this phase produces

- Complete new-path inventory: `src/component/public/roadmap.ts`, `src/react/hooks/roadmap.ts`, `tests/component/roadmap.test.ts`, `tests/react/roadmap.test.tsx`.
- `src/component/public/roadmap.ts`: `listRoadmapGroup`, `ROADMAP_COMPLETE_WINDOW_MS`, roadmap status validator.
- `RoadmapItemDto`, `RoadmapGroupPageDto`, roadmap intent/result validators and host method.
- `src/react/hooks/roadmap.ts`: `useRoadmap` and fixed grouped state types.
- Named roadmap component/React tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser roadmap filter -> host read | Board/status/pagination are untrusted inputs. |
| Status projection -> public visitor | Secondary projection can leak hidden/cross-scope post existence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-20 | Information Disclosure | roadmap indexes | high | mitigate | Scope/visibility/status in index range, direct board scope check, two-scope tests. |
| T-02-21 | Tampering | roadmap ordering | medium | mitigate | Transactional currentStatusSince plus total-order tie keys; no client/manual order. |
| T-02-22 | Denial of Service | group pagination | low | accept | Three independent max-50 helper pages and fixed Complete time range bound work. |
</threat_model>

<verification>
Run roadmap suites, schema/contracts audits, typecheck/lint, and discovery/moderation regressions.
</verification>

<success_criteria>
- RMAP-01/02/03 work through component, host, and hooks.
- D-24..D-26 and D-53/D-55 remain executable invariants.
- No roadmap entity or changelog automation exists.
</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-06-SUMMARY.md` when done.
</output>
