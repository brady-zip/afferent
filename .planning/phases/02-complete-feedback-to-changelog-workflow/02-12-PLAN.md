---
phase: 02-complete-feedback-to-changelog-workflow
plan: "12"
type: execute
wave: 12
depends_on: ["02-11"]
files_modified:
  - package.json
  - package-lock.json
  - vitest.react.config.ts
  - src/react/provider.tsx
  - src/react/bindings.ts
  - src/react/query.ts
  - src/react/index.ts
  - src/react/hooks/mutations.ts
  - src/react/hooks/feedback.ts
  - src/react/hooks/roadmap.ts
  - src/react/hooks/changelog.ts
  - src/react/hooks/notifications.ts
  - src/react/hooks/admin.ts
  - tests/react/live-headless.test.tsx
  - tests/react/provider.test.tsx
  - tests/react/mutations.test.tsx
  - tests/react/feedback.test.tsx
  - tests/react/feedback-search.test.tsx
  - tests/react/roadmap.test.tsx
  - tests/react/changelog.test.tsx
  - tests/react/notifications.test.tsx
  - tests/react/admin.test.tsx
  - tests/react/tags.test.tsx
  - tests/react/merge.test.tsx
  - tests/integration/headless-backend.test.mjs
  - scripts/test-headless-backend.mjs
  - tests/integration/packed-artifact.test.mjs
  - tests/static/contracts.test.ts
  - tests/static/exports.test.ts
  - fixtures/packed-vite-convex/src/App.tsx
  - fixtures/packed-vite-convex/src/App.test.tsx
  - fixtures/packed-vite-convex/convex/afferent.ts
  - scripts/test-packed-consumer.mjs
autonomous: true
gap_closure: true
requirements: [UI-03, QUAL-01]
must_haves:
  truths:
    - "Every published direct and paginated read hook catches initial and reactive Convex query failures into the closed typed Afferent error state without throwing through render, Suspense, or a library-owned error boundary (D-46, D-50, D-51)."
    - "Pagination keeps every previously successful ordered page when a later page fails, recovers reactively, and handles growth, shrink, and split replacement without gaps, duplicates, or ID-based deduplication (D-47)."
    - "Authenticated hosts supply a non-empty opaque identity token that is stable within one identity and distinct across identities; Afferent synchronously advances a local generation on every auth/token transition and has no authenticated default identity (D-43, D-52)."
    - "Query/page snapshots, pending/error/retry state, native optimistic projections, and async completions are generation-owned, so logout/login, A-to-B, and rapid A-to-B-to-A switches show no prior actor state on the first committed render (D-48, D-49, D-52)."
    - "Mounted React tests and a disposable real Convex backend prove failure, recovery, identity-switch, and stale-completion behavior; mapper-only fabricated Error states cannot satisfy the Phase 2 gate (QUAL-01)."
  artifacts:
    - path: src/react/query.ts
      provides: "Non-throwing direct and ordered page watch stores consumed through useSyncExternalStore"
    - path: src/react/provider.tsx
      provides: "Injected watch client plus required identity-token and synchronous generation contract"
    - path: tests/react/live-headless.test.tsx
      provides: "Mounted every-hook error, recovery, pagination, account-switch, and stale-completion matrix"
    - path: scripts/test-headless-backend.mjs
      provides: "Disposable real Convex watchQuery failure/recovery and reactive-page oracle"
  key_links:
    - from: src/react/provider.tsx
      to: src/react/query.ts
      via: "Provider-injected watch client and monotonically advancing local generation select a cold generation-owned store"
    - from: src/react/query.ts
      to: src/react/hooks/feedback.ts
      via: "Direct and paginated domain hooks consume stable non-throwing useSyncExternalStore snapshots instead of Convex throwing hooks"
    - from: src/react/hooks/mutations.ts
      to: src/react/provider.tsx
      via: "Every mutation/retry captures the start generation and fences all completion-side state writes"
    - from: tests/react/live-headless.test.tsx
      to: scripts/test-headless-backend.mjs
      via: "The same failure, recovery, page-retention, and split invariants run mounted with controlled watches and against real Convex watches"
---

<objective>
Make the published headless React contract actually non-throwing under live query failures and strictly isolate all client state across authenticated identity transitions.

Purpose: Close the two actionable gaps in `02-VERIFICATION.md` without reopening completed Phase 2 backend, merge, moderation, roadmap, changelog, notification, or delivery work.
Output: A provider-injected `watchQuery` adapter, ordered reactive pagination, required identity-token generations, stale async fencing, mounted acceptance tests, and a real-backend oracle.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- ranked, searchable, and filterable discovery with admin duplicate merges; authenticated participation; admin moderation with a status-driven roadmap; manually published changelog entries linked to feedback; and in-app notifications backed by a host-consumable delivery outbox -- through tested component APIs and framework-light headless React hooks with explicit async, auth, and error states, **so that** my users and admins can complete the entire feedback lifecycle while I retain ownership of identity, authorization, and my own presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-PATTERNS.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-DISCUSSION-LOG.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-VERIFICATION.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-10-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-11-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Turn both verifier gaps into mounted and real-backend acceptance oracles</name>
  <files>package.json, package-lock.json, vitest.react.config.ts, tests/react/live-headless.test.tsx, tests/react/provider.test.tsx, tests/react/mutations.test.tsx, tests/react/feedback.test.tsx, tests/react/feedback-search.test.tsx, tests/react/roadmap.test.tsx, tests/react/changelog.test.tsx, tests/react/notifications.test.tsx, tests/react/admin.test.tsx, tests/react/tags.test.tsx, tests/react/merge.test.tsx, tests/integration/headless-backend.test.mjs, scripts/test-headless-backend.mjs</files>
  <read_first>src/react/provider.tsx, src/react/bindings.ts, src/react/hooks/mutations.ts, src/react/hooks/feedback.ts, src/react/hooks/roadmap.ts, src/react/hooks/changelog.ts, src/react/hooks/notifications.ts, src/react/hooks/admin.ts, node_modules/convex/src/react/client.ts, node_modules/convex/src/react/use_paginated_query.ts</read_first>
  <behavior>
    - Mount real hook consumers for `useFeedbackFeed`, `useFeedbackSearch`, `useSimilarPosts`, `usePost`, all three `useRoadmap` groups, `useChangelogFeed`, `useChangelogEntry`, `usePostSubscription`, `useNotifications`, `useUnreadNotificationCount`, `useAdminCapability`, `usePostModeration` activity, and `useTags`; each initial and post-success refetch failure renders its typed error state and never reaches the sentinel error boundary.
    - A second-or-later page failure keeps the exact prior ordered items visible, disables duplicate loads, reports the typed error, and recovers on a subsequent successful watch update. Reactive first/middle/last-page growth, shrink, `SplitRecommended`, and `SplitRequired` preserve exact backend order with no missing or repeated item.
    - Mounted switches cover authenticated A to unauthenticated to A, A to B, rapid A to B to A, and loading/unauthenticated to authenticated. The first render under the new generation contains no old pages, capability, inbox, unread count, subscription, pending/error/retry state, or optimistic overlay.
    - Resolve old-generation mutation, retry timer, optimistic vote/subscription/read projection, direct-query callback, page callback, and split callback after a switch; none may mutate the current generation. Admin capability and inbox must subscribe anew and resolve only for the new identity.
    - A disposable anonymous Convex deployment drives real `ConvexReactClient.watchQuery` direct and paginated probes through initial failure, success, refetch failure, second-page failure, recovery, growth, shrink, and page split. Harness/setup failures are reported separately from contract assertion failures.
  </behavior>
  <action>Add the smallest dev-only DOM renderer needed by Vitest and create a mounted test configuration; server rendering and mapper invocation do not count as mounted coverage. Build one controllable fake watch client with real `onUpdate`/`localQueryResult` timing and throwing semantics, render public hook consumers under a sentinel boundary, and assert the complete behavior matrix above. Remove or replace every existing test that fabricates an unreachable helper `status: "Error"` or passes `Error` directly to a mapper while the production hook would have thrown before mapping. Add a real-backend script following the existing disposable anonymous Convex harness pattern: generate narrow fault-toggle and paginated probe functions, connect with `ConvexReactClient`, exercise the same store-level states over WebSocket watches, close the client, and always tear down the deployment. Add a Node integration assertion that the script is part of `test:backend:phase2` and that `test:phase2` runs both mounted and real-backend gates. This task must remain red against the current `useQuery`, `convex-helpers/usePaginatedQuery`, optional generation fallback, effect-only clearing, and unfenced promises.</action>
  <acceptance_criteria>The red suite proves actual render/subscription behavior, distinguishes thrown render failures from typed hook errors, and fails independently for query transport, pagination retention/split correctness, synchronous identity clearing, and stale completion fencing.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run --config vitest.react.config.ts tests/react/live-headless.test.tsx tests/react/provider.test.tsx tests/react/mutations.test.tsx tests/react/feedback.test.tsx tests/react/feedback-search.test.tsx tests/react/roadmap.test.tsx tests/react/changelog.test.tsx tests/react/notifications.test.tsx tests/react/admin.test.tsx tests/react/tags.test.tsx tests/react/merge.test.tsx && node --test tests/integration/headless-backend.test.mjs && node scripts/test-headless-backend.mjs</automated></verify>
  <done>Both verifier gaps have executable red acceptance oracles that cannot pass through synthetic mapper states or source inspection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace throwing Convex read hooks with direct and ordered page watch stores</name>
  <files>src/react/provider.tsx, src/react/bindings.ts, src/react/query.ts, src/react/index.ts, src/react/hooks/feedback.ts, src/react/hooks/roadmap.ts, src/react/hooks/changelog.ts, src/react/hooks/notifications.ts, src/react/hooks/admin.ts, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs</files>
  <read_first>tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, src/react/provider.tsx, src/react/bindings.ts, node_modules/convex/src/react/client.ts, node_modules/convex/src/react/use_paginated_query.ts, node_modules/convex/src/server/pagination.ts</read_first>
  <action>Add a required provider `client` capability typed to the public `watchQuery` surface and implement stable external stores in `src/react/query.ts`, consumed only through `useSyncExternalStore`. A direct store subscribes before reading, catches every `localQueryResult()` throw on its initial read and every update, maps it with `mapAfferentError`, and publishes referentially stable `loading | ready | error` snapshots with a server-side loading snapshot. Do not call `useQuery`, do not use `convex-helpers/react` pagination, do not add Suspense, and do not add a library-owned React error boundary. Implement pagination as ordered page descriptors whose individual `watchQuery` args contain Convex `paginationOpts` with one per-session ID. Concatenate active successful pages in page order without ID deduplication. On a later-page error, retain all earlier successful pages and expose a typed error; keep the errored watch live so a successful update recovers. Guard `loadMore` once per terminal cursor and ignore stale page callbacks. When a page requests a split, subscribe to `(cursor, splitCursor]` and `(splitCursor, continueCursor]`, keep the original page visible, and atomically replace it only after both children succeed; `SplitRequired` never exposes an incomplete page. On an invalid cursor or explicit retry, start a fresh pagination session while retaining the last complete visible snapshot until the replacement first page succeeds. Reconcile reactive growth and shrink from complete page snapshots, then update every published domain hook to map these non-throwing snapshots into its existing closed result union, including unsupported/auth-loading states. Keep the query-store builders internal to the React subpath and preserve the domain-only public hook inventory.</action>
  <acceptance_criteria>`src/react/hooks` contains no `useQuery` or `usePaginatedQuery` read path; every watch throw becomes a typed snapshot, later-page failure retains prior results, split swap is atomic, and error-to-success recovery occurs without remounting.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run --config vitest.react.config.ts tests/react/live-headless.test.tsx tests/react/feedback.test.tsx tests/react/feedback-search.test.tsx tests/react/roadmap.test.tsx tests/react/changelog.test.tsx tests/react/notifications.test.tsx tests/react/admin.test.tsx tests/react/tags.test.tsx tests/react/merge.test.tsx && node scripts/test-headless-backend.mjs && npm run typecheck && npm run lint</automated></verify>
  <done>Every direct and paginated headless read is non-throwing under initial and reactive backend failures and remains correctly reactive across page changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make identity generations synchronous and fence every actor-scoped completion</name>
  <files>src/react/provider.tsx, src/react/bindings.ts, src/react/query.ts, src/react/hooks/mutations.ts, src/react/hooks/feedback.ts, src/react/hooks/roadmap.ts, src/react/hooks/changelog.ts, src/react/hooks/notifications.ts, src/react/hooks/admin.ts, tests/react/live-headless.test.tsx, tests/react/provider.test.tsx, tests/react/mutations.test.tsx, fixtures/packed-vite-convex/convex/afferent.ts, fixtures/packed-vite-convex/src/App.tsx, fixtures/packed-vite-convex/src/App.test.tsx, scripts/test-packed-consumer.mjs, tests/integration/packed-artifact.test.mjs, tests/static/contracts.test.ts, tests/static/exports.test.ts, package.json</files>
  <read_first>tests/react/live-headless.test.tsx, src/react/provider.tsx, src/react/query.ts, src/react/hooks/mutations.ts, src/react/hooks/feedback.ts, src/react/hooks/notifications.ts, src/react/hooks/admin.ts, fixtures/packed-vite-convex/src/App.tsx, scripts/test-packed-consumer.mjs</read_first>
  <action>Amend `AfferentAuthState` so authenticated state requires a non-empty opaque `identityToken` supplied by the host, stable for consecutive renders of one identity and distinct for different identities. Treat the token only as browser-local cache identity, never as authorization, and never serialize the token itself. In the provider compare `loading | unauthenticated | authenticated:<identityToken>` synchronously during render and monotonically advance a local generation on every transition; this makes logout/login and A-to-B-to-A distinct even when A's token returns. Delete `authenticated:default`; make the derived generation required on every actor-sensitive or paginated binding that needs a distinct Convex cache key, and make host wrappers validate then explicitly strip/ignore that cache discriminator before calling component operations. It conveys no actor, scope, or admin authority. Key every query/page store, visible pending/error/retry map, in-flight duplicate guard, and optimistic projection by generation. A newly selected generation starts cold, so admin capability, inbox, unread count, subscription, pages, and actor-sensitive public projections re-resolve before display. Capture the generation at every mutation, retry-delay, page/direct subscription, split, and optimistic-update start; compare it before all success/error/finally writes, retry registration/invocation, load-more/split work, and current-generation cache projection. Preserve Convex-native optimism only for vote, subscription, and mark-read, but have each updater match only query args carrying its captured generation so an old overlay is invisible immediately after a switch and its eventual settlement cannot touch the current snapshot. Refactor bespoke admin/changelog/notification controllers onto the same fence rather than leaving local unfenced variants. Update the packed host wrappers, consumer provider, and tests to accept the generation cache discriminator, pass the actual `ConvexReactClient` and an identity token, assert the declaration/runtime contract from the tarball, run the complete external install, and re-run the Phase 2 verifier. No first-render clearing may depend on `useEffect`.</action>
  <acceptance_criteria>There is no optional authenticated identity or fallback generation; every A/unauthenticated/B transition is synchronous, all old callbacks and retry/optimistic settlements are inert for the current generation, and the packed public provider contract works from a clean tarball.</acceptance_criteria>
  <verify><automated>npm run test:react && node scripts/test-headless-backend.mjs && npm run test:static && npm run typecheck && npm run lint && npm run build && npm run test:package && npm run test:phase2</automated></verify>
  <done>Identity changes cannot leak actor-owned UI state or accept stale async work, and the verified packed headless API enforces the amended adapter contract.</done>
</task>

</tasks>

## Source Coverage Audit

| Source                | Coverage in this plan                                                                                                                                                                                                                             |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Phase goal            | Preserved verbatim; this additive plan changes only the headless correctness seam.                                                                                                                                                                |
| Requirements          | UI-03 is the outstanding hook-contract requirement; QUAL-01 requires mounted, real-backend, and packed evidence.                                                                                                                                  |
| Context decisions     | D-43 and D-52 are amended by the live identity-token decision; D-46 through D-51 remain locked and become executable rather than synthetic.                                                                                                       |
| Research and patterns | The provider-neutral host boundary, closed DTOs, bounded pagination, external-store subscription lifecycle, and packed-consumer rules remain in force. The live decision supersedes the earlier optional session-generation/remount escape hatch. |
| Verification          | Both actionable gaps from `02-VERIFICATION.md` are closed together; all previously verified backend and UI slices remain untouched except for regression coverage.                                                                                |
| Prior plans           | 02-10 and 02-11 are dependencies, not rewrite targets. No completed merge work is reopened.                                                                                                                                                       |

## Artifacts this phase produces

- `src/react/query.ts`: internal direct and paginated watch-store builders with stable non-throwing snapshots, ordered page descriptors, atomic split replacement, and explicit disposal.
- `AfferentProvider` public contract: required watch client and required opaque authenticated `identityToken`; synchronous local `generation` replaces the fallback identity and is used only as a validated, ignored query-cache discriminator where isolation requires it.
- Generation-aware mutation controller shared by participation, moderation, tags, merge, changelog editor, and notifications, including retry-delay and finally-path fences.
- Mounted every-hook acceptance matrix using real React subscription/effect timing and a controllable `watchQuery` client.
- Disposable real Convex query/page fault harness proving initial/refetch/later-page error, recovery, reactive growth/shrink, and split behavior.
- Updated clean packed Vite/Convex consumer proving the changed provider declarations, runtime client injection, identity token, and domain-hook inventory from the tarball.

<threat_model>
## Trust Boundaries

| Boundary                                    | Description                                                                                                |
| ------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| Convex watch -> React render                | A server query can fail initially or after successful data and must not throw through the public hook.     |
| Page watches -> accumulated feed            | Independently changing or failing pages must retain exact ordered truth without gaps or duplicates.        |
| Host auth adapter -> local generation       | The host identifies identity changes; the token is cache identity only and conveys no authority.           |
| Old async work -> current identity          | Mutation, retry, optimistic, direct-query, and page completions can arrive after logout or account switch. |
| Packed consumer -> published React artifact | The new client/token contract must resolve and run without source-tree imports.                            |

## STRIDE Threat Register

| Threat ID       | Category                           | Component                                | Severity | Disposition | Mitigation Plan                                                                                                                                                                  |
| --------------- | ---------------------------------- | ---------------------------------------- | -------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-02-46         | Denial of Service                  | direct query render                      | high     | mitigate    | Catch every `localQueryResult` throw inside a stable external store and publish a closed typed error; mounted boundary sentinel plus real refetch oracle.                        |
| T-02-47         | Tampering / Denial of Service      | page accumulation and split              | high     | mitigate    | Ordered cursor descriptors, one-load guards, retained successful prefix, atomic two-child split swap, fresh-session invalid-cursor recovery, and real growth/shrink/split tests. |
| T-02-48         | Information Disclosure             | identity-token transition                | critical | mitigate    | Required non-empty distinct token, synchronous monotonic generation, no fallback identity, cold first snapshot, and A/B/A mounted matrix.                                        |
| T-02-49         | Information Disclosure / Tampering | stale async and retry completion         | critical | mitigate    | Capture generation at start and fence every success/error/finally write, delayed retry, callback, and split/load action.                                                         |
| T-02-50         | Information Disclosure             | optimistic vote/subscription/read state  | critical | mitigate    | Generation-owned native optimistic projection; new generation never consumes the old local result and stale settlement is inert.                                                 |
| T-02-51         | Spoofing / Elevation of Privilege  | host identity token and query generation | high     | mitigate    | Never serialize the opaque token; validate and strip/ignore the derived cache discriminator, and continue deriving actor/admin authority from `ctx.auth` on every server call.   |
| T-02-52         | Supply Chain                       | mounted test runtime and packed artifact | medium   | mitigate    | Dev-only pinned renderer, lockfile review, no new runtime dependency, clean tarball install, publint, attw, typecheck, and build gates.                                          |
</threat_model>

<verification>
Run the red-first mounted acceptance matrix, the disposable real Convex watch matrix, all existing React/static/backend regressions, typecheck, lint, build, and the clean packed-consumer gate. Then re-run Phase 2 verification and require 39/39 must-haves with UI-03 satisfied. Static source probes, server rendering, fabricated mapper errors, and an internal error boundary do not satisfy this plan.
</verification>

<success_criteria>
- Every published direct or paginated query hook renders typed initial and refetch failures without throwing, and every error can recover on a live update.
- Later-page failure preserves prior ordered pages; reactive growth, shrink, load-more, and split replacement have no gaps or duplicates.
- Authenticated state cannot omit identity, no `authenticated:default` exists, and logout/login plus A-to-B-to-A advance generation synchronously.
- The first render for a new generation is clear of old actor data, pages, pending/errors/retries, admin/inbox results, and optimistic overlays.
- Old mutation, retry, optimistic, direct-query, page, and split completions cannot alter the current generation.
- Mounted React, real Convex, complete Phase 2, and clean packed-tarball gates pass; re-verification reports 39/39.
</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-12-SUMMARY.md` when done.
</output>
