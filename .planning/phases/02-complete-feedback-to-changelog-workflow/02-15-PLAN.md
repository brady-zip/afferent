---
phase: 02-complete-feedback-to-changelog-workflow
plan: "15"
type: execute
wave: 15
depends_on: ["02-14"]
files_modified:
  - src/react/bindings.ts
  - src/react/hooks/feedback.ts
  - src/react/index.ts
  - fixtures/packed-vite-convex/convex/afferent.ts
  - fixtures/packed-vite-convex/src/App.tsx
  - tests/react/feedback.test.tsx
  - tests/react/live-headless.test.tsx
  - scripts/test-headless-backend.mjs
  - tests/integration/headless-backend.test.mjs
  - tests/static/contracts.test.ts
  - tests/static/exports.test.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03, QUAL-01]
must_haves:
  truths:
    - "A React consumer can read a post's comments through one optional public binding and a closed useComments state without calling Convex or the server client directly."
    - "Comment reads publish bounded creation-ordered flat CommentDto rows, preserve parentCommentId for replies, and never expose raw documents, recursive trees, or N+1 UI queries."
    - "Every comment-feed notification is one exact coherent ordered cursor-window prefix from the existing generation-fenced, endCursor-pinned, all-descriptor atomic pagination substrate."
    - "Omitting public.listComments yields an explicit unsupported state, while configured query failures retain the exact coherent prefix and recover without throwing during render."
  artifacts:
    - path: src/react/bindings.ts
      provides: "Narrow CommentFeedQueryReference and optional PublicBindings.listComments capability"
    - path: src/react/hooks/feedback.ts
      provides: "Closed CommentFeedState mapping and useComments domain hook"
    - path: tests/react/live-headless.test.tsx
      provides: "Mounted exact-publication, identity, loading, and recovery oracle"
    - path: scripts/test-headless-backend.mjs
      provides: "Disposable real Convex comment-feed coherence oracle"
  key_links:
    - from: src/react/hooks/feedback.ts
      to: src/react/query.ts
      via: "useComments delegates to usePaginatedWatchQuery instead of owning a parallel cache"
      pattern: "usePaginatedWatchQuery"
    - from: src/react/hooks/feedback.ts
      to: src/react/bindings.ts
      via: "optional CommentFeedQueryReference selects configured versus unsupported behavior"
      pattern: "CommentFeedQueryReference|listComments"
    - from: fixtures/packed-vite-convex/convex/afferent.ts
      to: src/client/internal.ts
      via: "trusted host query validates and strips sessionGeneration before client.read.listComments"
      pattern: "listComments|withoutSessionGeneration"
    - from: tests/react/live-headless.test.tsx
      to: src/react/hooks/feedback.ts
      via: "mounted useComments recorder asserts every exact ordered publication"
      pattern: "useComments"
---

<objective>
Close the missing React comment-read seam before copied discussion UI is allowed to execute.

Purpose: Complete the Phase 2 headless public/participation boundary so Phase 3 renders discussion through the same closed, non-throwing, generation-isolated API as every other public workflow.
Output: An optional comment query binding, a stable `useComments` hook over the existing atomic paginated watch store, packed-host wiring, and mounted plus disposable-real-backend coherence evidence.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- ranked, searchable, and filterable discovery with admin duplicate merges; authenticated participation; admin moderation with a status-driven roadmap; manually published changelog entries linked to feedback; and in-app notifications backed by a host-consumable delivery outbox -- through tested component APIs and framework-light headless React hooks with explicit async, auth, and error states, **so that** my users and admins can complete the entire feedback lifecycle while I retain ownership of identity, authorization, and my own presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-DISCUSSION-LOG.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-PATTERNS.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-12-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-13-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-14-SUMMARY.md
@src/client/contracts.ts
@src/client/internal.ts
@src/component/public/comments.ts
@src/component/model/comments.ts
@src/react/bindings.ts
@src/react/query.ts
@src/react/hooks/feedback.ts
@tests/react/live-headless.test.tsx
@scripts/test-headless-backend.mjs
@.planning/phases/03-source-owned-product-interface/03-02-PLAN.md

<interfaces>
Reuse the existing `CommentDto` and `CommentPageDto` contracts unchanged: `CommentDto` is `contractVersion: 1`, contains string `id`, `postId`, safe `body`, closed `author`, and optional `parentCommentId`, and has no recursive `replies` or raw component fields. `client.read.listComments` already supplies bounded indexed pagination, and `usePaginatedWatchQuery` already supplies non-throwing `watchQuery`/`useSyncExternalStore`, generation fencing, end-cursor-pinned descriptor windows, last-coherent retention, and every-notification all-descriptor atomic publication. Add no second page cache or alternate query path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the missing comment-read contract red-first</name>
  <files>tests/react/feedback.test.tsx, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs, tests/static/contracts.test.ts, tests/static/exports.test.ts</files>
  <read_first>src/client/contracts.ts, src/client/internal.ts, src/component/public/comments.ts, src/react/bindings.ts, src/react/query.ts, src/react/hooks/feedback.ts, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs</read_first>
  <behavior>
    - Omitting `bindings.public.listComments` returns `status: "unsupported"`, an empty flat item list, and inert pagination without starting a watch or throwing.
    - A configured feed maps initial loading, empty, ready, loading-more, exhausted, and typed error states while retaining only the maximal coherent creation-ordered prefix on a fault.
    - Each row is the exact closed/versioned `CommentDto`; roots omit `parentCommentId`, replies carry their root ID, and neither raw documents nor nested reply arrays cross the hook boundary.
    - Mounted insert, reply, middle delete, pending-load mutation, page failure/recovery, and A-to-B identity change record every notification and allow only the exact prior or current canonical loaded prefix for that generation.
    - The disposable real Convex oracle asserts exact arrays, order, prefix length, descriptor boundaries, and exact-once disposal; Set uniqueness, eventual-only assertions, selected `waitFor` snapshots, and N+1 per-comment reads cannot satisfy it.
  </behavior>
  <action>Write failing unit, mounted, integration-source, export, and disposable-real-backend tests before implementation. Extend the disposable backend with a post-scoped comment-like table/query and test-only scoped insert, reply, and delete mutations so the real `ConvexReactClient.watchQuery` path can exercise flat rows and parent IDs without adding a product mutation. Reuse the lifetime publication recorder and canonical cursor-window oracle established by Plans 02-13 and 02-14. Assert every notification for both listener orders and exact ordered prefixes across initial load, append, root/reply insertion, deletion, mutation while `loadMore` is incomplete, first/middle/tail fault and recovery, retry, logout/login, A-to-B-to-A, stale callbacks, unmount, and disposal. Add static failures for a missing public export/binding, recursive reply shape, direct React access to `client.read.listComments`, generic CRUD, and a new comment cache.</action>
  <verify><automated>npm exec -- vitest run --config vitest.react.config.ts tests/react/feedback.test.tsx tests/react/live-headless.test.tsx -t "comments|comment feed"; node --test tests/integration/headless-backend.test.mjs; node scripts/test-headless-backend.mjs; npm exec -- vitest run tests/static/contracts.test.ts tests/static/exports.test.ts</automated></verify>
  <done>The missing optional binding/hook and every required comment transition fail against exact closed DTO, prefix, temporal, identity, and disposal expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add useComments on the existing atomic pagination substrate</name>
  <files>src/react/bindings.ts, src/react/hooks/feedback.ts, src/react/index.ts, tests/react/feedback.test.tsx, tests/react/live-headless.test.tsx, tests/static/contracts.test.ts, tests/static/exports.test.ts</files>
  <read_first>tests/react/feedback.test.tsx, tests/react/live-headless.test.tsx, src/react/bindings.ts, src/react/hooks/feedback.ts, src/react/query.ts, src/react/index.ts, src/client/contracts.ts</read_first>
  <action>Add a narrow `CommentFeedQueryReference` whose public query arguments are `postId`, browser-local `sessionGeneration`, and required `paginationOpts`, returning the existing `CommentPageDto`; add it as optional `PublicBindings.listComments?`. Define and export a closed `CommentFeedState` with explicit unsupported/loading/empty/ready/error states, flat `CommentDto[]`, and the established `isLoadingMore`, `canLoadMore`, and `loadMore` semantics. Implement `mapCommentFeedState` and `useComments(postId)` beside the other feedback hooks. When the binding is absent, skip the query and map to unsupported; when present, pass `{ postId, sessionGeneration: generation }` into `usePaginatedWatchQuery<CommentDto, CommentFeedQueryReference>` with the bounded default page size. Delegate all subscription, retry/recovery, descriptor, cache, atomic-publication, and generation behavior to that existing common store. Do not modify `CommentDto`, materialize a tree, issue author/reply lookups from React, call `client.read.listComments` directly, introduce a comment-specific cache, trust the generation as authority, or let the hook navigate/toast.</action>
  <verify><automated>npm exec -- vitest run --config vitest.react.config.ts tests/react/feedback.test.tsx tests/react/live-headless.test.tsx -t "comments|comment feed"; npm exec -- vitest run tests/static/contracts.test.ts tests/static/exports.test.ts; npm run typecheck; npm run lint</automated></verify>
  <done>Consumers receive one stable optional `useComments` capability whose flat comment pages inherit every non-throwing, cursor-window, atomic-publication, and identity guarantee from Plans 02-12 through 02-14.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove packed host wiring and real comment coherence</name>
  <files>fixtures/packed-vite-convex/convex/afferent.ts, fixtures/packed-vite-convex/src/App.tsx, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs</files>
  <read_first>fixtures/packed-vite-convex/convex/afferent.ts, fixtures/packed-vite-convex/src/App.tsx, src/client/internal.ts, src/component/public/comments.ts, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, package.json</read_first>
  <behavior>
    - The packed fixture exposes a trusted host `listComments` query with validated numeric cache generation, strips that discriminator, and calls the already-scoped `client.read.listComments`; actor, admin, user, and scope facts never come from browser arguments.
    - The packed React consumer binds the optional query and invokes `useComments` through `AfferentProvider`, proving declarations and runtime exports from the installed tarball rather than repository-relative imports.
    - Real insert, reply, delete, mutation-during-load-more, error/recovery, and identity-switch scenarios produce only exact ordered canonical prefixes at every notification and settle on the exact backend window.
    - Existing feedback, roadmap, changelog, notification, admin, split/collapse, and packed-consumer guarantees remain green.
  </behavior>
  <action>Add the fixture host query by composing the existing list-comments intent fields, required pagination options, and validated cache-generation field, then call `client.read.listComments` only after `withoutSessionGeneration`. Bind it under `public.listComments` and render enough `useComments` state in the packed fixture to prove the public type/runtime surface. Finish the mounted and disposable-real matrices from Task 1, including exact root/reply `parentCommentId` rows and bounded indexed flat pagination. Keep the test-only delete capability inside the disposable harness; do not add public deletion, schema, DTO, tree, or generic CRUD APIs. Run the full React, integration, real backend, static, type, lint, build, clean packed-tarball consumer, and Phase 2 gates.</action>
  <verify><automated>npm run test:react && node --test tests/integration/headless-backend.test.mjs && node scripts/test-headless-backend.mjs && npm run test:static && npm run typecheck && npm run lint && npm run build && npm run test:package && npm run test:phase2</automated></verify>
  <done>The packed supported boundary and real Convex client prove coherent comment reads for every required transition without widening backend authority or bypassing the headless layer.</done>
</task>

</tasks>

## Source Coverage Audit

| Source       | ID / constraint                                                  | Plan coverage                                                                                                                                        | Status  |
| ------------ | ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| GOAL         | Complete provider-neutral lifecycle through tested headless APIs | Adds only the missing React read seam required to display the already-supported comment workflow.                                                    | COVERED |
| REQ          | UI-01, UI-02                                                     | Trusted host references remain injected and provider-neutral; copied UI receives no identity, scope, admin, or raw backend records.                  | COVERED |
| REQ          | UI-03                                                            | `useComments` supplies explicit unsupported/loading/pagination/empty/error states over the common non-throwing watch substrate.                      | COVERED |
| REQ          | QUAL-01                                                          | Unit, mounted, static, disposable-real-backend, packed-consumer, and full-phase gates prove the contract.                                            | COVERED |
| CONTEXT      | D-43, D-46, D-47, D-50, D-51, D-52                               | Optional capability behavior, stable hooks, reactive pagination, no thrown render errors, no navigation/toasts, and generation clearing stay locked. | COVERED |
| PRIOR PLANS  | 02-12, 02-13, 02-14                                              | Reuses non-throwing external stores, pinned cursor windows, structural fencing, and all-descriptor atomic publication unchanged.                     | COVERED |
| CURRENT CODE | `client.read.listComments`, `CommentDto`, `addComment`           | Existing bounded read, closed flat DTO, and write capability are wired rather than reimplemented.                                                    | COVERED |
| PHASE 3      | 03-02 discussion surface                                         | The UI plan depends on this plan and consumes `useComments`; direct component/client reads remain forbidden.                                         | COVERED |

No completed Phase 2 plan is replaced or rewritten. This is a single additive headless-contract correction discovered while preparing Phase 3 Plan 03-02; every prior requirement and decision remains owned by Plans 02-01 through 02-14 and stays in the regression gate.

## Artifacts this phase produces

- Optional `CommentFeedQueryReference`/`PublicBindings.listComments` and exported `CommentFeedState`/`useComments`.
- Stable flat versioned comment rows with root/reply linkage through existing `parentCommentId`.
- Mounted and disposable-real-Convex every-notification oracles for comment insertion, reply, deletion, loading, fault recovery, and identity transitions.
- Packed fixture proof that the trusted host strips cache identity and the React consumer stays on injected references.
- Full Phase 2 regression evidence without product schema, generic CRUD, recursive DTO, or UI-owned cache expansion.

<threat_model>

## Trust Boundaries

| Boundary                                       | Description                                                                                          |
| ---------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| Browser query args -> trusted host wrapper     | `sessionGeneration` is cache identity only; host auth still derives viewer and scope on every call.  |
| Component comment documents -> public DTO      | Only closed v1 flat rows and optional root-parent IDs cross the component boundary.                  |
| Convex page transitions -> React comment state | One notification must reflect one complete all-descriptor local-client transition.                   |
| Account identity -> mounted feed               | Old descriptors, results, errors, and callbacks must become invisible synchronously.                 |
| Copied discussion UI -> headless package       | UI consumes `useComments`; it cannot query the component/server client or build a second cache/tree. |

## STRIDE Threat Register

| Threat ID | Category                           | Component                    | Severity | Disposition | Mitigation Plan                                                                                                           |
| --------- | ---------------------------------- | ---------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------- |
| T-02-67   | Spoofing / Elevation of Privilege  | comment host query           | critical | mitigate    | Validate then strip generation; derive viewer and scope only inside the trusted host/client boundary.                     |
| T-02-68   | Information Disclosure             | comment query omission/error | high     | mitigate    | Return explicit unsupported/typed error state and never fall through to direct client access or raw thrown render errors. |
| T-02-69   | Tampering / Information Disclosure | flat comment DTO             | high     | mitigate    | Reuse the closed validator/versioned DTO; reject raw documents, nested replies, and per-row follow-up queries.            |
| T-02-70   | Tampering                          | multi-page comment snapshot  | high     | mitigate    | Reuse endCursor-pinned all-descriptor staging and require exact prior/current prefixes at every notification.             |
| T-02-71   | Information Disclosure             | identity replacement         | critical | mitigate    | Include local generation in query identity and fence store callbacks, structural work, errors, and disposal.              |
| T-02-72   | Repudiation                        | real-backend acceptance      | high     | mitigate    | Record every notification and compare exact ordered arrays, boundaries, parent IDs, prefixes, and exact-once disposal.    |

</threat_model>

<verification>
Run red-first state/export/mounted tests, implement only the optional binding and hook over the existing common store, prove the trusted packed wrapper, then execute disposable-real-Convex every-notification scenarios and the complete React/static/type/lint/build/package/Phase 2 suite. Exact ordered prefixes at every tick, closed DTO assertions, bounded indexed access, and generation/disposal evidence are mandatory; eventual convergence, Set uniqueness, nested trees, N+1 reads, direct UI access, or repository-relative fixture imports cannot satisfy the gate.
</verification>

<success_criteria>

- `public.listComments` is optional and omission maps to a stable unsupported `useComments` state without opening a watch.
- Configured comment reads return only bounded creation-ordered flat `CommentDto` rows, including optional root `parentCommentId`, through injected host references.
- `useComments` owns no query machinery beyond mapping the existing non-throwing, generation-fenced, endCursor-pinned, all-descriptor atomic pagination snapshot.
- Mounted and real-backend recorders prove exact ordered prefixes on every insert, reply, delete, pending-load, fault/recovery, and identity-switch notification.
- The packed consumer and all prior Phase 2 gates pass, and Phase 3 Plan 03-02 cannot execute before this prerequisite completes.

</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-15-SUMMARY.md` when done.
</output>
