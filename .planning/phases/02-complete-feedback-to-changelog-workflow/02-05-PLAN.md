---
phase: 02-complete-feedback-to-changelog-workflow
plan: "05"
type: execute
wave: 8
depends_on: ["02-08"]
files_modified:
  - src/component/_generated/api.ts
  - src/component/_generated/component.ts
  - src/component/_generated/dataModel.ts
  - src/component/_generated/server.ts
  - src/component/schema.ts
  - src/component/validators.ts
  - src/component/model/merge.ts
  - src/component/model/scope.ts
  - src/component/model/visibility.ts
  - src/component/model/views.ts
  - src/component/public/posts.ts
  - src/component/admin/merge.ts
  - src/component/jobs/merge.ts
  - src/component/feedback.ts
  - src/client/contracts.ts
  - src/client/internal.ts
  - src/react/hooks/feedback.ts
  - src/react/hooks/admin.ts
  - tests/model/merge.test.ts
  - tests/component/merge.test.ts
  - tests/integration/merge-backend.test.ts
  - tests/react/merge.test.tsx
  - scripts/test-merge-backend.mjs
autonomous: true
requirements: [DISC-07, DISC-08, UI-01, UI-02, UI-03, QUAL-01]
must_haves:
  truths:
    - "An admin can irreversibly merge a source into a live canonical post while preserving and deduplicating every related membership/history relation (D-07, D-08)."
    - "Direct lookup returns `post | merged | notFound`; visible tombstones point only to the final canonical ID and never appear in projections (D-09, D-10, D-11)."
    - "Large merges resume in bounded batches but readers never observe a half-merged state (D-12, D-56)."
  artifacts:
    - path: src/component/admin/merge.ts
      provides: "Guarded merge intent and atomic public cutover"
    - path: src/component/jobs/merge.ts
      provides: "Bounded resumable relation reparenting"
    - path: tests/integration/merge-backend.test.ts
      provides: "Crash-boundary/atomicity real-backend proof"
  key_links:
    - from: src/component/jobs/merge.ts
      to: src/component/admin/merge.ts
      via: "Prepared batches finish with one guarded counter/redirect cutover"
    - from: src/component/public/posts.ts
      to: src/component/model/visibility.ts
      via: "Redirect disclosure follows canonical visibility"
---

<objective>
Deliver deterministic admin duplicate merging with preserved history, durable redirect tombstones, and atomic public resolution.

Purpose: Close the duplicate lifecycle without data loss, counter drift, redirect oracles, or half-merged reads.
Output: Merge state machine, direct-result contract, host/hooks, and exhaustive model/component/real-backend tests.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- ranked, searchable, and filterable discovery with admin duplicate merges; authenticated participation; admin moderation with a status-driven roadmap; manually published changelog entries linked to feedback; and in-app notifications backed by a host-consumable delivery outbox -- through tested component APIs and framework-light headless React hooks with explicit async, auth, and error states, **so that** my users and admins can complete the entire feedback lifecycle while I retain ownership of identity, authorization, and my own presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-PATTERNS.md
@.planning/phases/01-secure-installable-feedback-board/01-04-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify merge preservation, redirect privacy, and crash boundaries</name>
  <files>tests/model/merge.test.ts, tests/component/merge.test.ts, tests/integration/merge-backend.test.ts, tests/react/merge.test.tsx, scripts/test-merge-backend.mjs</files>
  <read_first>src/component/model/votes.ts, src/component/public/posts.ts, src/component/model/activity.ts, src/component/model/visibility.ts, .planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md</read_first>
  <behavior>
    - Vote/subscription memberships union by actor and counters reconcile from truth; comments/activity/changelog links reparent; source snapshot is bounded/admin-only (D-07, D-08).
    - Redirects flatten, reject cycles/tombstone targets, disclose only requested/final IDs, and follow canonical visibility (D-09..D-11).
    - Every batch boundary can retry/resume without exposing prepared relations or a tombstone before final cutover (D-12).
  </behavior>
  <action>Create red pure-model, two-scope component, React direct-result, and disposable-backend crash/resume matrices. Exercise duplicate actor memberships, anonymized authors, relation counts above the inline threshold, chained target flattening, archived canonical targets, cross-scope IDs, repeated merge intent, and stored counter reconciliation.</action>
  <acceptance_criteria>Tests fail only for missing merge contracts and prove DISC-07/08 plus D-07..D-12 at each public and job boundary.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/model/merge.test.ts tests/component/merge.test.ts tests/react/merge.test.tsx</automated></verify>
  <done>The merge behavior is fully executable before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the guarded resumable merge state machine</name>
  <files>src/component/schema.ts, src/component/validators.ts, src/component/model/merge.ts, src/component/model/scope.ts, src/component/model/visibility.ts, src/component/model/views.ts, src/component/public/posts.ts, src/component/admin/merge.ts, src/component/jobs/merge.ts, src/component/feedback.ts</files>
  <read_first>tests/model/merge.test.ts, tests/component/merge.test.ts, tests/integration/merge-backend.test.ts, src/component/model/tags.ts</read_first>
  <action>Add scoped merge-history/job/redirect state and purpose-built guards. Validate distinct live source/canonical posts in one scope; retain a bounded source snapshot; inline only when every relation total is at most 50, otherwise create a durable pre-cutover job and process 50 scoped rows per batch. Reparent comments/activity/changelog links, union/deduplicate vote/subscription memberships, repoint tag/search/notification relations as applicable, and reconcile counters from canonical rows. Only the final bounded transaction activates prepared relations, flattens the final target, tombstones the source, updates projections, and appends merge activity. Extend `getPost` to versioned `post | merged | notFound` and apply canonical visibility/not-found equivalence everywhere (D-07..D-12, D-31, D-34, D-53, D-54, D-56). Run `npm run codegen:component` and include generated outputs; never hand-edit them.</action>
  <acceptance_criteria>Merge is irreversible, bounded, resumable, idempotently guarded, scope-safe, counter-correct, and atomically visible; tombstones never enter feed/search/roadmap/changelog lists.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/model/merge.test.ts tests/component/merge.test.ts tests/static/schema-scope.test.ts && node scripts/test-merge-backend.mjs</automated></verify>
  <done>The component preserves the complete duplicate history and exposes only coherent canonical state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose direct resolution and merge through host/headless APIs</name>
  <files>src/client/contracts.ts, src/client/internal.ts, src/react/hooks/feedback.ts, src/react/hooks/admin.ts, tests/react/merge.test.tsx</files>
  <read_first>src/component/public/posts.ts, src/component/admin/merge.ts, src/react/hooks/feedback.ts, src/react/hooks/admin.ts</read_first>
  <action>Add `PostLookupResult` and `MergePostResult` contracts, authorized host merge capability, `usePost` merged/notFound states, and `useMergePost`. A merged result never silently fetches/substitutes the canonical record; the caller explicitly fetches the final ID. Merge waits for server truth, keys pending/error by source/action, guards duplicate submit, returns typed conflict/not-found outcomes, and never navigates/toasts.</action>
  <acceptance_criteria>A React consumer can render a durable redirect state and an admin can initiate/observe merge completion through provider-neutral refs without client authority or optimistic relation mutation.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/react/merge.test.tsx && npm run typecheck && npm run lint</automated></verify>
  <done>The duplicate merge and direct-resolution slice is complete end to end.</done>
</task>

</tasks>

## Artifacts this phase produces

- Complete new-path inventory: `src/component/model/merge.ts`, `src/component/admin/merge.ts`, `src/component/jobs/merge.ts`, `tests/model/merge.test.ts`, `tests/component/merge.test.ts`, `tests/integration/merge-backend.test.ts`, `tests/react/merge.test.tsx`, `scripts/test-merge-backend.mjs`.
- `src/component/model/merge.ts`: merge validation, relation/counter reconciliation, redirect flattening.
- `src/component/admin/merge.ts`: `mergePost` and final cutover.
- `src/component/jobs/merge.ts`: `continueMerge`.
- Versioned `PostLookupResult`, `MergePostResult`, merge intent/result validators and host methods.
- `usePost` merged/notFound extensions and `useMergePost`.
- Named merge test paths and `scripts/test-merge-backend.mjs`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser merge intent -> admin wrapper | Source/target IDs are attacker-controlled and merge is irreversible. |
| Scheduled job -> public cutover | Retried prepared relation batches must remain private and coherent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-16 | Elevation / Tampering | merge intent | critical | mitigate | Fresh admin authorization, scope-normalized live endpoints, cycle/tombstone guards, irreversible explicit intent. |
| T-02-17 | Information Disclosure | redirects | high | mitigate | Canonical visibility check and identical not-found result for hidden/cross-scope identifiers. |
| T-02-18 | Tampering | relation/counter reconciliation | high | mitigate | Canonical membership union/dedupe and counter recomputation, never arithmetic addition. |
| T-02-19 | Denial of Service | large merge | high | mitigate | Durable guard, 50-row batches, transaction headroom, crash/resume tests, bounded final cutover. |
</threat_model>

<verification>
Run all merge suites, real-backend crash harness, schema/contracts audits, typecheck/lint, then public discovery regressions.
</verification>

<success_criteria>
- DISC-07/08 are complete without data loss or half-state.
- D-07..D-12 are proven under duplicates, retries, hidden targets, and two scopes.
- Direct consumers receive explicit redirect state rather than an oracle or silent identity swap.
</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-05-SUMMARY.md` when done.
</output>
