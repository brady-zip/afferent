---
phase: 02-complete-feedback-to-changelog-workflow
plan: "17"
type: execute
wave: 17
depends_on: ["02-16"]
files_modified:
  - tests/helpers/headless-publication-oracle.mjs
  - tests/integration/headless-backend.test.mjs
  - scripts/test-headless-backend.mjs
  - tests/react/live-headless.test.tsx
  - src/react/query.ts
autonomous: true
gap_closure: true
requirements: [DISC-07, ADMN-10, UI-03, QUAL-01]
must_haves:
  truths:
    - "Every non-error installed-product publication is deep-equal to the expected model's precise prior committed or current target full loaded window; a shorter prefix, subset, membership match, or eventual final state is a failure (D-12, D-47)."
    - "A first-, middle-, or tail-descriptor fault publishes the typed error and the exact maximal contiguous prefix for that same publication; either the wrong code or the wrong prefix fails (D-46, D-50, D-51)."
    - "Installed comment and activity watches expose an exact contiguous active descriptor chain with no overlap and one unbounded tail through 1-to-2 cutover, cleanup repoints, 2-to-1 completion, and append/restart (D-02, D-21, D-47)."
    - "Each scenario has an exact expected publication sequence with required count, order, status, content, and boundaries; cleanup emits at least one repoint-reflecting publication, and zero or fewer-than-modeled publications fail QUAL-01."
    - "A controllable deferred transport releases both a late old-generation result and a late old-generation error after A-to-B-to-A replacement; neither changes the current snapshot, descriptor chain, publication count, or error state (D-52)."
    - "The direct installed headless harness passes repeatedly under one await-next-with-deadline schedule and the complete Phase 2 gate passes; any failed run is a gate failure and is never erased by an immediate rerun."
  artifacts:
    - path: tests/helpers/headless-publication-oracle.mjs
      provides: "Pure expected temporal model, exact publication/boundary assertions, negative fixtures, and deferred watch transport controls"
      exports: ["createExpectedPublicationModel", "assertExactPublicationSequence", "assertExactDescriptorChain", "createDeferredWatchTransport", "awaitNextPublication"]
    - path: tests/integration/headless-backend.test.mjs
      provides: "Negative meta-tests proving each oracle rejection class and source wiring to installed readers"
    - path: scripts/test-headless-backend.mjs
      provides: "Deterministic installed listComments/listPostActivity lifecycle oracle after a real merge"
    - path: tests/react/live-headless.test.tsx
      provides: "Controlled late result/error generation-fence proof over the common paginated store"
  key_links:
    - from: scripts/test-headless-backend.mjs
      to: tests/helpers/headless-publication-oracle.mjs
      via: "Each merge-lifecycle operation supplies its exact expected publication model and awaits every required notification by deadline"
      pattern: "createExpectedPublicationModel|assertExactPublicationSequence|awaitNextPublication"
    - from: scripts/test-headless-backend.mjs
      to: src/component/public/comments.ts
      via: "The disposable host wrapper calls the installed component's real public comments listComments query after a real merge"
      pattern: "components.*afferent.*public.*comments.*listComments"
    - from: scripts/test-headless-backend.mjs
      to: src/component/admin/activity.ts
      via: "The same installed merge job drives the real admin listPostActivity query and its descending full-key windows"
      pattern: "components.*afferent.*admin.*activity.*listPostActivity"
    - from: tests/helpers/headless-publication-oracle.mjs
      to: tests/integration/headless-backend.test.mjs
      via: "Pure negative cases independently prove rejection of truncated, mismatched fault, invalid-boundary, vacuous, and stale-generation evidence"
      pattern: "assert\.throws|assert\.rejects"
    - from: tests/react/live-headless.test.tsx
      to: src/react/query.ts
      via: "A deferred watch transport delivers old-generation result and error callbacks only after A-to-B-to-A replacement"
      pattern: "createPaginatedWatchStore|late.*generation"
---

<objective>
Replace Plan 02-16's permissive installed-product prefix recorder with a deterministic, expected-model-driven temporal oracle that can reject every missing verifier case.

Purpose: Close the remaining 62/64 verification gap at its root cause: the product readers and lifecycle scenarios exist, but the execution/test oracle accepts evidence weaker than the written exact-publication contract.
Output: A pure rejection-capable oracle with negative meta-tests, exact installed comment/activity publication and active-boundary proof, deterministic stale-generation injection, repeated clean direct runs, and the complete Phase 2 gate.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- including lossless duplicate merges and exact reactive comment/activity windows -- through tested component APIs and framework-light headless React hooks, **so that** users and admins never observe truncated, overlapping, torn, or prior-identity history while I retain ownership of identity, authorization, and presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-PATTERNS.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-DISCUSSION-LOG.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-VERIFICATION.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-16-PLAN.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-16-SUMMARY.md
@scripts/test-headless-backend.mjs
@tests/integration/headless-backend.test.mjs
@tests/react/live-headless.test.tsx
@src/react/query.ts

<interfaces>
From `scripts/test-headless-backend.mjs`:
- `TrackingWatchClient.watchQuery(reference, args)` records function name, full pagination arguments, active listener count, disposal count, last result, and last error.
- `assertActiveBoundaries(tracker)` already proves adjacency and one unbounded tail, but currently inspects every active paginated watch together rather than one product reader/store generation.
- `recordEveryProductPublication(store).assertPrefixesSince(...)` currently expands every endpoint into all lengths `0..N`; this is the verifier-rejected seam and must be removed from product acceptance.
- `settleProductStore(...)` polls final equality and conditionally calls `loadMore`; it is useful only as a final truth helper and cannot remain the temporal scheduler or acceptance oracle.
- `productTruth(...)` returns exact full installed comment/activity identities after the real component's `mergeReadPostIds` selection.

From `src/react/query.ts`:
- `createPaginatedWatchStore({ client, query, args, generation, initialNumItems })` owns one generation, one descriptor chain, all-descriptor synchronous rereads, typed maximal-prefix errors, structural append/split/collapse replacement, and `dispose()` fencing.
- Each attached watch receives `{ ...args, paginationOpts: { numItems, cursor, id, endCursor? } }`; a non-tail descriptor is bounded by `endCursor`, and exactly one tail omits it.
- `dirty(page)` ignores callbacks when the descriptor epoch no longer matches the current store epoch; hook identity replacement creates and disposes whole stores through the generation-keyed memo boundary.

From `tests/react/live-headless.test.tsx`:
- `ControlledWatchClient` can assign values/errors and invoke listeners deterministically, while `updateTransaction` installs all sibling values before firing listeners in a chosen order.
- Existing generation tests deliver stale values only after listeners are removed; extend this seam so a captured callback can be released after disposal with either a queued result or queued error.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a rejection-capable expected temporal oracle red-first</name>
  <files>tests/helpers/headless-publication-oracle.mjs, tests/integration/headless-backend.test.mjs</files>
  <read_first>.planning/phases/02-complete-feedback-to-changelog-workflow/02-VERIFICATION.md, .planning/phases/02-complete-feedback-to-changelog-workflow/02-16-PLAN.md, .planning/phases/02-complete-feedback-to-changelog-workflow/02-16-SUMMARY.md, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs, src/react/query.ts</read_first>
  <behavior>
    - A non-error publication passes only when its complete `{status, ids, boundaries}` value deep-equals the exact expected publication at that sequence index; arbitrary shorter prefixes of a prior/current truth fail.
    - A fault publication passes only when one object contains both the expected closed error code and the exact maximal contiguous prefix ending before the failed first/middle/tail descriptor. Correct code with wrong prefix and wrong code with correct prefix both fail.
    - Descriptor validation requires one chain beginning at `cursor: null`, identical adjacent end/start cursors, no gap, no overlap, no duplicate start, and exactly one final descriptor without `endCursor`.
    - Sequence validation requires exact publication count and ordered content. Zero publications, a missing cleanup repoint publication, or an extra late publication fails.
    - Deferred transport validation identifies the originating generation and records whether a late result or late error was rejected rather than merely disposed.
  </behavior>
  <action>Create `tests/helpers/headless-publication-oracle.mjs` as a pure Node assertion/model module. Model each scenario as an ordered list of exact publication objects containing reader, generation, status, full IDs, exact active descriptor boundaries, and optional error code; normal steps may name precise prior/current full windows, but the model must choose the complete expected value for every sequence slot instead of accepting a set of all prefixes. Implement `awaitNextPublication(recorder, cursor, deadline, label)` as an event-driven promise that resolves only for the next sequence entry and rejects at an explicit deadline; do not sample with timers or poll final state. Add reader/store-scoped boundary extraction so simultaneous comment and activity watches cannot be combined accidentally. Implement the deferred watch transport as a wrapper that can freeze one old-generation result callback or error callback, allow the store to be replaced A-to-B-to-A, and release the captured delivery deterministically. Write negative meta-tests first and require the oracle to reject all eight verifier classes independently: shorter non-error publication; correct code with wrong prefix; wrong code with correct prefix; overlapping boundaries; gapped boundaries; zero publications; accepted stale result; accepted stale error. Also test an exact positive first/middle/tail fault model so rejection is not caused by an unusable oracle. Do not import or execute the disposable backend from this helper.</action>
  <acceptance_criteria>The pure meta-test suite proves by explicit failing fixtures that no arbitrary prefix, separately checked fault, malformed chain, vacuous recorder, or accepted old-generation delivery can satisfy the oracle; exact modeled sequences and exact coupled fault publications pass.</acceptance_criteria>
  <verify><automated>node --test tests/integration/headless-backend.test.mjs</automated></verify>
  <done>The old `exactPrefixes`/`assertPrefixesSince` acceptance model has a tested replacement whose own negative cases demonstrate that it can falsify every remaining verifier concern.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive exact installed-reader publications, boundaries, and stale deliveries</name>
  <files>scripts/test-headless-backend.mjs, tests/helpers/headless-publication-oracle.mjs, tests/integration/headless-backend.test.mjs, tests/react/live-headless.test.tsx</files>
  <read_first>tests/helpers/headless-publication-oracle.mjs, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs, tests/react/live-headless.test.tsx, src/react/query.ts, src/component/public/comments.ts, src/component/admin/activity.ts</read_first>
  <behavior>
    - After one real staged merge, both installed `listComments` and `listPostActivity` stores follow exact expected sequences through initial 1-stream load, 1-to-2 cutover, first/middle/tail faults and recovery, insert/delete, incomplete-tail append, every cleaning repoint, 2-to-1 completion, and append/restart.
    - Every notification for each reader is checked immediately against its sequence slot and exact full loaded window; a later correct snapshot cannot repair an earlier truncated, extra, overlapped, or missing publication.
    - Every sequence slot also inspects only that store's active descriptors and requires exact model equality, contiguity, no overlap, and one unbounded tail. Cutover/completion restart models explicitly replace rather than concatenate the old chain.
    - At least one cleaning step must change the modeled physical row identity or descriptor state and emit the corresponding exact repoint publication for comments and activity; cleanup with no publication fails.
    - An old A store has one real result and one real typed fault held behind the deferred transport. After A is disposed, B settles, and a fresh A generation settles, releasing both old deliveries produces no current publication and no snapshot/boundary/error change.
  </behavior>
  <action>Replace product-only calls to `assertPrefixesSince` and the polling acceptance loop with the Task 1 model/recorder and `awaitNextPublication` schedule. Keep the generic synthetic item/comment matrices as independent existing regressions, but remove permissive prefix expansion from installed-product acceptance entirely. Build expected comment and activity windows from the real `productTruth` before and after each operation, plus the exact descriptor model captured when each load/restart is requested. Each expected sequence must declare its required count and content; do not accept zero notifications or an unspecified number of duplicate status emissions. Scope boundary inspection by query reference, scope, generation, and store lifetime, and call it at every notification through 1-to-2 cutover, cleaning repoints, 2-to-1 completion, and append/restart. Make cleanup advance one bounded step at a time and require at least one exact repoint-reflecting publication per reader before `done`. Add a controllable real `ConvexReactClient.watchQuery` transport wrapper: defer one A result and one A error while their callbacks remain captured, dispose A, settle B, settle a new A generation, release the late deliveries, and assert no new current recorder entry and deep-equal current snapshot/boundaries before versus after. Mirror the same late-result/late-error fence in `tests/react/live-headless.test.tsx` with deterministic controlled callbacks so a product failure is localized without weakening the installed proof. Await each required event with one explicit deadline and fail on first mismatch; do not use sampling sleeps, retry loops, final-state-only settlement, or pass-on-rerun logic.</action>
  <acceptance_criteria>Both installed readers produce the exact modeled publication count, content, statuses, errors, and descriptor chains across every lifecycle stage; cleanup is non-vacuous, and late old-generation result/error deliveries are observably rejected after A-to-B-to-A replacement.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run --config vitest.react.config.ts tests/react/live-headless.test.tsx && node --test tests/integration/headless-backend.test.mjs && node scripts/test-headless-backend.mjs</automated></verify>
  <done>The real installed product path, not a synthetic substitute, satisfies exact temporal and boundary truth for comments and activity and rejects both stale result and stale error delivery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix only proven product defects and enforce the no-flake release gate</name>
  <files>src/react/query.ts, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, tests/helpers/headless-publication-oracle.mjs, tests/integration/headless-backend.test.mjs</files>
  <read_first>scripts/test-headless-backend.mjs, tests/helpers/headless-publication-oracle.mjs, tests/integration/headless-backend.test.mjs, tests/react/live-headless.test.tsx, src/react/query.ts, .planning/phases/02-complete-feedback-to-changelog-workflow/02-16-SUMMARY.md, package.json</read_first>
  <behavior>
    - If the strict red oracle exposes a production failure, the failing exact sequence is committed as the reproducer before the smallest existing-design fix; if product behavior is already correct, `src/react/query.ts` remains byte-for-byte unchanged.
    - Any production fix stays inside the current epoch/descriptor/operation fencing and all-descriptor staging design. It adds no schema field, DTO member, authority input, public token, cache, API, package dependency, or alternate merge truth.
    - Three consecutive direct installed-harness executions pass in one command invocation, stopping immediately on the first failure; a failed run followed by a successful rerun is still failure evidence.
    - The focused React/integration gates and complete Phase 2 gate pass after the consecutive direct runs, with the unrelated tracked dirty fingerprint unchanged.
  </behavior>
  <action>Run the strict oracle red against the existing product first. If it fails only because the recorder/model is wrong, correct the test oracle and retain the negative meta-tests; do not edit production to satisfy a mistaken schedule. If the exact evidence proves a real store defect, modify only `src/react/query.ts` at the existing dirty-callback, staged reread, structural restart, or generation fence responsible for that publication, keep the red scenario, and preserve every Plan 02-12 through 02-16 contract. Do not touch component schema/readers/writers or public types: the installed real queries are already required and production remains unchanged unless the red oracle demonstrates otherwise. After focused green, run the direct harness three consecutive times with a shell loop that exits on the first nonzero result, then run the full Phase 2 gate once. Do not automatically retry a failed command. Recompute `git diff -- .codex/config.toml .codex/hooks.json package-lock.json package.json | shasum -a 256` and require `09b5f7179dc749a197ec6a59c5ba188313b8c84536e0c35b6487ae7fcb058381`; stage/commit none of those paths.</action>
  <acceptance_criteria>The strict oracle is green without weakening its negative meta-tests, any production change is justified by a preserved red exact-sequence reproducer and stays within the existing design, three consecutive direct runs and the full Phase 2 gate pass without retry, and the unrelated tracked diff fingerprint is unchanged.</acceptance_criteria>
  <verify><automated>for run in 1 2 3; do node scripts/test-headless-backend.mjs || exit 1; done && npm run test:react && node --test tests/integration/headless-backend.test.mjs && npm run typecheck && npm run lint && npm run test:phase2 && test "$(git diff -- .codex/config.toml .codex/hooks.json package-lock.json package.json | shasum -a 256 | awk '{print $1}')" = "09b5f7179dc749a197ec6a59c5ba188313b8c84536e0c35b6487ae7fcb058381"</automated></verify>
  <done>One flaky failure fails the plan, the exact rejection-capable oracle and complete Phase 2 matrix are reproducibly green, and no unrelated or unjustified production surface changed.</done>
</task>

</tasks>

## Prohibitions

- Do not overwrite, retry in place, or edit completed Plan 02-16 or its summary; this explicit `plan-phase 02 --gaps` invocation owns additive Plan 02-17.
- Do not accept arbitrary prefixes, `startsWith`, subset/Set membership, selected `waitFor` snapshots, eventual convergence, final-state-only equality, optional cleanup publications, or pass-on-rerun as acceptance evidence.
- Do not check the typed error separately from the exact maximal prefix on its publication.
- Do not combine active descriptors from different readers, scopes, generations, or store lifetimes in one boundary assertion.
- Do not replace the installed real `listComments` and `listPostActivity` paths with synthetic tables or direct model helpers.
- Do not add schema, validators, DTO members, public APIs, authority inputs, packages, revision/watermark tokens, a parallel React cache, or new merge truth.
- Do not modify copied Phase 3 UI, package metadata, the user's TypeScript experiment, or unrelated Codex/h5i files.

## Source Coverage Audit

| Source | ID / constraint | Plan coverage | Status |
| --- | --- | --- | --- |
| GOAL | Complete provider-neutral lifecycle through exact tested headless APIs | Replaces only the insufficient installed-reader acceptance seam while every completed domain capability remains in the full regression gate. | COVERED |
| REQ | DISC-07 | Exact non-vacuous publications and active boundaries prove preserved merged comments through cutover, cleanup repoint, and completion. | COVERED |
| REQ | ADMN-10 | The same exact model proves complete append-only activity history through the real installed admin reader. | COVERED |
| REQ | UI-03 | Exact loading, full-window, fault-prefix, recovery, append/restart, and generation-replacement states are checked at every notification. | COVERED |
| REQ | QUAL-01 | Negative oracle meta-tests, mounted controls, installed real-Convex watches, repeated direct runs, and the full Phase 2 gate are mandatory. | COVERED |
| CONTEXT | D-02, D-12, D-21, D-47 | Total order, reader-atomic merge stages, bounded activity pagination, and exact accumulated windows are modeled together. | COVERED |
| CONTEXT | D-43, D-46, D-50, D-51, D-52 | No provider coupling or thrown render error is added; fault coupling and stale-generation rejection remain closed headless behavior. | COVERED |
| CONTEXT | D-53, D-54, D-58 | Existing visibility/merge semantics and stable DTO/API boundaries remain unchanged. | COVERED |
| RESEARCH | Pattern 8 and Pitfall 7 | Convex's installed watch store remains canonical; tests add no page cache or cursor substitute. | COVERED |
| PATTERNS | Real backend plus React/static proof layers | Pure oracle meta-tests strengthen the assigned integration layer while installed queries remain the behavioral authority. | COVERED |
| VERIFICATION | 62/64 remaining exact-publication and active-boundary gaps | Tasks implement every `gaps.missing` item and the required late result/error, non-vacuity, negative, and no-flake constraints. | COVERED |
| PRIOR PLAN | 02-16 | Completed production reader work and bounded retry remain preserved; the root cause is an execution/test-oracle miss, not a reason to rewrite historical work. | COVERED |
| LIVE PEER | ASK #f44421ca15b39572 | Technical oracle constraints are adopted; the overwrite/retry ownership recommendation is rejected because canonical `--gaps` routing requires sequential additive Plan 02-17. | COVERED |

No source item is missing. All other Phase 2 requirements and D-01 through D-58 decisions remain owned by completed Plans 02-01 through 02-16 and are exercised by the final aggregate gate. Deferred ideas, Phase 3 copied UI, schema work, and new external dependencies remain excluded.

## Artifacts this phase produces

- `tests/helpers/headless-publication-oracle.mjs` with test-only exports `createExpectedPublicationModel`, `assertExactPublicationSequence`, `assertExactDescriptorChain`, `createDeferredWatchTransport`, and `awaitNextPublication`.
- Negative meta-tests in `tests/integration/headless-backend.test.mjs` for shorter publication, correct-code/wrong-prefix, wrong-code/correct-prefix, overlapping boundaries, gapped boundaries, zero publications, accepted stale result, and accepted stale error.
- A reader/scope/generation/store-scoped active descriptor model covering 1-to-2 cutover, every cleaning repoint, 2-to-1 completion, and append/restart.
- Exact installed `listComments` and `listPostActivity` publication schedules with mandatory counts/content, same-publication fault coupling, non-vacuous cleanup, and event-driven deadlines.
- Deterministic deferred-transport evidence that late old-generation results and errors after A-to-B-to-A are rejected.
- Repeated direct harness and full Phase 2 regression evidence with no pass-on-rerun policy.
- No new production symbols, schema fields, validators, DTOs, public APIs, packages, or merge operations are planned. `src/react/query.ts` changes only if the strict red oracle proves a product defect, and then only within its existing private store design.

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Installed component publication -> test oracle | A permissive oracle can certify truncated or torn comment/activity windows as correct. |
| Query fault -> public headless error state | Prefix and error code must describe the same descriptor failure and same publication. |
| Active watch records -> boundary evidence | Concurrent readers/scopes/generations must not be combined into a fictitious chain. |
| Old generation transport -> replacement store | Late results/errors must not cross account-generation isolation after A-to-B-to-A. |
| Cleanup scheduler -> publication recorder | Optional or unobserved repoints can make the lifecycle proof vacuous. |
| Flaky command -> release decision | A later passing rerun must not erase a failed mandatory gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-02-80 | Repudiation / Tampering | temporal publication oracle | critical | mitigate | Exact sequence count/content and deep equality against the precise full expected window; negative meta-tests reject arbitrary prefixes and vacuous success. |
| T-02-81 | Tampering | typed fault publication | high | mitigate | Assert the closed error code and exact maximal contiguous failed-descriptor prefix on one publication object. |
| T-02-82 | Tampering / Denial of Service | active descriptor chain | high | mitigate | Scope inspection by reader/store/generation and require exact adjacency, no gap/overlap, and one tail at every lifecycle notification. |
| T-02-83 | Information Disclosure / Tampering | stale generation delivery | critical | mitigate | Deterministically release deferred old result and error after replacement and require no current publication or snapshot/boundary change. |
| T-02-84 | Repudiation | cleanup lifecycle proof | high | mitigate | Require modeled repoint evidence and at least one exact publication for both installed readers; zero/fewer publications fail. |
| T-02-85 | Repudiation | flaky release gate | high | mitigate | Three consecutive direct executions stop on first failure, followed by one full gate; no automatic rerun path. |
| T-02-SC | Tampering | dependency surface | low | accept | No install task or dependency change is permitted; existing audited runtime and packed gates remain unchanged. |

</threat_model>

<verification>
Run pure negative oracle meta-tests first, then mounted deferred-generation controls, then the installed real-merge comment/activity matrix with exact event-driven publication and boundary schedules. After focused green, execute the direct harness three consecutive times in one fail-fast command and run the full Phase 2 gate once. Preserve the unrelated tracked-diff fingerprint and hand the unchanged `02-VERIFICATION.md` to the independent verifier. No sampled snapshots, eventual convergence, retry-until-green, or fabricated numeric pass count is acceptable.
</verification>

<success_criteria>

- Every ordinary installed-product publication deep-equals its exact modeled full window and boundary chain at the correct sequence index.
- First/middle/tail errors couple the exact maximal contiguous prefix and typed code on the same publication, then recover through an exact modeled sequence.
- Cutover, cleanup, completion, and append/restart each expose exact contiguous active descriptors with no overlap and one unbounded tail.
- Cleanup produces mandatory repoint-reflecting publications; exact count/content checks make zero, missing, duplicate, or extra emissions fail.
- Late A-generation results and errors released after A-to-B-to-A produce no current state change.
- All eight negative meta-tests demonstrate rejection capability.
- Three consecutive direct installed-harness runs and one complete Phase 2 gate pass without retry; one failed run fails closure.
- Production code remains unchanged unless strict red evidence proves a defect, and any fix stays inside the existing private watch-store design without schema/API/package expansion.

</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-17-SUMMARY.md` when done.
</output>
