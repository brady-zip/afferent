---
phase: 02-complete-feedback-to-changelog-workflow
plan: "13"
type: execute
wave: 13
depends_on: ["02-12"]
files_modified:
  - src/react/query.ts
  - tests/react/live-headless.test.tsx
  - scripts/test-headless-backend.mjs
  - tests/integration/headless-backend.test.mjs
autonomous: true
gap_closure: true
requirements: [UI-03, QUAL-01]
must_haves:
  truths:
    - "Every loaded pagination descriptor is an ordered cursor window, every non-tail descriptor has an explicit end cursor, and adjacent windows are contiguous: endCursor[i] equals cursor[i+1] (D-47)."
    - "Reactive insertion grows the visible cursor-bounded loaded window, while deletion, sort-key movement, split, and empty-page collapse publish the exact current ordered window without gaps, overlaps, ID deduplication, or a data-layer fixed-item cap (D-47)."
    - "SplitRecommended pages remain publishable and split opportunistically; SplitRequired pages split before publication, and a required split without a cursor returns a closed typed error instead of hanging or exposing an incomplete page (D-46, D-47, D-50, D-51)."
    - "A mid-chain failure publishes only the last coherent contiguous prefix plus its typed error, then restores the complete window after recovery without remounting (D-47, D-50, D-51)."
    - "Append, split, collapse, load-more, and recovery transitions are serialized and generation-fenced, so rapid calls or stale callbacks cannot duplicate descriptors or mutate a replacement generation (D-47, D-52)."
    - "Mounted and real Convex tests compare every observed coherent tick by exact ordered array equality with canonical server truth for the cursor-bounded window; uniqueness, nonempty, Set, or item-count proxies cannot pass QUAL-01."
  artifacts:
    - path: src/react/query.ts
      provides: "Non-throwing end-cursor-pinned page-window state machine with serialized structural replacement"
    - path: tests/react/live-headless.test.tsx
      provides: "Mounted deterministic window, split, collapse, race, error-prefix, and generation-fence matrix"
    - path: scripts/test-headless-backend.mjs
      provides: "Disposable real Convex exact canonical-window oracle across mutations and real split thresholds"
    - path: tests/integration/headless-backend.test.mjs
      provides: "Release-gate audit that rejects weak Set/nonempty pagination assertions"
  key_links:
    - from: src/react/query.ts
      to: node_modules/convex-helpers/react.ts
      via: "A load-more replacement pins the old tail at its continueCursor and adds the next cursor window atomically"
    - from: src/react/query.ts
      to: tests/react/live-headless.test.tsx
      via: "Mounted controlled watches assert adjacency and exact arrays at every serialized structural transition"
    - from: scripts/test-headless-backend.mjs
      to: src/react/query.ts
      via: "Real watchQuery snapshots are checked against unpaginated canonical truth over the active cursor-bounded window"
    - from: tests/integration/headless-backend.test.mjs
      to: scripts/test-headless-backend.mjs
      via: "Static gate requires exact ordered canonical comparisons and forbids uniqueness/nonempty substitutes"
---

<objective>
Replace independent downstream cursor descriptors with the installed Convex-clone end-cursor-pinned page-window model while preserving Plan 02-12's non-throwing watch substrate.

Purpose: Close the sole 38/39 re-verification gap by making every reactive loaded window exact under insertion, deletion, reorder, split, collapse, errors, rapid loads, and identity-generation changes.
Output: One serialized contiguous-window state machine, mounted race coverage, a strict real Convex canonical oracle, and passing packed/full Phase 2 regression gates.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- ranked, searchable, and filterable discovery with admin duplicate merges; authenticated participation; admin moderation with a status-driven roadmap; manually published changelog entries linked to feedback; and in-app notifications backed by a host-consumable delivery outbox -- through tested component APIs and framework-light headless React hooks with explicit async, auth, and error states, **so that** my users and admins can complete the entire feedback lifecycle while I retain ownership of identity, authorization, and my own presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-PATTERNS.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-DISCUSSION-LOG.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-VERIFICATION.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-12-PLAN.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-12-SUMMARY.md
@src/react/query.ts
@node_modules/convex-helpers/react.ts
@node_modules/convex/src/server/pagination.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace weak pagination checks with exact mounted and real cursor-window oracles</name>
  <files>tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs</files>
  <read_first>src/react/query.ts, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs, node_modules/convex-helpers/react.ts, node_modules/convex/src/server/pagination.ts</read_first>
  <behavior>
    - Starting from at least three loaded windows, front, middle, and back insertion; front and middle deletion; deletion that empties a page; and sort-key movement emit only exact canonical ordered cursor-window arrays. A front insertion increases visible length when it falls inside the loaded cursor range.
    - Mounted watches assert `endCursor[i] === cursor[i+1]` after load-more, split, and collapse. No reconciliation rule truncates to the original item count, rebases a downstream cursor from an earlier page's new `continueCursor`, or removes repeated IDs after concatenation.
    - Real `SplitRecommended` and `SplitRequired` are forced by backend byte/read thresholds. Recommended content stays visible during its opportunistic replacement; required content remains hidden until both exact child windows are ready; required status without `splitCursor` produces typed error state.
    - `loadMore` during a reactive update, two rapid `loadMore` calls, and `loadMore` immediately after split or collapse create one contiguous append only. The same cases followed by a generation change ignore every old queued operation and watch callback.
    - A first, middle, and tail page failure returns the exact coherent prefix ending before the failed window plus typed error. Recovery restores the full exact window, with no intermediate mixed chain.
    - The real harness records every store notification and compares exact labels, order, and length to a real unpaginated canonical query for that revision and active cursor-bounded terminal window; any gap, overlap, reorder, shortening, or extra row fails.
  </behavior>
  <action>Strengthen the mounted controlled client so it can model explicit `(cursor, endCursor]` ranges, revision-tagged canonical arrays, structural-operation interleavings, real split statuses, empty windows, and stale-generation callbacks. Add the complete matrix above before changing `src/react/query.ts`. In the disposable backend, replace delete-and-reinsert-only probes with narrow insert, delete, sort-key move, fault, and canonical-list operations that retain stable logical labels and return a revision. Wrap real page watches to associate every returned opaque cursor with the last logical sort key at that revision; combine those cursor anchors with the active descriptor chain to slice the unpaginated canonical list through the active tail boundary. Record every store emission and active subscription, assert exact array equality against that canonical cursor-bounded slice, and separately assert adjacent opaque descriptor boundaries. Exercise real byte/read thresholds for both split statuses rather than fabricating them. Remove the generated-ID `Set` uniqueness and nonempty assertions; neither is evidence of content completeness. Update the integration gate to require exact ordered/canonical assertions and reject those weak proxies. This task is red against the current independent descriptor concatenation.</action>
  <acceptance_criteria>The test matrix fails specifically with the verifier's shortened `[zero,a,b]`/`[a,b,c]` outcomes and separately reports boundary, exact-array, split, collapse, race, prefix, and harness-setup failures.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run --config vitest.react.config.ts tests/react/live-headless.test.tsx && node --test tests/integration/headless-backend.test.mjs && node scripts/test-headless-backend.mjs</automated></verify>
  <done>The remaining D-47 gap is represented by strict red mounted and real-backend oracles that cannot pass on uniqueness, nonempty output, or a coincidentally correct last item.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port the end-cursor-pinned contiguous page-window state machine</name>
  <files>src/react/query.ts, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs</files>
  <read_first>tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, src/react/query.ts, node_modules/convex-helpers/react.ts, node_modules/convex/src/server/pagination.ts</read_first>
  <action>Replace independent active descriptors with an ordered window chain and one serialized structural-operation queue. Preserve the Plan 02-12 `watchQuery`/`useSyncExternalStore` error-catching substrate, but port the installed helper's end-cursor model: the initial tail uses `numItems`; on `loadMore`, capture its `continueCursor`, create a pinned replacement with the same start and `endCursor` equal to that cursor, create one new unbounded tail starting at the same cursor, keep the old tail published, and atomically substitute both only after both watches have complete results. All committed non-tail windows must have `endCursor`, and every adjacent pair must satisfy `endCursor[i] === cursor[i+1]`. Earlier-page updates then grow or shrink inside their fixed cursor bounds; never rebase or rebuild downstream descriptors from a changed `continueCursor`. The loaded data contract is the complete current cursor-bounded window, so front insertion legitimately grows the visible array; any fixed-N presentation belongs above the hook and never in reconciliation. Concatenate exact page arrays in window order without ID deduplication. Treat `SplitRecommended` as a complete publishable window and enqueue an opportunistic two-child replacement at `splitCursor`; treat `SplitRequired` as incomplete, enqueue its two-child replacement before publication, and return a closed typed invariant error if the server omits `splitCursor`. Keep the original complete window visible until both split children load, then swap atomically. Collapse an empty committed window by atomically replacing it with a deterministic adjacent merge: merge an interior/tail empty range into its left neighbor by extending that neighbor to the empty range's end, or merge an empty first range into its right neighbor by moving the right start to `null`; carry the sum of both descriptors' requested item counts only as the merged query's initial hint, while explicit cursor bounds remain authoritative, and coalesce consecutive empties one serialized replacement at a time. A sole empty window remains the valid empty chain because it has no adjacent range. For a watch error, walk from the head and publish only the maximal loaded, error-free, boundary-contiguous prefix plus the typed error; keep later watches alive and restore the full chain only after recovery. Fence queue entries, append, split, collapse, watch reads, and commit callbacks by store epoch/generation, and permit only one structural replacement at a time so rapid loads cannot duplicate descriptors.</action>
  <acceptance_criteria>At every publish point the active chain is contiguous and results are the exact concatenation of its coherent prefix; no changed earlier cursor can shorten or overlap later coverage, incomplete required pages never render, and structural races are deterministic.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run --config vitest.react.config.ts tests/react/live-headless.test.tsx && node scripts/test-headless-backend.mjs && npm run typecheck && npm run lint</automated></verify>
  <done>The non-throwing paginated store implements cursor-bounded loaded-window semantics with atomic load-more, split, collapse, error recovery, and generation-fenced structural serialization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stress structural interleavings and close the packed Phase 2 gate</name>
  <files>src/react/query.ts, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs</files>
  <read_first>src/react/query.ts, tests/react/live-headless.test.tsx, scripts/test-headless-backend.mjs, tests/integration/headless-backend.test.mjs, tests/integration/packed-artifact.test.mjs, package.json</read_first>
  <behavior>
    - Repeated runs of insert/delete/reorder while append, recommended split, required split, or empty collapse is pending produce the same exact canonical array sequence and dispose every superseded watch once.
    - Two rapid loads, load during update, and load after structural replacement never create duplicate boundary pairs, orphan watches, or a second unbounded tail.
    - An auth generation transition during every structural operation publishes the new generation's cold state immediately and no old operation changes its descriptors, errors, results, or status.
    - Every published domain paginator retains its existing state vocabulary and packed declaration/runtime contract while consuming the corrected common store.
  </behavior>
  <action>Run the mounted interleaving matrix repeatedly with deterministic scheduling, add watch attach/dispose accounting, and make invariant failures identify the structural operation and boundary pair. Run the real backend matrix enough times to cover update ordering without weakening exact per-tick equality. Preserve all Plan 02-12 direct-query, typed-error, retry, identity, mutation, and optimism behavior. Then run every React suite, the headless integration audit, the real backend oracle, static contracts, typecheck, lint, build, the clean packed tarball consumer, and the full Phase 2 gate. Re-run Phase 2 verification and require 39/39 with UI-03 and QUAL-01 satisfied; no source-text, Set-uniqueness, nonempty, or fixed-count assertion substitutes for the real canonical-window oracle.</action>
  <acceptance_criteria>The corrected common store survives repeated structural/auth interleavings, all existing domain hooks and the packed consumer remain compatible, and the strict real oracle plus full release gate pass without regression.</acceptance_criteria>
  <verify><automated>npm run test:react && node --test tests/integration/headless-backend.test.mjs && node scripts/test-headless-backend.mjs && npm run test:static && npm run typecheck && npm run lint && npm run build && npm run test:package && npm run test:phase2</automated></verify>
  <done>D-47 is exact under real reactive mutation and concurrency, prior Plan 02-12 guarantees remain green, the packed artifact passes, and re-verification reaches 39/39.</done>
</task>

</tasks>

## Source Coverage Audit

| Source | Coverage in this plan |
|---|---|
| Phase goal | Preserved verbatim; this final additive plan changes only the published headless pagination reconciliation seam. |
| Requirements | UI-03 owns the exact reactive paginated hook behavior; QUAL-01 requires mounted, real-backend, full, and packed proof. |
| Context decisions | D-47 is the remaining failed truth. D-46, D-50, D-51, and D-52 constrain typed errors, non-throwing recovery, and generation isolation. All other D-01..D-58 decisions remain covered by completed plans. |
| Research and patterns | The installed helper's end-cursor page pinning and split replacement are ported onto the required non-throwing watch substrate. The live radio decision supersedes rebase/rebuild and fixed-N interpretations. |
| Verification | The sole actionable 38/39 gap is closed; weak generated-ID/nonempty assertions are replaced by exact real canonical-window equality. |
| Prior plan | Plan 02-12's watch client, typed snapshots, identity generation, mutation fences, and packed host contract are dependencies and regression requirements, not rewrite targets. |

## Artifacts this phase produces

- An ordered page-window chain with a single unbounded tail, explicit non-tail `endCursor` bounds, adjacency assertions, and one serialized structural replacement queue.
- Atomic tail-pin-plus-append, recommended/required split, and empty-window collapse transitions, all fenced by store epoch/generation.
- Maximal coherent-prefix error snapshots that retain live recovery watches without publishing later disconnected windows.
- A mounted matrix for front/middle/back insert, deletion, empty collapse, sort-key reorder, real split semantics, rapid/update-time loads, error recovery, and generation switches.
- A disposable real Convex revision-aware oracle that checks every observed tick by exact ordered equality against canonical server truth over the active loaded cursor window.
- Full React/static/type/lint/build/packed/Phase 2 regressions and a final 39/39 verification result.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Reactive page result -> loaded React window | Independent watch updates must compose into one exact contiguous ordered range. |
| Structural callback -> descriptor chain | Append, split, collapse, and recovery callbacks may interleave or arrive after replacement. |
| Server split status -> public snapshot | A required split can represent incomplete data and must not be rendered as complete. |
| Auth generation -> queued page work | Old identity callbacks and queued structural operations must not alter the current generation. |
| Test oracle -> release decision | Weak proxies can certify a shortened or overlapping real loaded window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-53 | Tampering | page-window adjacency | critical | mitigate | Explicit end-cursor pinning, boundary invariant checks, exact ordered canonical oracle, and no ID deduplication. |
| T-02-54 | Denial of Service / Tampering | SplitRequired handling | high | mitigate | Hide incomplete page, eagerly create both bounded children, typed missing-cursor error, and real threshold coverage. |
| T-02-55 | Tampering | structural-operation races | high | mitigate | One serialized queue, atomic replacement groups, one unbounded tail, duplicate-load guards, and repeated interleaving tests. |
| T-02-56 | Information Disclosure / Tampering | stale generation page work | critical | mitigate | Capture epoch/generation on every queued operation and watch; reject stale reads, commits, errors, and disposal callbacks. |
| T-02-57 | Denial of Service | empty-window churn | medium | mitigate | Deterministic adjacent collapse, consecutive-empty serialization, exact disposal accounting, and stable terminal tail rules. |
| T-02-58 | Repudiation | real-backend oracle | high | mitigate | Revision-aware exact label/order/length comparison at every tick; integration audit rejects Set/nonempty proxies. |
| T-02-59 | Denial of Service | mid-chain query error | high | mitigate | Publish only maximal coherent prefix plus closed error, keep watches live, and require exact recovery without remount. |
</threat_model>

<verification>
Run the red-first mounted and disposable real Convex exact-window matrices, then the complete React/static/type/lint/build, clean packed-consumer, and full Phase 2 gates. Re-run verification and require 39/39. A Set of generated IDs, nonempty output, fixed item count, downstream-cursor rebase, or source inspection cannot satisfy the D-47/QUAL-01 gate.
</verification>

<success_criteria>
- Every committed non-tail page is end-cursor pinned and every adjacent descriptor boundary is identical.
- Exact visible results equal canonical ordered truth for the cursor-bounded loaded window at every observed coherent tick across insertion, deletion, reorder, split, collapse, load, error, and recovery.
- Front insertion grows the loaded result when inside the current cursor window; no data-layer fixed-N cap or ID deduplication hides legitimate changes.
- SplitRecommended is non-blocking and opportunistic; SplitRequired is eager and never publishes incomplete data; a missing required cursor is typed error state.
- Structural operations are serialized and generation-fenced; rapid or stale operations cannot duplicate, orphan, overlap, or mutate the current chain.
- All Plan 02-12 guarantees, the packed artifact, and the full Phase 2 gate pass; re-verification reports 39/39.
</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-13-SUMMARY.md` when done.
</output>
