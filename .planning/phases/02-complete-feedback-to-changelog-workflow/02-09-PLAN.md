---
phase: 02-complete-feedback-to-changelog-workflow
plan: "09"
type: execute
wave: 9
depends_on: ["02-05"]
files_modified:
  - src/component/_generated/api.ts
  - src/component/_generated/component.ts
  - src/component/_generated/dataModel.ts
  - src/component/_generated/server.ts
  - src/component/schema.ts
  - src/component/validators.ts
  - src/component/notifications/outbox.ts
  - src/component/jobs/outbox.ts
  - src/component/feedback.ts
  - src/client/contracts.ts
  - src/client/internal.ts
  - src/client/server.ts
  - tests/component/outbox.test.ts
  - tests/integration/outbox-backend.test.ts
  - tests/static/contracts.test.ts
  - scripts/test-outbox-backend.mjs
autonomous: true
requirements: [NOTF-06, QUAL-01]
must_haves:
  truths:
    - "The component creates one delivery row per eligible actor with typed facts and no PII/rendered message/provider data (D-39, D-41)."
    - "Trusted host-only workers claim bounded fenced leases, then ack/release; expiry redelivers at least once and poison rows park after bounded retries (D-40)."
    - "Claim resolves the current non-PII recipientKey and discards anonymized actors; event metadata remains snapshotted (D-42)."
  artifacts:
    - path: src/component/notifications/outbox.ts
      provides: "Claim/ack/release/prune delivery operations with fenced leases"
    - path: src/client/server.ts
      provides: "Explicit host-server-only delivery capability surface"
    - path: tests/integration/outbox-backend.test.ts
      provides: "Concurrent claims, stale ack, expiry, retry/dead-letter proof"
  key_links:
    - from: src/component/notifications/fanout.ts
      to: src/component/notifications/outbox.ts
      via: "Same logical recipient event materializes one inbox and one host delivery row"
    - from: src/client/server.ts
      to: src/component/notifications/outbox.ts
      via: "Trusted server-only capability; absent from browser React bindings"
---

<objective>
Deliver the vendor-neutral host-consumable notification outbox with at-least-once fenced delivery semantics.

Purpose: Let hosts choose contact/channel vendors without moving PII, provider records, or rendering into the component.
Output: Outbox schema/operations, server-only client contracts, and concurrency/lease tests.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- ranked, searchable, and filterable discovery with admin duplicate merges; authenticated participation; admin moderation with a status-driven roadmap; manually published changelog entries linked to feedback; and in-app notifications backed by a host-consumable delivery outbox -- through tested component APIs and framework-light headless React hooks with explicit async, auth, and error states, **so that** my users and admins can complete the entire feedback lifecycle while I retain ownership of identity, authorization, and my own presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-PATTERNS.md
@.planning/phases/01-secure-installable-feedback-board/01-03-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify vendor-neutral delivery, fencing, and poison handling</name>
  <files>tests/component/outbox.test.ts, tests/integration/outbox-backend.test.ts, tests/static/contracts.test.ts, scripts/test-outbox-backend.mjs</files>
  <read_first>src/component/notifications/fanout.ts, src/component/model/actors.ts, src/client/server.ts, .planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md</read_first>
  <behavior>
    - Claim returns max 50 typed versioned facts with stable event ID/idempotency key, sequence/time, entity metadata, and freshly resolved recipientKey only (D-40..D-42).
    - Lease is 5 minutes; owner plus incrementing version fences ack/release; expiry reclaims; release backs off and attempt 8 parks deadLetter.
    - Acked rows prune in bounded batches; anonymized/missing recipients discard before delivery; concurrent claims never duplicate an active lease.
  </behavior>
  <action>Create red component/static and disposable real-backend tests for two workers, lease expiry/reclaim, stale ack/release, repeated host retry, exponential backoff `min(3_600_000, 30_000 * 2^attempt)`, dead-letter at 8 attempts, ack prune, anonymize-after-capture, two scopes, and privacy/React-export absence.</action>
  <acceptance_criteria>Tests fail at missing server-only outbox contracts and fully specify NOTF-06 plus D-39..D-42.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/outbox.test.ts tests/static/contracts.test.ts</automated></verify>
  <done>The delivery contract is executable before implementation and explicitly at-least-once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fenced leased delivery and bounded maintenance</name>
  <files>src/component/schema.ts, src/component/validators.ts, src/component/notifications/outbox.ts, src/component/jobs/outbox.ts, src/component/feedback.ts</files>
  <read_first>tests/component/outbox.test.ts, tests/integration/outbox-backend.test.ts, src/component/notifications/fanout.ts, src/component/model/actors.ts</read_first>
  <action>Add scoped delivery rows/indexes and versioned typed payload validators. Fan-out materializes one delivery row per eligible recipient from the same logical event guard. Implement trusted `claimDeliveryBatch`, `ackDelivery`, `releaseDelivery` plus internal bounded prune: claim max 50 pending/expired rows atomically, increment leaseVersion, set 5-minute lease, resolve actor externalKey freshly as `recipientKey`, and discard anonymized actors. Ack/release require row ID, owner, exact version; release increments attempts/backoff and attempt 8 parks. Payload contains opaque IDs/event facts/sequence/time only; ordering is best effort and stable event ID is the host idempotency key (D-39..D-42, D-54, D-56, D-57). Run `npm run codegen:component` and include generated outputs; never hand-edit them.</action>
  <acceptance_criteria>Claims are scope-safe/concurrency-safe/fenced, retries are bounded, stale workers cannot mutate reclaimed rows, and no contact/display/provider PII is stored or returned.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/component/outbox.test.ts tests/static/schema-scope.test.ts tests/static/contracts.test.ts && node scripts/test-outbox-backend.mjs</automated></verify>
  <done>The component provides a durable vendor-neutral at-least-once delivery queue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose delivery only through the trusted server client</name>
  <files>src/client/contracts.ts, src/client/internal.ts, src/client/server.ts, tests/static/contracts.test.ts</files>
  <read_first>src/component/notifications/outbox.ts, src/client/internal.ts, src/client/server.ts, tests/static/contracts.test.ts</read_first>
  <action>Add `DeliveryEventDto`, `DeliveryLeaseDto`, claim/ack/release intents/results, and `createDeliveryClient` or an explicit `delivery` group available only from `afferent/server.js`. Resolve scope on every call and require host server authority; do not export delivery through root browser bindings or `src/react/**`. Expected lease loss/dead-letter conflicts return typed results, not generic CRUD/raw rows.</action>
  <acceptance_criteria>A host worker can claim/send/ack or release typed events using recipientKey and stable event ID, while browser/public package surfaces cannot reach the server-only capability.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/static/contracts.test.ts && npm run typecheck && npm run lint</automated></verify>
  <done>The host-consumable outbox contract is complete without a built-in email/push vendor.</done>
</task>

</tasks>

## Artifacts this phase produces

- Complete new-path inventory: `src/component/notifications/outbox.ts`, `src/component/jobs/outbox.ts`, `tests/component/outbox.test.ts`, `tests/integration/outbox-backend.test.ts`, `scripts/test-outbox-backend.mjs`.
- `src/component/notifications/outbox.ts`: `claimDeliveryBatch`, `ackDelivery`, `releaseDelivery`.
- `src/component/jobs/outbox.ts`: `pruneAckedDeliveries` and bounded maintenance constants.
- `DeliveryEventDto`, `DeliveryLeaseDto`, lease/conflict result unions and validators.
- Server-only delivery capability factory/group exported only from `afferent/server.js`.
- Named outbox tests and `scripts/test-outbox-backend.mjs`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Component outbox -> host worker | Trusted server receives recipient routing key and event facts for external side effects. |
| Worker ack/release -> leased row | Stale or forged lease operations can lose/duplicate delivery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-31 | Spoofing / Elevation | server delivery API | critical | mitigate | Server-only export, per-call scope resolution/authority, no React/browser binding. |
| T-02-32 | Tampering | lease ack/release | high | mitigate | Row + owner + incrementing leaseVersion fencing and expiry reclaim. |
| T-02-33 | Information Disclosure | delivery payload | high | mitigate | Typed opaque facts only, fresh non-PII recipientKey, discard anonymized actors. |
| T-02-34 | Denial of Service | retry/pruning | medium | mitigate | Max-50 claims, bounded exponential backoff, attempt-8 dead-letter, bounded prune. |
</threat_model>

<verification>
Run component/static suites, concurrent real-backend outbox harness, schema/contracts audits, typecheck/lint, fan-out regressions.
</verification>

<success_criteria>
- NOTF-06 works through a typed trusted server contract.
- D-39..D-42 are proven under concurrency, expiry, stale workers, retries, and anonymization.
- No vendor, contact PII, rendered message, or browser delivery API enters Afferent.
</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-09-SUMMARY.md` when done.
</output>
