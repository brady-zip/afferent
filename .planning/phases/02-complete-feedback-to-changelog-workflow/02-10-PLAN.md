---
phase: 02-complete-feedback-to-changelog-workflow
plan: "10"
type: execute
wave: 10
depends_on: ["02-09"]
files_modified:
  - package.json
  - src/client/contracts.ts
  - src/client/index.ts
  - src/react/provider.tsx
  - src/react/bindings.ts
  - src/react/hooks/mutations.ts
  - src/react/hooks/feedback.ts
  - src/react/hooks/admin.ts
  - src/react/hooks/roadmap.ts
  - src/react/hooks/changelog.ts
  - src/react/hooks/notifications.ts
  - src/react/index.ts
  - fixtures/packed-vite-convex/package.json
  - fixtures/packed-vite-convex/convex/afferent.ts
  - fixtures/packed-vite-convex/src/App.tsx
  - fixtures/packed-vite-convex/src/App.test.tsx
  - scripts/test-packed-consumer.mjs
  - tests/react/provider.test.tsx
  - tests/react/mutations.test.tsx
  - tests/integration/packed-artifact.test.mjs
  - tests/static/contracts.test.ts
  - tests/static/exports.test.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03, QUAL-01]
must_haves:
  truths:
    - "One AfferentProvider accepts grouped generated host refs plus a minimal auth adapter; optional groups report unsupported and runtime authorization remains distinct (D-43..D-46)."
    - "All domain hooks share explicit query/mutation/error/auth states, bounded pagination/debounce, duplicate guards, and only the three allowed optimistic operations (D-47..D-51)."
    - "Logout/account switch clears every actor-scoped overlay/page/inbox/admin state (D-52), and the full contract installs from a packed tarball."
  artifacts:
    - path: src/react/index.ts
      provides: "Complete stable headless React public entrypoint"
    - path: fixtures/packed-vite-convex/src/App.tsx
      provides: "Consumer-owned presentation exercising every headless capability group"
    - path: tests/integration/packed-artifact.test.mjs
      provides: "Clean tarball codegen/typecheck/build/export acceptance"
  key_links:
    - from: src/react/provider.tsx
      to: consumer-generated host refs
      via: "Typed grouped bindings and auth generation, never provider-specific imports"
    - from: fixtures/packed-vite-convex/src/App.tsx
      to: src/react/index.ts
      via: "Installed package subpath only; no repository-relative source"
---

<objective>
Freeze the complete cross-capability headless React contract and prove it from the packed artifact in a clean consumer.

Purpose: Turn the Phase 2 domain into a provider/router/design-system-neutral integration surface with safe async/auth/error semantics.
Output: Final React entrypoint/contracts, shared mutation/error state, actor reset behavior, packed fixture, and complete invariant/security gates.
</objective>

## Phase Goal

**As a** developer integrating Afferent into my existing Convex + React application, **I want to** run the complete provider-neutral feedback-to-roadmap-to-changelog workflow -- ranked, searchable, and filterable discovery with admin duplicate merges; authenticated participation; admin moderation with a status-driven roadmap; manually published changelog entries linked to feedback; and in-app notifications backed by a host-consumable delivery outbox -- through tested component APIs and framework-light headless React hooks with explicit async, auth, and error states, **so that** my users and admins can complete the entire feedback lifecycle while I retain ownership of identity, authorization, and my own presentation.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-CONTEXT.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-RESEARCH.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-PATTERNS.md
@.planning/phases/01-secure-installable-feedback-board/01-05-SUMMARY.md
@.planning/phases/01-secure-installable-feedback-board/01-08-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-09-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the complete provider/state/error/reset contract</name>
  <files>tests/react/provider.test.tsx, tests/react/mutations.test.tsx, tests/static/contracts.test.ts, tests/static/exports.test.ts, tests/integration/packed-artifact.test.mjs</files>
  <read_first>src/react/provider.tsx, src/react/bindings.ts, src/react/hooks/feedback.ts, src/client/contracts.ts, scripts/test-packed-consumer.mjs</read_first>
  <behavior>
    - Public refs/auth adapter are baseline; omitted participation/admin/roadmap/changelog/notifications groups return unsupported; not-authorized is separate (D-43..D-46).
    - Shared error union maps authentication, authorization, validation, not-found, rate-limit, conflict, transient/network, unknown; expected failures resolve and appear in state (D-50).
    - Public reads may run during auth loading under public policy; protected reads wait; retry only transient and rate-limit uses retryAfterMs (D-51).
    - Account/session generation change clears actor-sensitive pages, optimism, inbox, and admin capability (D-52).
  </behavior>
  <action>Expand red React/static/packed tests to enumerate every public domain hook/action and state variant, duplicate-submit guard, pagination/debounce mapping, three allowed optimistic rollbacks, unsupported groups, server-derived admin gating, actor switch, strict export map, declaration resolution, and forbidden provider/router/toast/design-system/source-relative imports. The packed fixture must exercise complete host-generated refs from an installed tarball.</action>
  <acceptance_criteria>Tests fail only on missing cross-cutting contract/export/reset behavior and make UI-01/02/03 plus D-43..D-52/QUAL-01 executable.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/react/provider.test.tsx tests/react/mutations.test.tsx tests/static/contracts.test.ts tests/static/exports.test.ts</automated></verify>
  <done>The final headless and packed acceptance contract is red and complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize all hooks onto one stable state and error vocabulary</name>
  <files>src/client/contracts.ts, src/client/index.ts, src/react/provider.tsx, src/react/bindings.ts, src/react/hooks/mutations.ts, src/react/hooks/feedback.ts, src/react/hooks/admin.ts, src/react/hooks/roadmap.ts, src/react/hooks/changelog.ts, src/react/hooks/notifications.ts, src/react/index.ts, tests/react/provider.test.tsx, tests/react/mutations.test.tsx</files>
  <read_first>tests/react/provider.test.tsx, tests/react/mutations.test.tsx, all existing src/react/hooks/*.ts, src/client/contracts.ts</read_first>
  <action>Finalize grouped binding types and stable domain hooks, never generic query/mutation wrappers. Add one versioned closed `AfferentError` mapping layer and shared mutation controller for `{ok:true,data}|{ok:false,error}`, pending/error/reset, duplicate guards, per-row keys, transient retry, and delayed RATE_LIMITED metadata. Keep create/edit/comment/admin/status/archive/merge/publish on server truth; only vote/subscription/mark-read use native reversible optimism (D-48..D-50). Require auth adapter `state` plus optional opaque `sessionGeneration` (or documented provider remount key); on change clear every actor-sensitive hook key/cache/overlay and admin state without sending it to the backend (D-43..D-52). Preserve D-53..D-58 across hook DTOs: shared visibility, merge/hide/anonymize consistency, no status automation, bounded jobs, late actor resolution, explicit additive contract versioning.</action>
  <acceptance_criteria>Every domain hook has the documented explicit vocabulary, expected failures never reject, no broad optimistic cache exists, and actor switches cannot display stale protected state.</acceptance_criteria>
  <verify><automated>npm exec -- vitest run tests/react tests/static/contracts.test.ts tests/static/exports.test.ts && npm run typecheck && npm run lint</automated></verify>
  <done>The headless layer is coherent, provider-neutral, and safe across every Phase 2 capability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the complete Phase 2 contract from a clean packed consumer</name>
  <files>package.json, fixtures/packed-vite-convex/package.json, fixtures/packed-vite-convex/convex/afferent.ts, fixtures/packed-vite-convex/src/App.tsx, fixtures/packed-vite-convex/src/App.test.tsx, scripts/test-packed-consumer.mjs, tests/integration/packed-artifact.test.mjs</files>
  <read_first>package.json, scripts/test-packed-consumer.mjs, tests/integration/packed-artifact.test.mjs, fixtures/packed-vite-convex/convex/afferent.ts</read_first>
  <action>Add the `./react.js` package export/declarations and a named `test:phase2` gate. Extend the external OS-temp consumer to install the tarball plus runtime dependencies, register/codegen/typecheck the component, compile consumer-generated public/participation/admin/roadmap/changelog/notification refs, import every root/server/react export, exercise the headless feedback-to-changelog/inbox path with consumer-owned markup, and run strict typecheck/Vite build/publint/ATTW. Include all model/component/real-backend/static/React suites in the gate. Preserve the user's dirty TypeScript/package experiment: verify committed-baseline compatibility from a clean detached copy and do not stage unrelated manifest/lock changes.</action>
  <acceptance_criteria>A clean Vite/Convex consumer installs only the packed artifact, builds the complete headless integration with no source-relative imports, and the named Phase 2 invariant/security gate passes.</acceptance_criteria>
  <verify><automated>npm run build && npm run test:phase2</automated></verify>
  <done>The complete Phase 2 developer story is proven from the artifact consumers will install.</done>
</task>

</tasks>

## Artifacts this phase produces

- Complete new-path inventory: `src/react/hooks/mutations.ts`, `tests/react/provider.test.tsx`, `tests/react/mutations.test.tsx`, `tests/static/exports.test.ts`.
- `src/react/hooks/mutations.ts`: shared typed mutation controller/error mapper.
- Final `AfferentProviderProps`, grouped binding interfaces, `AuthStateAdapter` with optional opaque session generation.
- Versioned `AfferentError` and `AfferentResult<T>` contracts.
- `afferent/react.js` export with declarations for all named domain hooks/providers/types.
- `tests/react/provider.test.tsx`, `tests/react/mutations.test.tsx`, `tests/static/exports.test.ts`.
- Expanded packed fixture and `test:phase2` package script; presentation-source and deployment artifacts remain outside this plan.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Auth adapter -> React cache/state | UX authentication state is untrusted as backend authority and can change actors. |
| Installed package -> consumer app | Export/declaration/runtime dependency drift can bypass tested repository source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-35 | Spoofing / Elevation | provider/admin hooks | critical | mitigate | Hooks use trusted host refs/capability query only; server derives actor/admin/scope every call. |
| T-02-36 | Information Disclosure | actor-sensitive React state | high | mitigate | Auth generation/remount resets pages, overlays, inbox, and admin capability before render. |
| T-02-37 | Tampering | optimistic mutations | medium | mitigate | Only three idempotent reversible operations; native rollback and server truth. |
| T-02-38 | Tampering / Supply Chain | packed exports | high | mitigate | Clean tarball-only install, lock/runtime audit, codegen/typecheck/build/publint/ATTW and source-root rejection. |
</threat_model>

<verification>
Run all React/static suites, every real-backend seam, complete model/component tests, build/typecheck/lint, then the named clean packed Phase 2 gate.
</verification>

<success_criteria>
- UI-01/02/03 cover every Phase 2 capability group with explicit async/auth/error semantics.
- QUAL-01 includes domain invariants, authority/visibility, merges, roadmap, changelog, notifications, outbox, real-backend seams, and packed consumer proof.
- D-43..D-58 are all enforced within the component, host-client, headless, and packed-consumer boundary.
</success_criteria>

<output>
Create `.planning/phases/02-complete-feedback-to-changelog-workflow/02-10-SUMMARY.md` when done.
</output>
