---
phase: 03-source-owned-product-interface
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - ui/afferent/core/afferent-ui-provider.tsx
  - ui/afferent/core/copy.ts
  - ui/afferent/core/icons.tsx
  - ui/afferent/core/navigation.tsx
  - ui/afferent/core/state-region.tsx
  - ui/afferent/core/utils.ts
  - ui/afferent/board/board-screen.tsx
  - ui/afferent/afferent.css
  - ui/afferent/registry.ts
  - scripts/generate-ui-artifacts.mjs
  - registry/registry.json
  - registry/r/afferent-ui-core.json
  - registry/r/afferent-board.json
  - examples/ui/afferent/core/afferent-ui-provider.tsx
  - examples/ui/afferent/core/copy.ts
  - examples/ui/afferent/core/icons.tsx
  - examples/ui/afferent/core/navigation.tsx
  - examples/ui/afferent/core/state-region.tsx
  - examples/ui/afferent/core/utils.ts
  - examples/ui/afferent/board/board-screen.tsx
  - examples/ui/afferent/afferent.css
  - fixtures/registry-vite/package.json
  - fixtures/registry-vite/components.json
  - fixtures/registry-vite/index.html
  - fixtures/registry-vite/tsconfig.json
  - fixtures/registry-vite/vite.config.ts
  - fixtures/registry-vite/src/index.css
  - fixtures/registry-vite/src/main.tsx
  - fixtures/registry-vite/src/App.tsx
  - tests/integration/registry-ui.test.mjs
  - tests/integration/ui-artifacts.test.mjs
autonomous: true
requirements: [UI-04, UI-06, UI-07, QUAL-04]
must_haves:
  truths:
    - "A developer can install a packed Afferent tarball and the generated afferent-board item through the pinned local shadcn CLI, then typecheck and build the clean Vite consumer."
    - "One hand-edited core/board source emits deterministic registry JSON and a byte-equal repository mirror; generated output is never the authority."
    - "The core package keeps Convex/React as its only peers and gains no UI runtime dependency, while copied consumers receive exactly the approved five UI runtime packages."
    - "The user's unstaged TypeScript 7.0.2 manifest and lock experiment is restored after the exact Phase 3 dependency commit and remains unstaged."
  artifacts:
    - path: ui/afferent/board/board-screen.tsx
      provides: "First route-agnostic source-owned screen over the Phase 2 feedback hooks"
    - path: scripts/generate-ui-artifacts.mjs
      provides: "Stable registry and byte-equivalent mirror generator"
    - path: tests/integration/registry-ui.test.mjs
      provides: "Packed tarball plus real local shadcn install/typecheck/build acceptance"
  key_links:
    - from: ui/afferent/board/board-screen.tsx
      to: afferent/react.js
      via: "Explicit client component consumes useFeedbackFeed and injected UI provider contracts only"
      pattern: "useFeedbackFeed"
    - from: scripts/generate-ui-artifacts.mjs
      to: registry/r/afferent-board.json
      via: "Stable-sorted schema payload sourced from canonical files"
      pattern: "afferent-board"
    - from: registry/r/afferent-board.json
      to: fixtures/registry-vite/src/App.tsx
      via: "Pinned local shadcn add copies the item into an OS-temporary consumer"
      pattern: "afferent-board"
---

<objective>
Deliver the first Phase 3 vertical slice: a developer installs one real source-owned feedback screen from canonical source through the generated registry into a clean packed consumer.

Purpose: Prove the package-placement, registry-resolution, canonical-source, and dirty-manifest constraints before the remaining interface surface builds on them.
Output: Approved dependency closure, UI core and board slice, deterministic generator/mirror, real shadcn fixture, and executable install/build gates.
</objective>

## Phase Goal

**As a** developer adopting Afferent, **I want to** install and restyle source-owned public and admin shadcn interfaces from one canonical registry-backed source, **so that** my users and administrators can complete the feedback lifecycle through accessible, responsive screens without replacing the headless behavior layer.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-source-owned-product-interface/03-CONTEXT.md
@.planning/phases/03-source-owned-product-interface/03-RESEARCH.md
@.planning/phases/03-source-owned-product-interface/03-UI-SPEC.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-10-SUMMARY.md
@.planning/phases/02-complete-feedback-to-changelog-workflow/02-12-SUMMARY.md
@src/react/provider.tsx
@src/react/hooks/feedback.ts
@fixtures/packed-vite-convex/package.json
@scripts/test-packed-consumer.mjs

<interfaces>
`AfferentProvider` supplies grouped host-generated bindings, auth, `AfferentWatchClient`, and a browser-local generation. `useFeedbackFeed({order, boardId?, status?, tagId?})` returns the closed loading/ready/empty/error union with `items`, `canLoadMore`, `isLoadingMore`, and `loadMore`. Copied source imports only `afferent/react.js`; UI adapter functions never become backend authority.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the packed registry happy path as a failing end-to-end contract</name>
  <files>tests/integration/registry-ui.test.mjs, tests/integration/ui-artifacts.test.mjs, fixtures/registry-vite/package.json, fixtures/registry-vite/components.json, fixtures/registry-vite/index.html, fixtures/registry-vite/tsconfig.json, fixtures/registry-vite/vite.config.ts, fixtures/registry-vite/src/index.css, fixtures/registry-vite/src/main.tsx, fixtures/registry-vite/src/App.tsx</files>
  <read_first>package.json, tests/integration/packed-artifact.test.mjs, scripts/test-packed-consumer.mjs, fixtures/packed-vite-convex/package.json, .planning/phases/03-source-owned-product-interface/03-UI-SPEC.md</read_first>
  <behavior>
    - Pack Afferent into an OS-temporary directory, install the tarball, run the repository-local `shadcn@4.11.0` binary against emitted local JSON, then typecheck and Vite-build the consumer.
    - The fixture alone owns `components.json`: new-york, neutral, Tailwind v4, cssVariables true, rsc false, blank Tailwind config, and deterministic aliases; the repository root must remain uninitialized.
    - A second generation is byte-identical, registry schemas validate, canonical/mirror SHA-256 manifests match, and tracked generated paths have no diff.
    - The installed feature renders a feedback-feed happy path from the actual `afferent/react.js` hook contract with native real links and no repository-relative imports.
  </behavior>
  <action>Create failing Node integration tests and the clean fixture template. The test must execute the actual packed/local-registry path and inspect installed source, dependency placement, declarations, typecheck, and Vite output rather than accepting fixture-local copies. Normalize temporary paths, timestamps, ports, and process IDs in any transcript. Assert no root `components.json`, no moving `npx`/latest invocation, explicit sibling item JSON addresses, and no raw Convex/router/auth/toast import in installed UI. Keep the test red on absent generator, item JSON, and canonical files.</action>
  <verify><automated>node --test tests/integration/registry-ui.test.mjs tests/integration/ui-artifacts.test.mjs</automated></verify>
  <done>The exact UI-04/06/07 and QUAL-04 happy path is executable and fails only because the canonical source/generator/dependency closure is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit the isolated package matrix and make the board registry slice installable</name>
  <files>package.json, package-lock.json, ui/afferent/core/afferent-ui-provider.tsx, ui/afferent/core/copy.ts, ui/afferent/core/icons.tsx, ui/afferent/core/navigation.tsx, ui/afferent/core/state-region.tsx, ui/afferent/core/utils.ts, ui/afferent/board/board-screen.tsx, ui/afferent/afferent.css, ui/afferent/registry.ts, scripts/generate-ui-artifacts.mjs, registry/registry.json, registry/r/afferent-ui-core.json, registry/r/afferent-board.json, examples/ui/afferent/core/afferent-ui-provider.tsx, examples/ui/afferent/core/copy.ts, examples/ui/afferent/core/icons.tsx, examples/ui/afferent/core/navigation.tsx, examples/ui/afferent/core/state-region.tsx, examples/ui/afferent/core/utils.ts, examples/ui/afferent/board/board-screen.tsx, examples/ui/afferent/afferent.css</files>
  <read_first>tests/integration/registry-ui.test.mjs, tests/integration/ui-artifacts.test.mjs, ui/afferent/registry.ts if created by the red test, src/react/index.ts, src/react/hooks/feedback.ts, .planning/phases/03-source-owned-product-interface/03-CONTEXT.md</read_first>
  <behavior>
    - The first generated items install and render a loading/ready/empty/error feedback screen through `useFeedbackFeed`, with exhaustive closed-state handling and no second cache.
    - Exact approved pins are reproducible, the core package runtime/peer surface is unchanged, and registry metadata places only the five copied-source packages into consumer runtime dependencies.
    - Generator output is stable-sorted, timestamp-free, schema-valid, and mirrors canonical bytes exactly.
  </behavior>
  <action>First verify `git diff -- package.json package-lock.json` contains only the unstaged TypeScript 6.0.3 to 7.0.2 experiment and its lock closure; stop if any unrelated package delta exists. With the locked authorization from 03-CONTEXT.md, restore only those two paths to the committed TypeScript 6 baseline. Add all ten approved exact pins atomically as root authoring/test devDependencies: class-variance-authority@0.7.1, clsx@2.1.1, tailwind-merge@3.6.0, lucide-react@1.20.0, radix-ui@1.6.0, shadcn@4.11.0, tailwindcss@4.3.0, @tailwindcss/vite@4.3.0, @playwright/test@1.59.1, and @axe-core/playwright@4.11.0. Add deterministic `ui:generate`, `ui:check`, `test:registry`, and `test:phase3` scripts. Preserve `dependencies` and `peerDependencies` byte-for-byte. The research audit and live peer already approved every SUS exact pin; install no alternative or newer version. Implement the explicit client UI provider, complete deep-merged English dictionary seam, swappable lucide icon slots, native link/navigation/current-location adapter, reusable live/error state regions, semantic shadcn-variable CSS, and a thin board screen over the actual feed hook. Implement `ui/afferent/registry.ts` as the authoritative six-item metadata catalog and a deterministic generator that emits the two current items plus byte-equal mirrors, stable file hashes, no timestamps, and explicit `./afferent-ui-core.json` sibling references. The five copied-runtime pins appear in generated registry dependency metadata; the five tool pins never do. Do not create root `components.json` or add UI exports to the `afferent` npm package.</action>
  <verify><automated>npm run ui:generate && node --test tests/integration/ui-artifacts.test.mjs && node --test tests/integration/registry-ui.test.mjs</automated></verify>
  <done>The dependency commit is based on TypeScript 6, and a clean developer can install/build the first canonical board screen through the real registry path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restore the TypeScript experiment and close deterministic board states</name>
  <files>ui/afferent/core/afferent-ui-provider.tsx, ui/afferent/core/copy.ts, ui/afferent/core/state-region.tsx, ui/afferent/board/board-screen.tsx, ui/afferent/afferent.css, scripts/generate-ui-artifacts.mjs, registry/registry.json, registry/r/afferent-ui-core.json, registry/r/afferent-board.json, examples/ui/afferent/core/afferent-ui-provider.tsx, examples/ui/afferent/core/copy.ts, examples/ui/afferent/core/state-region.tsx, examples/ui/afferent/board/board-screen.tsx, examples/ui/afferent/afferent.css, tests/integration/registry-ui.test.mjs, tests/integration/ui-artifacts.test.mjs</files>
  <read_first>03-01 task-2 commit, tests/integration/registry-ui.test.mjs, ui/afferent/board/board-screen.tsx, ui/afferent/core/afferent-ui-provider.tsx</read_first>
  <behavior>
    - Loading, empty, ready, error/retry, and load-more states use the approved product-neutral copy, semantic landmarks/headings, stable status regions, visible focus, and real post hrefs.
    - First render is deterministic: no router, `window`, time, randomness, viewport, or media read occurs during render; hook-consuming files begin with `"use client"`.
    - TypeScript 7.0.2 is restored only as an unstaged user experiment over the new Phase 3 HEAD, with a regenerated lock closure and no other package delta.
  </behavior>
  <action>After Task 2's dependency/product commit exists, restore the user's TypeScript 7.0.2 value with a scoped patch and run `npm install --package-lock-only --ignore-scripts` once. Do not stage `package.json` or `package-lock.json` in this or later Phase 3 commits. Prove their final diff against the new HEAD is solely the TypeScript experiment and lock closure. Then refine the installable board slice to exhaustively render the feed union, native shareable links, domain-specific recovery/load labels, status announcements, theme variables, and deterministic first render. Regenerate only tracked registry/mirror paths and make two consecutive runs byte-identical. The task commit must stage only the listed UI/generator/test/generated paths, leaving package files unstaged.</action>
  <verify><automated>npm run ui:generate && npm run ui:check && node --test tests/integration/registry-ui.test.mjs tests/integration/ui-artifacts.test.mjs && test "$(git diff --name-only -- package.json package-lock.json | wc -l | tr -d ' ')" = "2"</automated></verify>
  <done>The first vertical registry slice is deterministic and usable, and the user's isolated TypeScript experiment is preserved unstaged without contaminating Phase 3 commits.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                              | Description                                                                                     |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| Generated registry -> consumer filesystem             | Copied executable source and package metadata enter an adopter-owned application.               |
| Copied UI -> trusted host references                  | Browser presentation may request operations but cannot confer actor, admin, or scope authority. |
| Dirty package experiment -> Phase 3 dependency commit | Unapproved TypeScript adoption must not enter the product dependency closure.                   |

## STRIDE Threat Register

| Threat ID       | Category               | Component                     | Severity | Disposition | Mitigation Plan                                                                                                                                              |
| --------------- | ---------------------- | ----------------------------- | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-03-SC         | Tampering              | npm/shadcn dependency install | high     | mitigate    | Use the peer-approved exact ten-pin matrix, no lifecycle scripts, local binary only, registry schema validation, lockfile review, and clean fixture install. |
| T-03-01         | Tampering              | registry and mirror output    | high     | mitigate    | Stable source catalog, double generation, SHA-256 byte manifest, schema checks, and path-scoped diff gate.                                                   |
| T-03-02         | Elevation of Privilege | copied board controls         | high     | mitigate    | Consume only trusted host references through Phase 2 hooks; no identity/admin/scope prop is accepted or inferred.                                            |
| T-03-03         | Information Disclosure | TypeScript dirty experiment   | low      | accept      | Preserve the two-file experiment unstaged and prove the Phase 3 dependency commit from the committed baseline.                                               |
| </threat_model> |

<verification>
Run double generation, schema/hash/mirror checks, the real packed-tarball plus local-shadcn install, consumer typecheck/build, root package-placement audit, and final two-file dirty-diff audit.
</verification>

<success_criteria>

- UI-04 has one genuinely installable public screen over the real headless contract.
- UI-06/UI-07 have one canonical source, deterministic registry JSON, and byte-equivalent mirror.
- QUAL-04 uses a packed tarball and real pinned local shadcn CLI in a clean OS-temporary consumer.
- The complete Phase 3 package matrix is committed once from TypeScript 6, while the TypeScript 7 experiment remains unstaged.
  </success_criteria>

<output>
Create `.planning/phases/03-source-owned-product-interface/03-01-SUMMARY.md` when done.
</output>
