---
phase: 03-source-owned-product-interface
plan: "05"
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - ui/afferent/README.md
  - ui/afferent/registry.ts
  - scripts/generate-ui-artifacts.mjs
  - scripts/test-registry-consumer.mjs
  - registry/registry.json
  - registry/r/afferent-ui-core.json
  - registry/r/afferent-board.json
  - registry/r/afferent-roadmap.json
  - registry/r/afferent-changelog.json
  - registry/r/afferent-notifications.json
  - registry/r/afferent-admin.json
  - examples/ui/afferent/README.md
  - examples/ui/afferent/manifest.json
  - fixtures/registry-vite/src/App.tsx
  - tests/integration/registry-ui.test.mjs
  - tests/integration/ui-artifacts.test.mjs
  - tests/static/ui-contracts.test.ts
  - tests/ui/hydration.test.tsx
autonomous: true
requirements: [UI-04, UI-05, UI-06, UI-07, QUAL-04]
must_haves:
  truths:
    - "A clean consumer installs the packed Afferent tarball plus all six namespaced generated items through real local shadcn add, then typechecks and builds every public/admin screen."
    - "The registry catalog, every item, repository mirror, and stable manifest are reproducible from one canonical source and a second generation produces no scoped diff."
    - "Copied hook consumers are explicit route-agnostic client components with deterministic first render, complete provider/copy/icon/link/navigation/theme contracts, and no root shadcn initialization."
    - "The install/restyle guide states the client-only SEO limitation, host contrast responsibility, safe client adapter boundary, and future initial-data compatibility without introducing that API."
  artifacts:
    - path: registry/registry.json
      provides: "Complete stable six-item public registry index"
    - path: examples/ui/afferent/manifest.json
      provides: "Stable canonical-to-mirror relative path and SHA-256 evidence"
    - path: scripts/test-registry-consumer.mjs
      provides: "Clean all-item packed/local-shadcn consumer workflow"
    - path: tests/ui/hydration.test.tsx
      provides: "Deterministic server-render/hydration contract for loading/auth/empty UI"
  key_links:
    - from: ui/afferent/registry.ts
      to: registry/registry.json
      via: "Stable six-item metadata and canonical file inventory"
      pattern: "afferent-ui-core|afferent-board|afferent-roadmap|afferent-changelog|afferent-notifications|afferent-admin"
    - from: registry/r/afferent-admin.json
      to: fixtures/registry-vite/src/App.tsx
      via: "Real shadcn add installs item and explicit sibling dependencies before build"
      pattern: "afferent-admin"
    - from: ui/afferent/README.md
      to: ui/afferent/core/afferent-ui-provider.tsx
      via: "Documents host-owned client adapter, dictionary, icons, links, theming, and client-only boundary"
      pattern: "AfferentUiProvider"
---

<objective>
Close the developer adoption story across all public/admin registry items, the repository mirror, and deterministic client-boundary documentation.

Purpose: Ensure the interface consumers install is exactly the source they can inspect and restyle, not a repository-only implementation.
Output: Complete six-item registry, canonical/mirror manifest, all-item clean fixture gate, hydration/static contracts, and source adoption guide.
</objective>

## Phase Goal

**As a** developer adopting Afferent, **I want to** install and restyle source-owned public and admin shadcn interfaces from one canonical registry-backed source, **so that** my users and administrators can complete the feedback lifecycle through accessible, responsive screens without replacing the headless behavior layer.

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-source-owned-product-interface/03-CONTEXT.md
@.planning/phases/03-source-owned-product-interface/03-RESEARCH.md
@.planning/phases/03-source-owned-product-interface/03-UI-SPEC.md
@.planning/phases/03-source-owned-product-interface/03-01-SUMMARY.md
@.planning/phases/03-source-owned-product-interface/03-02-SUMMARY.md
@.planning/phases/03-source-owned-product-interface/03-03-SUMMARY.md
@.planning/phases/03-source-owned-product-interface/03-04-SUMMARY.md
@scripts/test-packed-consumer.mjs
@fixtures/registry-vite/components.json

<interfaces>
The public registry has exactly six namespaced items: `afferent-ui-core`, `afferent-board`, `afferent-roadmap`, `afferent-changelog`, `afferent-notifications`, and `afferent-admin`. Feature items use explicit relative generated JSON addresses for sibling dependencies. The root package exports remain unchanged; consumers own copied source under their configured aliases.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the all-item distribution and hydration contract fail first</name>
  <files>tests/integration/registry-ui.test.mjs, tests/integration/ui-artifacts.test.mjs, tests/static/ui-contracts.test.ts, tests/ui/hydration.test.tsx, fixtures/registry-vite/src/App.tsx</files>
  <read_first>scripts/generate-ui-artifacts.mjs, registry/registry.json, fixtures/registry-vite/components.json, ui/afferent/core/afferent-ui-provider.tsx, src/react/index.ts</read_first>
  <behavior>
    - A fresh external fixture installs tarball first, then all five feature item JSONs (which resolve core explicitly), compiles every screen against supported versions, and Vite-builds without source-relative paths.
    - Registry/catalog/file arrays and hashes are stable-sorted; generation twice is byte-identical; canonical and mirror bytes match for every declared path; timestamps are absent.
    - Static gates reject root `components.json`, package runtime/peer UI deps, direct Convex/auth/router/toast/raw-HTML/browser-render reads, missing client directives, generic item names, and tool packages in registry dependencies.
    - Server-render plus hydration of loading, unauthenticated, unsupported, empty, and ready shells has equal first DOM and no recoverable hydration error.
  </behavior>
  <action>Expand the integration gate from one board item to all public/admin items and add focused static/hydration tests. The fixture App must import and render every installed screen from consumer aliases, not canonical or mirror repository paths. Assert exact five registry runtime packages/versions, exact absence of the five build/evidence tools in item dependency metadata, all six namespaced names, explicit sibling JSON addresses, mirror manifest completeness, and no root shadcn config. Hydration tests must exercise deterministic UI provider/copy/icon/navigation/current-location props and fail on time/random/global/media/render branching.</action>
  <verify><automated>node --test tests/integration/registry-ui.test.mjs tests/integration/ui-artifacts.test.mjs && npm exec -- vitest run --config vitest.react.config.ts tests/static/ui-contracts.test.ts tests/ui/hydration.test.tsx</automated></verify>
  <done>Every distribution, source-equivalence, client-boundary, and hydration invariant is executable and red on the incomplete catalog/generator/guide.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize the six-item catalog, stable mirror, and adoption contract</name>
  <files>ui/afferent/README.md, ui/afferent/registry.ts, scripts/generate-ui-artifacts.mjs, registry/registry.json, registry/r/afferent-ui-core.json, registry/r/afferent-board.json, registry/r/afferent-roadmap.json, registry/r/afferent-changelog.json, registry/r/afferent-notifications.json, registry/r/afferent-admin.json, examples/ui/afferent/README.md, examples/ui/afferent/manifest.json, tests/integration/ui-artifacts.test.mjs, tests/static/ui-contracts.test.ts, tests/ui/hydration.test.tsx</files>
  <read_first>tests/integration/ui-artifacts.test.mjs, tests/static/ui-contracts.test.ts, tests/ui/hydration.test.tsx, ui/afferent/registry.ts, .planning/phases/03-source-owned-product-interface/03-UI-SPEC.md</read_first>
  <action>Complete the authoritative catalog and generator for all six items. Stable-sort items, files, dependencies, registryDependencies, and JSON keys; emit official-schema catalog/item JSON without timestamps; use explicit relative sibling addresses; copy every canonical source byte to the same relative mirror path; and emit a sorted relative-path/SHA-256 manifest. `ui:check` must generate twice into tracked paths, validate schemas and byte/hash equality, and use a path-scoped diff so the intentionally dirty package files do not make it lie. Add a canonical README mirrored byte-for-byte that documents installation, restyling through standard shadcn variables, deep-merged per-screen copy, icon slots, real link/href/navigation/current-location adapter, safe host client module, deterministic client boundary, client-only SEO limitation, host responsibility for contrast/focus under overrides, and additive future initial-data possibility without an API today. Keep localization/router/SSR data/drag behavior out of scope.</action>
  <verify><automated>npm run ui:generate && npm run ui:check && node --test tests/integration/ui-artifacts.test.mjs && npm exec -- vitest run --config vitest.react.config.ts tests/static/ui-contracts.test.ts tests/ui/hydration.test.tsx</automated></verify>
  <done>All registry and repository sources are reproducible from one canonical tree, with the complete client/theme/copy/navigation limitation contract documented and enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove every item through the real packed local-shadcn consumer</name>
  <files>scripts/test-registry-consumer.mjs, fixtures/registry-vite/src/App.tsx, tests/integration/registry-ui.test.mjs</files>
  <read_first>tests/integration/registry-ui.test.mjs, fixtures/registry-vite/package.json, fixtures/registry-vite/components.json, scripts/test-packed-consumer.mjs, registry/registry.json</read_first>
  <action>Factor a deterministic external-consumer runner that materializes the fixture outside the repository, packs/installs Afferent first, invokes only `node_modules/.bin/shadcn` from the pinned root dependency on each generated feature JSON, lets explicit core dependencies resolve, and then typechecks/builds the installed source against exact fixture versions. Inspect the resulting package manifest/files/import graph and normalized transcript to prove the five copied runtime packages were installed, the five root/fixture tools did not become UI runtime metadata, all imports resolve inside the consumer, and no canonical/mirror repository path was used. Render each public/admin screen against controlled generated refs in the fixture smoke build. Do not run a real Convex deployment; Phase 4 owns real-backend browser workflows.</action>
  <verify><automated>node --test tests/integration/registry-ui.test.mjs && npm run build && npm run test:phase2</automated></verify>
  <done>A clean adopter can install and build the exact complete source-owned interface through both supported distribution paths without weakening the existing packed package.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                         | Description                                                                                            |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------ |
| Canonical metadata/generator -> static registry  | Build output must not drift, inject moving dependencies, or resolve sibling names to another registry. |
| Server/RSC-capable host -> copied client subtree | Function props must originate in a host client module and first render must remain deterministic.      |

## STRIDE Threat Register

| Threat ID       | Category               | Component                               | Severity | Disposition | Mitigation Plan                                                                                                              |
| --------------- | ---------------------- | --------------------------------------- | -------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-03-16         | Tampering              | six-item registry                       | high     | mitigate    | Official schemas, explicit sibling addresses, exact pins, stable keys/order/hashes, double generation, and real CLI install. |
| T-03-17         | Information Disclosure | repository-relative consumer resolution | high     | mitigate    | OS-temporary fixture, tarball-first install, path scan, and realpath containment checks.                                     |
| T-03-18         | Tampering              | hydration/client adapter                | medium   | mitigate    | Static global-import bans plus server-render/hydrate equality and recoverable-error assertions.                              |
| T-03-19         | Repudiation            | generated mirror provenance             | low      | accept      | Stable manifest records source-relative paths/hashes; no timestamp is used as authority.                                     |
| </threat_model> |

<verification>
Run double generation, schema/hash/mirror/static/hydration gates, real all-item shadcn installation, clean consumer typecheck/build, root build/Phase 2 regression, and dirty package preservation checks.
</verification>

<success_criteria>

- UI-04/UI-05 install every complete public/admin screen.
- UI-06/UI-07 prove both distribution paths come from the same canonical bytes.
- QUAL-04 proves the real tarball and local shadcn workflow in a clean consumer across supported exact versions.
- Client-boundary, theme, copy, navigation, hydration, and documented SEO limitations are closed and testable.
  </success_criteria>

<output>
Create `.planning/phases/03-source-owned-product-interface/03-05-SUMMARY.md` when done.
</output>
