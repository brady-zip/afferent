---
phase: 04-hosted-production-release
plan: "06"
type: execute
wave: 6
depends_on: ["04-05"]
files_modified:
  - playwright.phase4.config.ts
  - scripts/dev-demo.mjs
  - scripts/serve-demo.mjs
  - scripts/test-demo.mjs
  - tests/e2e/demo.spec.ts
  - tests/e2e/sandbox-isolation.spec.ts
  - tests/e2e/sandbox-lifecycle.spec.ts
  - tests/e2e/demo-accessibility.spec.ts
  - tests/integration/phase4-gate.test.mjs
  - docs/accessibility/phase-4/README.md
  - package.json
autonomous: true
requirements:
  - DEMO-02
  - DEMO-03
  - DEMO-04
  - DEMO-05
  - DEMO-06
  - DEMO-07
  - DEMO-08
  - QUAL-05
  - QUAL-06
must_haves:
  truths:
    - "One documented command launches the canonical packed-and-registry-installed app against a real anonymous local Convex backend without a Convex account, deploy key, or committed auth material."
    - "The local app completes the full signed-out showcase and authenticated admin journey against real Convex, not an in-memory substitute."
    - "Two distinct Convex Auth password accounts with colliding data cannot observe or mutate one another through any read, write, search, count, seed, reset, quota, expiry, cleanup, or reactive path."
    - "The immutable showcase rejects browser writes while each authenticated sandbox grants admin capability only because the trusted host wrapper derived it."
    - "Keyboard, responsive, and automated axe checks cover all major journeys and lifecycle states with durable normalized evidence across desktop, tablet, and mobile."
    - "One named Phase 4 aggregate fails if a required browser suite is skipped, filtered out, or run against the wrong artifact/backend."
  artifacts:
    - path: "scripts/dev-demo.mjs"
      provides: "Credential-free one-command anonymous Convex and Vite developer experience"
    - path: "scripts/test-demo.mjs"
      provides: "Local real-Convex acceptance orchestration with anti-skip accounting"
    - path: "tests/e2e/demo.spec.ts"
      provides: "Canonical signed-out showcase and authenticated admin evaluator journey"
    - path: "tests/e2e/sandbox-isolation.spec.ts"
      provides: "Two-user colliding-data scope matrix"
    - path: "tests/e2e/sandbox-lifecycle.spec.ts"
      provides: "Reset, expiry, quota, cleanup, and stale-reactivity proof"
    - path: "tests/e2e/demo-accessibility.spec.ts"
      provides: "WCAG-oriented keyboard, responsive, and axe regression suite"
    - path: "tests/integration/phase4-gate.test.mjs"
      provides: "Required-suite manifest and no-skip enforcement"
  key_links:
    - from: "scripts/test-demo.mjs"
      to: "playwright.phase4.config.ts"
      via: "prepared candidate and owned real local Convex runtime are injected explicitly"
      pattern: "PHASE4_"
    - from: "tests/e2e/sandbox-isolation.spec.ts"
      to: "example/convex/sandbox.ts"
      via: "two browser contexts exercise host-derived authority"
      pattern: "storageState|browser.newContext"
    - from: "tests/e2e/demo-accessibility.spec.ts"
      to: "docs/accessibility/phase-4/README.md"
      via: "normalized evidence records tested state and disposition"
      pattern: "axe|keyboard"
---

<objective>
Ship the clone-and-run local demo and prove it against real anonymous Convex with two authenticated users, complete journeys, adversarial isolation, lifecycle maintenance, and accessibility gates.

Purpose: DEMO-08, QUAL-05/06, and the preserved DEMO-02..07 behaviors require a credential-free evaluator path whose deterministic browser evidence catches auth, scope, reactivity, cleanup, bootstrap, and UI regressions that unit substitutes cannot.
Output: One-command local dev launcher, real-Convex Playwright harness, two-user suites, accessibility evidence, and an anti-skip aggregate.

Decisions: D-15, D-16, D-17, D-18, D-19.
</objective>

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hosted-production-release/04-CONTEXT.md
@.planning/phases/04-hosted-production-release/04-SCOPE-PIVOT.md
@.planning/phases/04-hosted-production-release/04-UI-SPEC.md
@.planning/phases/04-hosted-production-release/04-05-SUMMARY.md
@example/convex/auth.ts
@tests/integration/scope-matrix.test.ts
@scripts/prepare-demo-consumer.mjs

<interfaces>
Harness contract created here:
- `npm run dev:demo` prepares the exact artifact-installed candidate, generates ephemeral auth configuration, starts a real anonymous local Convex backend, seeds showcase data, starts Vite, prints the local URL, and owns teardown.
- The acceptance harness reuses the production-shaped local bootstrap but injects lifecycle controls only into its temporary candidate.
- There is no remote mode, public URL, cloud deployment, Vercel path, or Convex project/deploy-key requirement.
- Test setup creates two distinct Convex Auth Password accounts through the app's public authentication flow.
- Each required suite emits a machine-readable completion marker consumed by the aggregate gate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the one-command local demo and deterministic real-Convex browser harness</name>
  <files>playwright.phase4.config.ts, scripts/dev-demo.mjs, scripts/serve-demo.mjs, scripts/test-demo.mjs, tests/integration/phase4-gate.test.mjs, package.json</files>
  <read_first>
    - scripts/prepare-demo-consumer.mjs
    - example/convex/auth.ts
    - example/convex/afferent.ts
    - package.json
    - .planning/research/STACK.md
    - .planning/phases/04-hosted-production-release/04-CONTEXT.md
  </read_first>
  <behavior>
    - `npm run dev:demo` prepares the release candidate, generates per-run JWT/JWKS and site configuration, starts a real isolated anonymous Convex backend and Vite app, seeds the showcase, reports one stable local URL, and tears down only owned processes.
    - The user-facing launcher requires no Convex login, project, deploy key, checked-in `.env.local`, or hidden cloud fallback and never injects test-only lifecycle controls.
    - Missing Node/npm/Convex/browser prerequisites, occupied resources, auth setup, backend readiness, or seed/build failure emits a specific recovery action and exits non-zero.
    - Missing browsers, auth setup, backend readiness, or any required suite causes failure rather than skip.
    - A manifest identifies required projects/specs and verifies each produced a completion marker for the exact artifact digest and target.
    - Static gates reject remote/HTTPS/Vercel branches, public lifecycle controls, committed key material or deploy keys, tracked generated `.env.local`, mock Convex imports, and unsafe broad process termination.
  </behavior>
  <action>Rename the partial hosted-named scripts/specs to local-demo names and delete the remote branch rather than leaving an unsupported half-path. Extract shared owned-process/bootstrap helpers so `dev:demo` and `test:e2e:phase4` use the same anonymous Convex, ephemeral auth, seed, and candidate preparation contract while the test runner alone injects `tests/e2e/fixtures/phase4Test.ts` into its generated candidate. Adapt the existing Convex Auth Password fixture rather than creating a fake auth route. Use allocated ports, explicit state ownership, readiness probes, bounded timeouts, signal-safe teardown, artifact/backend identity markers, and actionable prerequisite errors. Configure desktop Chromium plus responsive mobile/tablet projects required by the UI spec. Add static/runtime gates for key material, generated environment files, forbidden production controls, `.skip`, `.fixme`, empty selection, missing project/spec markers, mocked Convex imports, or digest mismatch. Expose `dev:demo` and local-only `test:e2e:phase4`; remove `test:e2e:phase4:remote` and `--base-url`.</action>
  <verify>
    <automated>npm exec -- vitest run tests/integration/phase4-gate.test.mjs &amp;&amp; npm run test:e2e:phase4 -- --list</automated>
  </verify>
  <done>A clean clone has one credential-free local demo command, and the browser harness deterministically targets the exact release candidate and real anonymous backend without missing coverage or shipping test authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise the full evaluator journey and adversarial two-user scope matrix</name>
  <files>tests/e2e/demo.spec.ts, tests/e2e/sandbox-isolation.spec.ts, tests/e2e/sandbox-lifecycle.spec.ts</files>
  <read_first>
    - .planning/phases/04-hosted-production-release/04-UI-SPEC.md
    - example/src/App.tsx
    - example/convex/showcase.ts
    - example/convex/sandbox.ts
    - example/convex/sandboxLifecycle.ts
    - example/convex/sandboxQuotas.ts
    - example/convex/sandboxCleanup.ts
    - tests/integration/scope-matrix.test.ts
  </read_first>
  <behavior>
    - Signed-out local showcase browsing covers board, post, roadmap, and linked published changelog and cannot discover an enabled write action.
    - Each account signs up through Convex Auth, creates/votes/comments, uses search/count views, moderates, changes status/boards, and publishes a linked changelog entry.
    - Accounts intentionally use identical titles, slugs, search terms, and actor-local sequences; neither direct navigation nor reactive updates reveals the other's records/counts/results.
    - Reset produces a complete new deterministic baseline, stops old reactive content, cleans the retired generation, and preserves the other account.
    - Quota rejection, seven-day expiry via controlled server test clock/internal fixture boundary, cleanup retry, and stale scheduled work remain isolated and recoverable.
  </behavior>
  <action>Complete the interrupted page-object-light Playwright journeys against visible user behavior and stable accessible roles. Use two independent browser contexts and two real local Password accounts. Cover every operation family and maintenance path named in DEMO-02..08/QUAL-06, including colliding search/count values and a live page held across the other user's reset. Permit controlled time/failure advancement only through a test-runner-injected internal server fixture that is absent from `example/convex`, the packed package, and the user-facing demo command and still resolves targets server-side; never add public scope/generation hooks. Fix real lifecycle defects rather than loosening timeouts, and give each test run unique account data so retries start cleanly. Record per-suite completion markers only after all assertions pass.</action>
  <verify>
    <automated>npm run test:e2e:phase4 -- --project=chromium tests/e2e/demo.spec.ts tests/e2e/sandbox-isolation.spec.ts tests/e2e/sandbox-lifecycle.spec.ts</automated>
  </verify>
  <done>The complete real application journey and every scope-sensitive lifecycle path pass with two colliding authenticated visitors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate WCAG-oriented keyboard, responsive, and axe behavior with durable evidence</name>
  <files>tests/e2e/demo-accessibility.spec.ts, docs/accessibility/phase-4/README.md, scripts/test-demo.mjs, package.json</files>
  <read_first>
    - .planning/phases/04-hosted-production-release/04-UI-SPEC.md
    - example/src/App.tsx
    - ui/afferent
    - docs/accessibility
    - tests/e2e/demo.spec.ts
  </read_first>
  <behavior>
    - Keyboard-only flows cover environment navigation, authentication, post creation, voting, commenting, admin status change, changelog publication, and reset cancellation/confirmation.
    - Focus enters dialogs, remains trapped, returns to the trigger, and moves to the correct heading/status after route or lifecycle transitions.
    - Chromium desktop, tablet, and mobile projects have no serious/critical axe violations or horizontal overflow in required states.
    - Reduced-motion and zoom/reflow checks preserve information and controls; lifecycle live regions do not over-announce.
    - Evidence names the artifact digest, backend kind, viewports, states, tool versions, violations, and explicit dispositions.
  </behavior>
  <action>Replace the intentional accessibility stub with deterministic journeys and normalized evidence generation. Exercise the approved UI-spec state matrix rather than only the happy path, including signed-out, preparing, quota, reset, error, and ready showcase/admin pages. Treat serious/critical axe results, focus failures, keyboard traps, overflow, missing completion markers, or unreviewed lower-severity findings as gate failures. Store a concise checked-in evidence index/disposition document, not volatile raw screenshots/traces; retain raw CI artifacts out of tree. Make `verify:phase4` run package, maintenance, installed local demo, real-Convex E2E, and accessibility aggregates in order.</action>
  <verify>
    <automated>npm run verify:phase4</automated>
  </verify>
  <done>Phase 4 has one no-skip release gate with durable real-browser isolation and WCAG-oriented evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test client -> real backend | Browser tests must exercise production-shaped functions and real Convex Auth, not privileged shortcuts. |
| Account A -> account B | Every colliding data and lifecycle path must remain isolated under reactivity. |
| Test-only controls -> local demo | Time/failure controls must be internal, runner-injected, and absent from the committed example and user-facing launcher. |
| Local bootstrap -> developer machine | Generated keys, ports, backend state, and process signals must remain ephemeral and owned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-24 | Spoofing | test authentication | critical | mitigate | Create distinct real Convex Auth accounts through the public app flow. |
| T-04-25 | Information Disclosure | reactive/query scope | critical | mitigate | Exercise colliding two-user reads, searches, counts, and live subscriptions. |
| T-04-26 | Elevation of Privilege | test controls | critical | mitigate | Compile controls only into test deployments and derive all targets server-side. |
| T-04-27 | Repudiation | skipped E2E | high | mitigate | Require artifact/target-bound completion markers for every declared suite/project. |
| T-04-28 | Denial of Service | hung local services | medium | mitigate | Use readiness deadlines, owned process groups, and bounded cleanup. |
| T-04-44 | Information Disclosure | local auth bootstrap | critical | mitigate | Generate JWT/JWKS per run and reject committed keys, deploy keys, or generated environment files. |
</threat_model>

<verification>
`npm run verify:phase4` is the local release gate. It must prove package shape, all scoped maintenance, the credential-free clone-and-run launcher, installed canonical UI, two-user real-Convex behavior, and accessibility without skips. No remote mode exists.
</verification>

<success_criteria>

- QUAL-05 runs complete Playwright journeys against real Convex.
- QUAL-06 proves all scope-sensitive paths with two colliding authenticated users.
- DEMO-02..08 are exercised as visible behavior, not inferred from units.
- DEMO-08 starts without a Convex account/deploy key and commits no auth material.
- WCAG 2.2 AA-oriented evidence is durable and release-blocking.

</success_criteria>

<output>
Create `.planning/phases/04-hosted-production-release/04-06-SUMMARY.md` when done.
</output>
