---
phase: 04-hosted-production-release
plan: "06"
type: execute
wave: 6
depends_on: ["04-05"]
files_modified:
  - playwright.phase4.config.ts
  - scripts/serve-hosted-demo.mjs
  - scripts/test-hosted-demo.mjs
  - tests/e2e/hosted-demo.spec.ts
  - tests/e2e/sandbox-isolation.spec.ts
  - tests/e2e/sandbox-lifecycle.spec.ts
  - tests/e2e/hosted-accessibility.spec.ts
  - tests/integration/phase4-gate.test.mjs
  - docs/accessibility/phase-4/README.md
  - package.json
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04
  - DEMO-05
  - DEMO-06
  - DEMO-07
  - QUAL-05
  - QUAL-06
must_haves:
  truths:
    - "The canonical packed-and-registry-installed app completes the full public and admin journey against a real Convex backend, not an in-memory substitute."
    - "Two distinct Convex Auth password accounts with colliding data cannot observe or mutate one another through any read, write, search, count, seed, reset, quota, expiry, cleanup, or reactive path."
    - "The immutable showcase rejects browser writes while each authenticated sandbox grants admin capability only because the trusted host wrapper derived it."
    - "Keyboard, responsive, and automated axe checks cover all major journeys and lifecycle states with durable normalized evidence."
    - "One named Phase 4 aggregate fails if a required browser suite is skipped, filtered out, or run against the wrong artifact/backend."
  artifacts:
    - path: "scripts/test-hosted-demo.mjs"
      provides: "Real-Convex local and remote-target orchestration with anti-skip accounting"
    - path: "tests/e2e/hosted-demo.spec.ts"
      provides: "Canonical public and admin evaluator journey"
    - path: "tests/e2e/sandbox-isolation.spec.ts"
      provides: "Two-user colliding-data scope matrix"
    - path: "tests/e2e/sandbox-lifecycle.spec.ts"
      provides: "Reset, expiry, quota, cleanup, and stale-reactivity proof"
    - path: "tests/e2e/hosted-accessibility.spec.ts"
      provides: "WCAG-oriented keyboard, responsive, and axe regression suite"
    - path: "tests/integration/phase4-gate.test.mjs"
      provides: "Required-suite manifest and no-skip enforcement"
  key_links:
    - from: "scripts/test-hosted-demo.mjs"
      to: "playwright.phase4.config.ts"
      via: "prepared candidate and real Convex runtime/remote URL are injected explicitly"
      pattern: "PHASE4_"
    - from: "tests/e2e/sandbox-isolation.spec.ts"
      to: "example/convex/sandbox.ts"
      via: "two browser contexts exercise host-derived authority"
      pattern: "storageState|browser.newContext"
    - from: "tests/e2e/hosted-accessibility.spec.ts"
      to: "docs/accessibility/phase-4/README.md"
      via: "normalized evidence records tested state and disposition"
      pattern: "axe|keyboard"
---

<objective>
Prove the release-shaped hosted experience against real Convex with two authenticated users, complete journeys, adversarial isolation, lifecycle maintenance, and accessibility gates.

Purpose: QUAL-05/06 and DEMO-01..07 are release criteria; deterministic browser evidence must catch auth, scope, reactivity, cleanup, and UI regressions that unit substitutes cannot.
Output: Real-Convex Playwright harness, two-user suites, accessibility evidence, and an anti-skip aggregate.

Decisions: D-15, D-16, D-17, D-18, D-19.
</objective>

<execution_context>
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/workflows/execute-plan.md
@/Users/bradywatkinson/dev/afferent/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hosted-production-release/04-CONTEXT.md
@.planning/phases/04-hosted-production-release/04-UI-SPEC.md
@.planning/phases/04-hosted-production-release/04-05-SUMMARY.md
@example/convex/auth.ts
@tests/integration/scope-matrix.test.ts
@scripts/prepare-demo-consumer.mjs

<interfaces>
Harness contract created here:
- Local mode starts a real local Convex deployment plus the prepared artifact-installed Vite candidate.
- Remote mode takes explicit preview/production base URLs and runs the same black-box journeys without local mutation shortcuts.
- Test setup creates two distinct Convex Auth Password accounts through the app's public authentication flow.
- Each required suite emits a machine-readable completion marker consumed by the aggregate gate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build a deterministic real-Convex browser harness with anti-skip accounting</name>
  <files>playwright.phase4.config.ts, scripts/serve-hosted-demo.mjs, scripts/test-hosted-demo.mjs, tests/integration/phase4-gate.test.mjs, package.json</files>
  <read_first>
    - scripts/prepare-demo-consumer.mjs
    - example/convex/auth.ts
    - example/convex/afferent.ts
    - package.json
    - .planning/research/STACK.md
    - .planning/phases/04-hosted-production-release/04-CONTEXT.md
  </read_first>
  <behavior>
    - Local orchestration prepares the release candidate, starts a real isolated Convex backend and built Vite app, waits for readiness, and tears down only owned processes.
    - Remote orchestration requires an explicit HTTPS base URL and never falls back silently to local mocks.
    - Missing browsers, auth setup, backend readiness, or any required suite causes failure rather than skip.
    - A manifest identifies required projects/specs and verifies each produced a completion marker for the exact artifact digest and target.
  </behavior>
  <action>Pin/revalidate official Playwright and axe dependencies before install. Adapt the existing Convex Auth Password fixture into the canonical example rather than creating a fake auth route. Implement process orchestration with allocated ports, explicit temp/state ownership, readiness probes, bounded timeouts, safe teardown, and artifact/backend identity markers. Configure desktop Chromium plus responsive mobile/tablet projects required by the UI spec. Add a static/runtime anti-skip test that rejects `.skip`, `.fixme`, empty grep results, missing project/spec markers, mocked Convex imports, or a digest mismatch. Expose `test:e2e:phase4` for local and `test:e2e:phase4:remote -- --base-url` for Plan 04-09.</action>
  <verify>
    <automated>npm exec -- vitest run tests/integration/phase4-gate.test.mjs &amp;&amp; npm run test:e2e:phase4 -- --list</automated>
  </verify>
  <done>The browser harness deterministically targets the exact release candidate and real backend, and cannot report green with missing required coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise the full evaluator journey and adversarial two-user scope matrix</name>
  <files>tests/e2e/hosted-demo.spec.ts, tests/e2e/sandbox-isolation.spec.ts, tests/e2e/sandbox-lifecycle.spec.ts</files>
  <read_first>
    - .planning/phases/04-hosted-production-release/04-UI-SPEC.md
    - example/src/App.tsx
    - example/convex/showcase.ts
    - example/convex/sandbox.ts
    - example/convex/sandboxLifecycle.ts
    - example/convex/sandboxQuotas.ts
    - example/convex/sandboxCleanup.ts
    - tests/integration/scope-matrix.test.ts
  </read_first>
  <behavior>
    - Anonymous showcase browsing covers board, post, roadmap, and linked published changelog and cannot discover an enabled write action.
    - Each account signs up through Convex Auth, creates/votes/comments, uses search/count views, moderates, changes status/boards, and publishes a linked changelog entry.
    - Accounts intentionally use identical titles, slugs, search terms, and actor-local sequences; neither direct navigation nor reactive updates reveals the other's records/counts/results.
    - Reset produces a complete new deterministic baseline, stops old reactive content, cleans the retired generation, and preserves the other account.
    - Quota rejection, seven-day expiry via controlled server test clock/internal fixture boundary, cleanup retry, and stale scheduled work remain isolated and recoverable.
  </behavior>
  <action>Implement page-object-light Playwright journeys against visible user behavior and stable accessible roles. Use two independent browser contexts and two real Password accounts. Cover every operation family and maintenance path named in DEMO-01..07/QUAL-06, including colliding search/count values and a live page held across the other user's reset. Permit controlled time/failure advancement only through a test-build internal server fixture that is absent from production deployment and still resolves targets server-side; never add public scope/generation hooks. Record per-suite completion markers only after all assertions pass.</action>
  <verify>
    <automated>npm run test:e2e:phase4 -- --project=chromium tests/e2e/hosted-demo.spec.ts tests/e2e/sandbox-isolation.spec.ts tests/e2e/sandbox-lifecycle.spec.ts</automated>
  </verify>
  <done>The complete real application journey and every scope-sensitive lifecycle path pass with two colliding authenticated visitors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate WCAG-oriented keyboard, responsive, and axe behavior with durable evidence</name>
  <files>tests/e2e/hosted-accessibility.spec.ts, docs/accessibility/phase-4/README.md, scripts/test-hosted-demo.mjs, package.json</files>
  <read_first>
    - .planning/phases/04-hosted-production-release/04-UI-SPEC.md
    - example/src/App.tsx
    - ui/afferent
    - docs/accessibility
    - tests/e2e/hosted-demo.spec.ts
  </read_first>
  <behavior>
    - Keyboard-only flows cover environment navigation, authentication, post creation, voting, commenting, admin status change, changelog publication, and reset cancellation/confirmation.
    - Focus enters dialogs, remains trapped, returns to the trigger, and moves to the correct heading/status after route or lifecycle transitions.
    - Chromium desktop, tablet, and mobile projects have no serious/critical axe violations or horizontal overflow in required states.
    - Reduced-motion and zoom/reflow checks preserve information and controls; lifecycle live regions do not over-announce.
    - Evidence names the artifact digest, backend kind, viewports, states, tool versions, violations, and explicit dispositions.
  </behavior>
  <action>Add deterministic accessibility journeys and normalized evidence generation. Exercise the approved UI-spec state matrix rather than only the happy path, including signed-out, preparing, quota, reset, error, and ready public/admin pages. Treat serious/critical axe results, focus failures, keyboard traps, overflow, missing completion markers, or unreviewed lower-severity findings as gate failures. Store a concise checked-in evidence index/disposition document, not volatile raw screenshots/traces; retain raw CI artifacts out of tree. Make `verify:phase4` run package, maintenance, installed demo, real-Convex E2E, and accessibility aggregates in order.</action>
  <verify>
    <automated>npm run verify:phase4</automated>
  </verify>
  <done>Phase 4 has one no-skip release gate with durable real-browser isolation and WCAG-oriented evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test client -> real backend | Browser tests must exercise production-shaped functions and real Convex Auth, not privileged shortcuts. |
| Account A -> account B | Every colliding data and lifecycle path must remain isolated under reactivity. |
| Test-only controls -> production | Time/failure controls must be internal, build-gated, and absent from production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-24 | Spoofing | test authentication | critical | mitigate | Create distinct real Convex Auth accounts through the public app flow. |
| T-04-25 | Information Disclosure | reactive/query scope | critical | mitigate | Exercise colliding two-user reads, searches, counts, and live subscriptions. |
| T-04-26 | Elevation of Privilege | test controls | critical | mitigate | Compile controls only into test deployments and derive all targets server-side. |
| T-04-27 | Repudiation | skipped E2E | high | mitigate | Require artifact/target-bound completion markers for every declared suite/project. |
| T-04-28 | Denial of Service | hung local services | medium | mitigate | Use readiness deadlines, owned process groups, and bounded cleanup. |
</threat_model>

<verification>
`npm run verify:phase4` is the local release gate. It must prove package shape, all scoped maintenance, installed canonical UI, two-user real-Convex behavior, and accessibility without skips; the same remote mode is reused for preview and production.
</verification>

<success_criteria>

- QUAL-05 runs complete Playwright journeys against real Convex.
- QUAL-06 proves all scope-sensitive paths with two colliding authenticated users.
- DEMO-01..07 are exercised as visible behavior, not inferred from units.
- WCAG 2.2 AA-oriented evidence is durable and release-blocking.

</success_criteria>

<output>
Create `.planning/phases/04-hosted-production-release/04-06-SUMMARY.md` when done.
</output>
