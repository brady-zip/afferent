---
phase: 01-secure-installable-feedback-board
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/walking-skeleton.test.mjs
autonomous: false
requirements:
  - QUAL-03
  - QUAL-10
must_haves:
  truths:
    - "The first executable specification fails because no packed Afferent consumer path exists yet."
    - "Every dependency version flagged by research is reviewed before installation."
    - "The approved dependency set contains no package classified as SLOP and preserves fixture-only provider isolation."
  artifacts:
    - path: "tests/integration/walking-skeleton.test.mjs"
      provides: "Failing external-consumer acceptance test for the walking skeleton"
      contains: "packed Afferent artifact"
  key_links:
    - from: "tests/integration/walking-skeleton.test.mjs"
      to: "fixtures/packed-vite-convex"
      via: "temporary clean-consumer materialization contract"
      pattern: "packed-vite-convex"
---

## Phase Goal

**As a** Convex developer, **I want to** install Afferent with my auth provider, **so that** users can safely run multi-board feedback.

<objective>
Define the red end-to-end acceptance test and clear the package-legitimacy gate before any install.

Purpose: Make the clean packed consumer—not workspace source—the first proof, and prevent recently published dependencies from entering the repository without review.
Output: One deliberately failing walking-skeleton test and an approved exact dependency matrix.

Decisions: D-01, D-02, D-05, D-07, D-09.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-installable-feedback-board/01-CONTEXT.md
@.planning/phases/01-secure-installable-feedback-board/01-RESEARCH.md
@.planning/phases/01-secure-installable-feedback-board/01-PATTERNS.md
@.planning/phases/01-secure-installable-feedback-board/01-SKELETON.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing packed-consumer walking-skeleton test</name>
  <files>tests/integration/walking-skeleton.test.mjs</files>
  <read_first>
    - .planning/phases/01-secure-installable-feedback-board/01-SKELETON.md
    - .planning/phases/01-secure-installable-feedback-board/01-RESEARCH.md
    - AGENTS.md
  </read_first>
  <behavior>
    - The test requires a tarball produced by `npm pack`, installed into a new temporary directory outside the repository.
    - The clean consumer must register the component, mount a trusted fixed-scope wrapper, create one board/post, read it back, and complete one fixture interaction.
    - Resolution under the repository root, a missing Apache-2.0 license, or any repository-relative import fails the test.
  </behavior>
  <action>Create a Node test-runner specification that copies only the committed `fixtures/packed-vite-convex` template into a fresh OS temporary directory, invokes the future package-gate script, and asserts the board/post write-read and Vite interaction transcript. Keep the test executable before dependencies exist by using Node built-ins for orchestration. Its current red state must be caused by the absent package/fixture implementation, not syntax errors or an unconditional failure. The browser-to-host call shape must contain intent fields only, preserving D-02, D-05, D-07, and D-09.</action>
  <verify>
    <automated>! node --test tests/integration/walking-skeleton.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The test parses and reaches the missing-implementation assertion.
    - Failure names the absent packed-artifact board/post path.
    - The test contract checks tarball-only resolution and `LICENSE` presence.
  </acceptance_criteria>
  <done>The executable end-to-end specification is red for the intended missing capability and defines the smallest full-stack proof.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve the exact package matrix before installation</name>
  <files>package.json</files>
  <read_first>
    - .planning/phases/01-secure-installable-feedback-board/01-RESEARCH.md
    - package.json
  </read_first>
  <action>Recheck the official repository, exact version, publication date, repository URL, and lifecycle scripts for `convex@1.42.2`, `convex-helpers@0.1.120`, `convex-test@0.0.54`, `typescript@6.0.3`, `vite@8.1.4`, `@vitejs/plugin-react@6.0.3`, `vitest@4.1.10`, `@clerk/react@6.12.4`, `@convex-dev/better-auth@0.12.5`, `better-auth@1.6.23`, and `@arethetypeswrong/cli@0.18.5`. Also verify the research-approved packages and compatible peers. Do not install anything at this checkpoint. Stop if a package name, owner, source repository, lifecycle script, or warning reason differs from the research audit.</action>
  <verify>
    <automated>for p in convex@1.42.2 convex-helpers@0.1.120 convex-test@0.0.54 typescript@6.0.3 vite@8.1.4 @vitejs/plugin-react@6.0.3 vitest@4.1.10 @clerk/react@6.12.4 @convex-dev/better-auth@0.12.5 better-auth@1.6.23 @arethetypeswrong/cli@0.18.5; do npm view "$p" name version repository scripts --json || exit 1; done</automated>
  </verify>
  <what-built>A red packed-consumer test plus a registry report for every research-flagged dependency.</what-built>
  <how_to_verify>
    1. Compare every package owner and repository with the Package Legitimacy Audit in `01-RESEARCH.md`.
    2. Confirm each warning remains only release recency and no lifecycle install script appeared.
    3. Approve the exact versions, or reject the matrix and stop before Plan 01-02.
  </how_to_verify>
  <resume_signal>Type "approved" to authorize the reviewed versions, or list rejected packages.</resume_signal>
  <acceptance_criteria>
    - All package identities and sources match official projects.
    - No inspected package is classified as SLOP.
    - Provider packages remain fixture-only development dependencies.
    - Explicit human approval exists before the first install.
  </acceptance_criteria>
  <done>The exact package matrix is approved for installation with every recency warning reviewed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry → repository | Package names, versions, tarballs, and lifecycle scripts are untrusted supply-chain inputs. |
| repository → clean fixture | Workspace source must not satisfy consumer imports or hide missing packed files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Spoofing | npm dependencies | high | mitigate | Human-review exact package ownership/source and block changed audit reasons before install. |
| T-01-02 | Tampering | packed-consumer test | high | mitigate | Require an external temporary consumer and reject repository-root resolution. |
| T-01-03 | Information Disclosure | fixture args | medium | mitigate | Acceptance test permits intent data only and rejects identity/admin/scope transport. |
| T-01-SC | Tampering | npm installs | high | mitigate | Package legitimacy checkpoint blocks all research-marked SUS packages until reviewed. |
</threat_model>

<verification>
- `! node --test tests/integration/walking-skeleton.test.mjs` succeeds because the specification is red for the missing capability.
- Registry metadata for every flagged dependency is reviewed without installing it.
</verification>

<success_criteria>
- The first test expresses the exact clean-consumer, real write/read, and UI interaction proof.
- The dependency gate is explicitly approved and safe to hand to Plan 01-02.
</success_criteria>

<output>
Create `.planning/phases/01-secure-installable-feedback-board/01-01-SUMMARY.md` when done.
</output>
