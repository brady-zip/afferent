---
phase: 01-secure-installable-feedback-board
plan: "08"
type: execute
wave: 8
depends_on:
  - "01-07"
files_modified:
  - src/component/admin/installation.ts
  - src/component/public/posts.ts
  - src/component/validators.ts
  - src/component/_generated/component.ts
  - src/client/contracts.ts
  - src/client/internal.ts
  - tests/component/installation.test.ts
  - tests/component/posts.test.ts
  - tests/integration/scope-matrix.test.ts
  - tests/integration/packed-artifact.test.mjs
  - tests/static/contracts.test.ts
  - package.json
autonomous: true
gap_closure: true
requirements:
  - ACCS-01
  - ACCS-06
  - FDBK-01
  - FDBK-08
  - COMP-02
  - COMP-03
  - QUAL-02
  - QUAL-03
must_haves:
  truths:
    - "Repeated installation configuration is additive and idempotent: same slugs update display fields, omitted boards remain intact, changed slugs create boards, and cumulative boards stay within the documented bound."
    - "Board-level post counts return `{ count, hasMore }` with an honest cap-plus-one signal, while every post DTO retains exact vote and comment totals."
    - "Independent provider fixture typechecks, real factory conformance, and real-backend pagination are named required automated Phase 1 gates."
  artifacts:
    - path: "tests/component/installation.test.ts"
      provides: "D-14 additive and non-destructive configuration proof"
    - path: "src/component/public/posts.ts"
      provides: "D-15 bounded count implementation"
    - path: "src/client/contracts.ts"
      provides: "Stable `{ count, hasMore }` public count DTO"
    - path: "package.json"
      provides: "Named fixture-typecheck and real-backend release gates"
  key_links:
    - from: "src/client/internal.ts"
      to: "src/component/public/posts.ts"
      via: "typed countPosts query returning count plus truncation signal"
      pattern: "hasMore"
    - from: "tests/component/installation.test.ts"
      to: "src/component/admin/installation.ts"
      via: "repeated disjoint and overlapping configuration calls"
      pattern: "configureInstallation"
    - from: "package.json"
      to: "scripts/test-pagination-backend.mjs"
      via: "named required real-backend pagination script"
      pattern: "test:backend:real"
---

## Phase Goal

**As a** Convex developer, **I want to** install Afferent with my auth provider, **so that** users can safely run multi-board feedback.

## Artifacts this phase produces

- Automated proof of additive, non-destructive installation configuration.
- A bounded count DTO that distinguishes an exact count from a capped lower bound.
- One named Phase 1 release command covering fixture typechecks, real factory conformance, real-backend pagination, and packed-artifact checks.

<objective>
Close the verifier warnings under the radio-locked configuration and count contracts, then make the full closure reproducible from named release gates.

Purpose: Implement D-14 and D-15 without importing Phase 2 lifecycle work, preserve exact per-post participation totals, and prevent the passing real-backend pagination proof from silently dropping out of automation.
Output: Installation idempotency tests, bounded count contract and tests, and required fixture/real-backend release scripts.

Decisions: D-01, D-02, D-05, D-06, D-13, D-14, D-15.
</objective>

<execution_context>
@.codex/gsd-core/workflows/execute-plan.md
@.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-secure-installable-feedback-board/01-CONTEXT.md
@.planning/phases/01-secure-installable-feedback-board/01-VERIFICATION.md
@.planning/phases/01-secure-installable-feedback-board/01-07-SUMMARY.md
@scripts/test-pagination-backend.mjs
@scripts/test-packed-consumer.mjs

<interfaces>
From `src/component/admin/installation.ts`:
- `configureInstallation({ scopeId, readPolicy, boards })` is the narrow authorized installation mutation.

From `src/client/contracts.ts`:
- `ReadCapabilities.countPosts(ctx, { boardId })` is the public typed count surface.
- `PostDto.voteCount`, `PostDto.commentCount`, and `PostDto.totals` are exact per-post projections.

From `src/component/public/posts.ts`:
- `MAX_POSTS` is the shared public page/count cap and `countPosts` queries `by_scope_board_state`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock additive idempotent installation configuration</name>
  <files>src/component/admin/installation.ts, tests/component/installation.test.ts, tests/integration/scope-matrix.test.ts</files>
  <read_first>
    - src/component/admin/installation.ts
    - src/component/public/boards.ts
    - src/component/schema.ts
    - tests/component/walking-skeleton.test.ts
    - tests/integration/scope-matrix.test.ts
  </read_first>
  <behavior>
    - Repeating the same slug updates its mutable name without creating a duplicate board or changing existing post ownership.
    - Omitting a previously configured board leaves that board, its posts, and its public visibility intact.
    - Supplying a new slug creates a distinct board; retrying that request is idempotent.
    - The operation rejects any call that would make the installation exceed the bounded board maximum without deleting or hiding existing boards.
  </behavior>
  <action>Write a dedicated repeated-configuration integration suite before changing implementation. Make `configureInstallation` explicitly additive per D-14: load existing scope boards through a bounded scope-leading index, update mutable display names for matching slugs, preserve omitted rows and their posts, and append new slugs with deterministic non-colliding ordering. Enforce the installation-wide board maximum against existing plus genuinely new slugs so repeated disjoint calls cannot make `listBoards` silently omit accumulated boards. Do not add archive, delete, reconciliation, detachment, post-move, or board-lifecycle operations; those capabilities belong to Phase 2.</action>
  <verify>
    <automated>npm run test:component -- tests/component/installation.test.ts && npm run test:scope -- tests/integration/scope-matrix.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Same-slug retries preserve one board ID and apply the latest allowed display name.
    - Omitted boards and their posts remain readable and unchanged.
    - New slugs create exactly one new board and receive stable ordering.
    - The cumulative bound fails closed before partial writes and never reconciles by deletion or hiding.
  </acceptance_criteria>
  <done>Installation configuration has executable proof of D-14's additive, idempotent, and non-destructive behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Return honest bounded board post counts</name>
  <files>src/component/public/posts.ts, src/component/validators.ts, src/component/_generated/component.ts, src/client/contracts.ts, src/client/internal.ts, tests/component/posts.test.ts, tests/integration/scope-matrix.test.ts, tests/static/contracts.test.ts</files>
  <read_first>
    - src/component/public/posts.ts
    - src/component/validators.ts
    - src/client/contracts.ts
    - src/client/internal.ts
    - tests/component/posts.test.ts
    - tests/integration/scope-matrix.test.ts
  </read_first>
  <behavior>
    - Zero through the count cap returns the exact number with `hasMore: false`.
    - More than the cap reads at most cap plus one and returns the cap with `hasMore: true`.
    - Counts remain scope-, board-, lifecycle-, and read-policy constrained.
    - Post DTO vote and comment totals stay exact before and after the count contract change.
  </behavior>
  <action>Define one exported count DTO and matching Convex validator with `contractVersion`, `count`, and `hasMore`. Update the scope-first `countPosts` query to take exactly the configured cap plus one, return the exact length when within the cap, and return the cap with `hasMore: true` when the sentinel row exists, as required by D-15. Propagate the shape through generated component typing, client capabilities, and fixed/scoped clients. Add boundary tests at zero, cap, and cap plus one, including withdrawn and cross-scope posts, and retain assertions that per-post vote/comment fields and `totals` are exact.</action>
  <verify>
    <automated>npm run test:component -- tests/component/posts.test.ts && npm run test:scope -- tests/integration/scope-matrix.test.ts && npm run test:static -- tests/static/contracts.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - No bounded result can be mistaken for an exact unbounded count.
    - The query reads no more than cap plus one rows through `by_scope_board_state`.
    - Count DTO validators and TypeScript capabilities agree on `hasMore`.
    - Exact vote/comment totals remain unchanged on every visible post DTO.
  </acceptance_criteria>
  <done>Board post counts satisfy D-15's honest bounded contract without weakening exact per-post participation totals.</done>
</task>

<task type="auto">
  <name>Task 3: Promote closure proofs into the named Phase 1 release gate</name>
  <files>package.json, tests/integration/packed-artifact.test.mjs</files>
  <read_first>
    - package.json
    - tests/integration/packed-artifact.test.mjs
    - scripts/test-pagination-backend.mjs
    - scripts/test-packed-consumer.mjs
    - fixtures/auth-convex-auth/tsconfig.json
    - fixtures/auth-clerk/tsconfig.json
    - fixtures/auth-better-auth/tsconfig.json
  </read_first>
  <action>Add named package scripts for independent auth-fixture typechecks and the existing real-backend pagination runner, then include both in the required Phase 1 release command alongside actual-factory conformance, scope, component, static, and packed-artifact gates. Keep the real-backend script focused on pagination/runtime behavior; D-13 explicitly uses convex-test and independent typechecks for live provider authority acceptance, so do not add provider tokens, hosted auth setup, or browser deployment. Extend the packed-artifact test to assert the named scripts are present and that the release gate cannot silently substitute the convex-test pagination test for the real-backend runner. Modify only script entries in the currently dirty package manifest and preserve the user's unrelated dependency-version experiment; no lockfile change is needed.</action>
  <verify>
    <automated>npm run test:fixtures && npm run test:auth-conformance && npm run test:backend:real && npm run test:package</automated>
  </verify>
  <acceptance_criteria>
    - One named command fails if any fixture typecheck, real factory conformance suite, real-backend pagination run, or packed artifact gate fails.
    - `test:backend:real` directly invokes `scripts/test-pagination-backend.mjs` and is distinct from the convex-test pagination script.
    - No live provider credentials or deployment-dependent auth test is introduced.
    - Package dependency values and package-lock remain outside this task's edits.
  </acceptance_criteria>
  <done>The passing real-backend proof and all provider closure evidence are documented by executable package scripts and required by Phase 1 release verification.</done>
</task>

</tasks>

## Gap-Closure Source Audit

The exhaustive original four-source audit remains in `01-SOURCE-AUDIT.md`. The additive audit below covers every new verification and radio item; existing Plans 01-01 through 01-05 continue to cover the unchanged Phase 1 goal, all original requirement IDs, research constraints, and D-01 through D-12.

| Source | ID | Item | Plan | Status |
|---|---|---|---|---|
| GOAL | - | Packed Afferent integration with secure equivalent Convex Auth, Clerk, and Better Auth paths | 01-06, 01-07, 01-08 | COVERED |
| REQ | ACCS-01, ACCS-06, FDBK-01, FDBK-08, COMP-02, COMP-03, QUAL-03 | Installation policy, bounded multi-board browse/DTO, typed client, and packed-consumer quality | 01-08 plus existing plans | COVERED |
| REQ | ACCS-02, ACCS-03, ACCS-04, ACCS-05, COMP-04, COMP-05, COMP-06, COMP-07, QUAL-02 | Trusted provider identity/admin behavior and equivalent real fixtures | 01-06, 01-07 | COVERED |
| REQ | FDBK-02, FDBK-03, FDBK-04, FDBK-05, FDBK-06, FDBK-07, QUAL-10 | Existing participation, reply, vote, lifecycle, and license requirements remain unchanged | 01-01 through 01-05 | COVERED |
| RESEARCH | - | Provider packages remain fixture-only; component and public DTOs remain provider-neutral | 01-06, 01-07 | COVERED |
| RESEARCH | - | Better Auth uses component-backed session-validating lookup | 01-06, 01-07 | COVERED |
| RESEARCH | - | Component pagination is tested on a real backend in addition to convex-test | 01-08 | COVERED |
| CONTEXT | D-01 through D-12 | Existing scope, actor, privacy, and isolation contract | 01-01 through 01-07 | COVERED |
| CONTEXT | D-13 | convex-test factory execution plus independent fixture typechecks; no live provider credentials | 01-06, 01-07, 01-08 | COVERED |
| CONTEXT | D-14 | Additive, idempotent, non-destructive installation configuration | 01-08 | COVERED |
| CONTEXT | D-15 | Exact per-post totals plus honest bounded board count DTO | 01-08 | COVERED |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Admin configuration -> component storage | Repeated configuration must not delete, hide, or detach existing board data. |
| Public count request -> scoped index | Count metadata must be bounded, scope-safe, and honest about truncation. |
| Release scripts -> verification evidence | Named commands must execute the real fixture and backend proofs they claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-34 | Tampering | installation configuration | high | mitigate | Test repeated overlapping/disjoint calls and reject cumulative overflow before writes. |
| T-01-35 | Denial of Service | board configuration | medium | mitigate | Bound existing-board reads and total stored boards per scope. |
| T-01-36 | Information Disclosure | countPosts | high | mitigate | Keep scope/board/read-policy predicates before cap-plus-one query and expose truncation explicitly. |
| T-01-37 | Repudiation | release gate | high | mitigate | Name and require direct fixture-typecheck and real-backend scripts; packed tests assert their wiring. |
| T-01-SC | Tampering | npm installs | high | mitigate | No install occurs and no dependency or lockfile change is planned. |
</threat_model>

<verification>
- Run D-14 installation reconfiguration tests and D-15 count-boundary tests.
- Run independent fixture typechecks and actual-factory conformance.
- Run the named real-backend pagination and packed-artifact gates.
- Run the complete Phase 1 release command from the reviewed TypeScript baseline used for verification.
</verification>

<success_criteria>
- D-14 and D-15 have executable contract tests and stable typed DTOs.
- Real-backend pagination is a named required automated gate without live provider scope.
- The full gap-closure set passes together and is ready for independent phase re-verification.
</success_criteria>

<output>
Create `.planning/phases/01-secure-installable-feedback-board/01-08-SUMMARY.md` when done.
</output>
