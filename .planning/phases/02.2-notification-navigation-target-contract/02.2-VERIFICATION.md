---
phase: 02.2-notification-navigation-target-contract
verified: 2026-07-21T19:38:47Z
status: gaps_found
score: 4/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "D-01/D-03: every closed target has the exact server-authored accessible label"
    status: failed
    reason: "A legacy-invalid comment anchor is omitted, but the resulting post-only target keeps the anchored-comment label instead of the locked post-only label."
    artifacts:
      - path: src/component/notifications/inbox.ts
        issue: "Lines 61-73 always emit `View comment on feedback: ...` for reply/mention events even when `validComment` is undefined."
      - path: tests/component/notifications.test.ts
        issue: "Lines 190-195 encode the same wrong label in the legacy-invalid fallback assertion, so the passing test is misleading."
    missing:
      - "Choose `View feedback: {post title}` whenever the optional comment anchor is absent; retain `View comment on feedback: {post title}` only when `commentId` is present."
      - "Correct the fallback assertion and retain the exact five-event label matrix."
  - truth: "D-12: RED matrices cover exact event, scope, relation, version, and merge behavior"
    status: failed
    reason: "The target-specific component suite has the valid five-event matrix and one un-normalizable legacy ID case, but it does not execute the promised cross-scope post/comment/changelog or same-scope mismatched comment/post cases."
    artifacts:
      - path: tests/component/notifications.test.ts
        issue: "No target-resolution test injects a cross-scope required post, cross-scope optional comment, cross-scope changelog entry, or a comment attached to a different same-scope post."
      - path: tests/static/notification-targets.test.ts
        issue: "The static guards cover public IDs, browser authority, client joins, and collect scans, but cannot prove the missing runtime scope/relation behavior."
    missing:
      - "Executable cross-scope and mismatched-relation target-resolution cases with exact invariant/no-leak or safe-degradation expectations."
---

# Phase 02.2: Notification Navigation Target Contract Verification Report

**Phase Goal:** In-app notification consumers receive a versioned, accessible, public navigation destination resolved from trusted source records without polymorphic internal IDs or client-side joins.

**Verified:** 2026-07-21T19:38:47Z

**Status:** gaps_found

**Score:** 4/6 must-haves verified

## Final Verdict

Phase 02.2 does not yet pass. The closed target shape, event mappings for valid records, type-aware merge normalization, DTO version split, headless pass-through, packed consumption, and all aggregate regressions are present and green. Two locked requirements remain unsatisfied.

First, legacy-invalid comment anchors correctly omit `commentId`, but `resolveNotificationTarget` still labels the resulting post-only destination `View comment on feedback: {post title}`. Context D-03 requires every post-only destination to use `View feedback: {post title}`, and D-07 explicitly classifies this path as post-only degradation. The current component test asserts the incorrect label, so its pass is not confirming the specified contract.

Second, Context D-12 and Task 1 explicitly require executable cross-scope post/comment/changelog and mismatched comment/post cases. The implementation contains the intended scope and comment-relation checks, but the target-specific runtime suite exercises only valid referents and one un-normalizable legacy comment ID. Static source guards are not behavioral substitutes for the omitted adversarial matrix.

No human verification is needed. These are deterministic implementation/test gaps.

## Must-Have Accounting

The four ROADMAP success criteria and six plan truths were deduplicated into the plan's six independently checkable truths.

| # | Truth | Status | Independent evidence |
| --- | --- | --- | --- |
| 1 | NotificationDto v2 contains a required closed target, exposes no public polymorphic `entityId`, and every target uses the exact locked accessible label | FAILED | Types and validators are closed, and valid-event labels pass. However, `src/component/notifications/inbox.ts:61-73` emits the anchored label after omitting an invalid comment anchor; `tests/component/notifications.test.ts:190-195` incorrectly blesses it. |
| 2 | Status, reply/mention, and changelog events map to the right public destination; referents and relationships are scope-checked with safe optional-anchor degradation | VERIFIED | `resolveNotificationTarget` requires a same-scope post, requires a same-scope changelog entry, and includes a comment only when both scope and `comment.postId === event.postId` match. The valid five-event matrix and legacy omission shape pass. Exact fallback label is counted once under Truth 1. |
| 3 | Resolution occurs once in the component; hooks preserve the DTO verbatim; consumers perform no target joins; packed and registry fixtures use the exported target | VERIFIED | `toNotificationDto` calls the component resolver; `mapNotificationFeedState` returns `pagination.results` directly; bindings expose no target authority; packed/registry consumers dispatch only on `target.kind` and render `target.label`. Real headless and packed gates pass. |
| 4 | Both small and resumable merges canonicalize event post IDs, rewrite only status entity IDs, preserve comment/changelog identity, and retain other notification behavior | VERIFIED | `normalizeSourceRelation` performs the type-aware patch and is shared by atomic and resumable cleanup. The parameterized atomic/51-relation test passes, as does the real merge backend. |
| 5 | Notification row/page contracts are explicitly v2 across public/component validators and generated consumption, while DeliveryEventDto remains unchanged at v1 | VERIFIED | Public types and validators, component validators, generated component references, fixtures, and packed consumer agree on notification v2/target; delivery remains v1 with `entityId`. Exact contract and aggregate gates pass. |
| 6 | Exact RED/adversarial matrices, static guards, focused/packed/full gates, and fingerprint preservation prove the contract | FAILED | All requested gates and static guards pass and the fingerprint is preserved, but the promised runtime cross-scope and mismatched-relation target matrix is absent. |

## Event and Destination Matrix

| Event | Required public target | Observed result | Status |
| --- | --- | --- | --- |
| `status_changed` | Canonical post ID; `View feedback: {post title}` | Exact target asserted and passed | VERIFIED |
| `admin_replied` | Canonical post ID plus valid comment anchor; `View comment on feedback: {post title}` | Exact valid target asserted and passed | VERIFIED |
| `comment_replied` | Canonical post ID plus valid comment anchor; `View comment on feedback: {post title}` | Exact valid target asserted and passed | VERIFIED |
| `mentioned` | Canonical post ID plus valid comment anchor; `View comment on feedback: {post title}` | Exact valid target asserted and passed | VERIFIED |
| `changelog_published` | Public slug only; `View changelog: {entry title}` | Exact target asserted and passed; no entry ID exposed | VERIFIED |
| Legacy-invalid optional comment | Post-only target; `View feedback: {post title}` | `commentId` omitted, but label remains `View comment on feedback: ...` | FAILED |

## Artifacts, Wiring, and Commits

| Artifact / link | Status | Details |
| --- | --- | --- |
| `src/client/contracts.ts` | VERIFIED | Closed v1 `NotificationTarget`; notification row/page v2; delivery v1 unchanged. |
| `src/component/notifications/inbox.ts` | PARTIAL | Single trusted resolver, scope/relation checks, and no internal-ID exposure are present; fallback label is wrong. |
| `src/component/model/merge.ts` | VERIFIED | Atomic and resumable paths share type-aware normalization. |
| `src/react/hooks/notifications.ts` | VERIFIED | Notification arrays and target objects pass through without reconstruction or direct reads. |
| `tests/static/notification-targets.test.ts` | PARTIAL | No-public-ID, no-browser-authority, no-client-resolution, no-collect guards exist; missing runtime adversarial cases remain outside this static file's capability. |
| `fixtures/packed-vite-convex/src/App.tsx` | VERIFIED | Installed consumer imports the public target contract and consumes kind/label without referent reads. |

Automated artifact query: **6/6 present and substantive**.

Automated key-link query: **5/5 wired**. Manual tracing confirmed inbox-to-contract/schema resolution, merge-to-inbox identity preservation, React contract pass-through, and packed target consumption.

Execution commits `dced9a3`, `c7fd7bd`, and `12f43b5`, plus documentation commit `c4057c5`, all exist. Their changed-path sets match the implementation, tests, fixtures, generated reference, and planning scope described by the summary. A later h5i context commit exists but is not implementation evidence and is not scored.

## Fresh Behavioral Evidence

| Command | Result | Status |
| --- | --- | --- |
| `npm exec -- vitest run tests/component/notifications.test.ts tests/component/merge.test.ts tests/static/contracts.test.ts tests/static/notification-targets.test.ts` | 4 files, 30/30 tests passed | PASS, but fallback assertion is specification-inverted |
| `npm exec -- vitest run --config vitest.react.config.ts tests/react/notifications.test.tsx tests/react/live-headless.test.tsx` | 2 files, 24/24 tests passed | PASS |
| `node scripts/test-merge-backend.mjs` | Exit 0 through h5i capture; real Convex merge matrix completed | PASS |
| `node scripts/test-headless-backend.mjs` | `Real Convex headless watch matrix passed` | PASS |
| `node --test tests/integration/packed-artifact.test.mjs` | 3/3 tests passed, including clean external packed consumer | PASS |
| `npm run test:phase2` | Build; model 32/32; component 44/44; real backend matrices; static 31/31; React 54/54; conformance 25/25; scope 3/3; typecheck; lint; packed consumer all passed | PASS, RUN ONCE |
| `git diff --check` | No output | PASS |
| Dirty fingerprint | `09b5f7179dc749a197ec6a59c5ba188313b8c84536e0c35b6487ae7fcb058381` | PRESERVED |

The fresh complete aggregate output is captured by h5i object `f621ec98d9be2a12`; the independent real merge run is captured by object `d53457743b258659`.

## Static Security and Performance Review

The public notification slice contains no `entityId`, component changelog ID, or changelog entry ID. The browser bindings accept no target authority. React notification hooks contain no target construction, `runQuery`, `watchQuery`, or entity-ID resolution. Inbox pagination remains indexed and contains no `.collect()` target scan. Consumers receive the complete destination in each row and issue no per-notification target read.

The resolver rejects a missing or cross-scope required post and a missing or cross-scope required changelog entry. Optional comment identity is included only when the comment matches both event scope and event post. Those branches are source-verified but the exact cross-scope/relation failure behavior lacks the locked executable cases described in Gap 2.

No unreferenced `TBD`, `FIXME`, `XXX`, `TODO`, `HACK`, or placeholder marker appears in Phase 02.2-owned implementation/test paths. The anti-pattern scan returned only existing functional `null`/`undefined` control flow. `git diff --check` passes.

## Requirements Coverage

| Requirement | Status | Evidence |
| --- | --- | --- |
| NOTF-08 | BLOCKED | Closed versioned targets, no public polymorphic ID, and server-side resolution are present, but the required human-readable label is wrong on the specified post-only legacy fallback and the locked adversarial scope/relation proof is incomplete. |

NOTF-08 is mapped only to Phase 02.2; no orphan requirement was found. Its checked/completed marker in `REQUIREMENTS.md` and the Phase 02.2 completion marker in `ROADMAP.md` are not sufficient to override the failed canonical verification.

## Disconfirmation Pass

| Required check | Investigation | Result |
| --- | --- | --- |
| Misleading passing test | Compared the locked D-03/D-07 wording to the legacy-invalid component assertion and resolver branch | FOUND: the test expects an anchored label after the anchor is removed. |
| Scope or relation leak | Traced post, comment, changelog, event, inbox, and initiator scope checks and searched target tests for adversarial records | No source-level leak found; FOUND missing executable cross-scope/mismatch matrix. |
| Merge identity clobber | Traced both atomic and resumable calls into `normalizeSourceRelation`; ran the parameterized and real backend matrices | Rejected: post IDs canonicalize; only status entity IDs change; comment/changelog identity remains. |
| Public internal-ID exposure | Compared types, validators, generated references, inbox output, static guards, fixtures, and delivery separation | Rejected for browser notification DTOs. Delivery intentionally retains its separate v1 host event identity. |
| Client-side join or N+1 read | Inspected bindings/hooks/fixtures and ran static, React, headless, and packed gates | Rejected: consumers receive and preserve resolved targets without referent reads. |
| Silent version mismatch | Compared public/component validators, generated surface, exact tests, and installed consumer | Rejected: notification row/page are v2; target and delivery remain v1 by design. |
| Unrelated-worktree drift | Recomputed the declared dirty fingerprint after all fresh gates | Rejected: exact fingerprint preserved. |

## Human Verification

None. All behavior-dependent evidence used for this verdict is automated; the two gaps are directly reproducible from source and test assertions.

---

_Verified: 2026-07-21T19:38:47Z_

_Verifier: independent generic-agent fallback using the installed gsd-verifier role_
