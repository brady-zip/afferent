---
phase: 02.2-notification-navigation-target-contract
verified: 2026-07-21T20:02:04Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/6
  gaps_closed:
    - "Post-only notification targets now use `View feedback: {post title}` whenever no validated commentId is emitted; anchored targets retain `View comment on feedback: {post title}`."
    - "Executable public-inbox tests now prove cross-scope required post/changelog rejection without destination leakage and cross-scope/wrong-post optional-comment safe degradation."
  gaps_remaining: []
  regressions: []
---

# Phase 02.2: Notification Navigation Target Contract Verification Report

**Phase Goal:** In-app notification consumers receive a versioned, accessible, public navigation destination resolved from trusted source records without polymorphic internal IDs or client-side joins.

**Verified:** 2026-07-21T20:02:04Z

**Status:** passed

**Score:** 6/6 must-haves verified

**Re-verification:** Yes -- after gap-closure commits `fd3ad1e`, `92e1e60`, and `3a46b80`

## Final Verdict

Phase 02.2 passes. The closure binds optional `commentId` presence and accessible wording to the same validated comment relation: a validated same-scope, same-post comment produces an anchored target and comment-specific label; every absent, legacy-invalid, cross-scope, or wrong-post comment produces only the validated post destination with the exact post-only label.

The new component tests execute the real public inbox query. Cross-scope required post and changelog records fail with their exact invariant errors and expose no foreign identifier, title, slug, label, or target. Cross-scope and same-scope-wrong-post comments return the exact safe event-post target without `commentId` or foreign metadata. The original five-event matrix remains exact.

All four previously verified truths also remain intact: closed/versioned DTOs, server-only one-path resolution and verbatim consumers, type-aware atomic/resumable merge normalization, and notification-v2 versus delivery-v1 separation. Focused, real-backend, headless, packed, and complete Phase 2 gates pass with the unrelated fingerprint unchanged.

## Must-Have Accounting

The four ROADMAP success criteria and Plan 02.2-01 truths remain represented by six non-overlapping must-haves. Both prior failures were fully re-verified; the other four received fresh regression checks.

| # | Truth | Status | Independent evidence |
| --- | --- | --- | --- |
| 1 | NotificationDto v2 contains a required closed target, exposes no public polymorphic `entityId`, and every target uses the exact locked accessible label | VERIFIED | `resolveNotificationTarget` now branches its label on the same `validComment` value that controls `commentId`. Full-object assertions prove anchored and all post-only variants; public/component contract tests remain green. |
| 2 | Status, reply/mention, and changelog events map to the right public destination; every referent and relationship is scope-checked with safe optional-anchor degradation | VERIFIED | The valid five-event matrix passes. Public-inbox adversarial cases prove exact cross-scope required-referent failures and exact optional-comment degradation for both cross-scope and same-scope-wrong-post relations. |
| 3 | Resolution occurs once in the component; hooks preserve the DTO verbatim; consumers perform no target joins; packed and registry fixtures use the exported target | VERIFIED | `toNotificationDto` remains the single resolver call; React returns `pagination.results` directly; bindings expose no target authority; packed/registry consumers use only exported target fields. React, real headless, static, and packed gates pass. |
| 4 | Both small and resumable merges canonicalize event post IDs, rewrite only status entity IDs, preserve comment/changelog identity, and retain other notification behavior | VERIFIED | The shared `normalizeSourceRelation` logic is unchanged. The focused atomic/51-relation merge matrix and independent real merge backend both pass. |
| 5 | Notification row/page contracts are explicitly v2 across public/component validators and generated consumption, while DeliveryEventDto remains unchanged at v1 | VERIFIED | Public types/validators, component validators, generated references, and packed consumption remain aligned; exact contract tests and the full gate pass. Delivery remains v1 with its host event `entityId`. |
| 6 | Exact RED/adversarial matrices, static guards, focused/packed/full gates, and fingerprint preservation prove the contract | VERIFIED | All previously missing runtime cases now execute through `listNotifications`; target/static, focused, packed, and the single full aggregate pass; exact protected fingerprint is preserved. |

## Destination and Adversarial Matrix

| Case | Required result | Fresh evidence | Status |
| --- | --- | --- | --- |
| `status_changed` | Post target; `View feedback: {post title}` | Exact five-event matrix | VERIFIED |
| Valid `admin_replied`, `comment_replied`, `mentioned` | Post plus validated `commentId`; `View comment on feedback: {post title}` | Exact five-event matrix checks all three event types | VERIFIED |
| `changelog_published` | Public slug only; `View changelog: {entry title}` | Exact five-event matrix; no entry ID in public row | VERIFIED |
| Legacy/un-normalizable optional comment | Event-post target only; post-only label | Exact full-object assertion and explicit no-`commentId` check | VERIFIED |
| Cross-scope required post | Exact `NOTIFICATION_TARGET_POST_INVARIANT`; no destination metadata | Real inbox query rejects; serialized error excludes foreign ID/title and `target` | VERIFIED |
| Cross-scope required changelog | Exact `NOTIFICATION_TARGET_CHANGELOG_INVARIANT`; no destination metadata | Real inbox query rejects; serialized error excludes foreign ID/slug/title and `target` | VERIFIED |
| Cross-scope optional comment | Exact same-scope event-post target only; post-only label | Full target equality, no `commentId`, no foreign identifiers/titles | VERIFIED |
| Same-scope comment on another post | Exact event-post target only; post-only label | Full target equality, no `commentId`, no wrong-post identifier/title | VERIFIED |

The required-referent tests exercise the same fail-closed branches used for absent/invalid required IDs. The optional relation matrix exercises both halves of the relation predicate: scope mismatch and post mismatch.

## Gap Closure Scope and Provenance

| Commit | Scope | Verification |
| --- | --- | --- |
| `fd3ad1e` | `tests/component/notifications.test.ts` only | Corrects the legacy expectation and adds the four verifier-named executable relation cases. |
| `92e1e60` | `src/component/notifications/inbox.ts` only | Three additions and one deletion: a single conditional label expression; no refactor or public contract change. |
| `3a46b80` | Requirements, roadmap, state, and `02.2-02-SUMMARY.md` only | Documentation closeout; no implementation or test changes. |

The test and fix commits span exactly the two Plan 02.2-02-owned files. They do not modify DTOs, validators, generated references, merge code, React, bindings, fixtures, schema, manifests, dependencies, prior Plan 02.2-01 artifacts, or the prior verification report. The RED test commit precedes the production correction.

Initial Plan 02.2-01 implementation/test commits `dced9a3`, `c7fd7bd`, and `12f43b5`, plus documentation commit `c4057c5`, remain present. h5i context commits are provenance metadata and are not scored as implementation evidence.

## Artifacts, Wiring, and Data Flow

| Artifact / link | Status | Details |
| --- | --- | --- |
| `src/client/contracts.ts` | VERIFIED | Closed v1 `NotificationTarget`; notification row/page v2; delivery v1 unchanged. |
| `src/component/notifications/inbox.ts` | VERIFIED | Required scope checks, optional scope/post relation check, relation-coupled label/anchor projection, and one DTO resolution point. |
| `src/component/model/merge.ts` | VERIFIED | Atomic and resumable paths still share type-aware notification normalization. |
| `src/react/hooks/notifications.ts` | VERIFIED | Carries complete notification rows and targets verbatim without resolution or direct reads. |
| `tests/static/notification-targets.test.ts` | VERIFIED | Guards public/internal IDs, browser authority, client resolution, collect scans, and consumer read amplification. |
| `fixtures/packed-vite-convex/src/App.tsx` | VERIFIED | Clean installed consumer uses the exported closed target without referent reads. |
| `tests/component/notifications.test.ts` | VERIFIED | Executes exact valid, invalid, cross-scope, relation, label, and no-leak behavior through the public inbox query. |

Automated Plan 02.2-01 artifact query: **6/6 present and substantive**.

Automated Plan 02.2-01 key-link query: **5/5 wired**.

Automated Plan 02.2-02 artifact query: **2/2 present and substantive**.

Automated Plan 02.2-02 key-link query: **1/1 wired**. Manual tracing confirms that the label and optional `commentId` use the same `validComment` branch.

## Fresh Behavioral Evidence

| Command | Result | Status |
| --- | --- | --- |
| `npm exec -- vitest run tests/component/notifications.test.ts --reporter=verbose` | 8/8 passed, including valid five-event, legacy fallback, both required invariants, and both optional relation failures | PASS |
| `npm exec -- vitest run tests/component/notifications.test.ts tests/component/merge.test.ts tests/static/contracts.test.ts tests/static/notification-targets.test.ts` | 4 files, 33/33 passed | PASS |
| `npm exec -- vitest run --config vitest.react.config.ts tests/react/notifications.test.tsx tests/react/live-headless.test.tsx` | 2 files, 24/24 passed | PASS |
| `node scripts/test-merge-backend.mjs` | Exit 0 through h5i capture; real Convex merge matrix completed | PASS |
| `node scripts/test-headless-backend.mjs` | `Real Convex headless watch matrix passed` | PASS |
| `node --test tests/integration/packed-artifact.test.mjs` | 3/3 passed, including clean external packed consumer | PASS |
| `npm run test:phase2` | Build; model 32/32; component 47/47; real backend matrices; static 31/31; React 54/54; conformance 25/25; scope 3/3; typecheck; lint; packed consumer all passed | PASS, RUN ONCE |
| `git diff --check` | No output | PASS |
| Dirty fingerprint | `09b5f7179dc749a197ec6a59c5ba188313b8c84536e0c35b6487ae7fcb058381` | PRESERVED |

The fresh full aggregate output is captured by h5i object `3b745013ff3bc94f`; the independent real merge run is captured by object `59143acd04483659`.

## Security and Performance Review

Required post/changelog failures return only fixed invariant strings; adversarial tests prove foreign IDs, titles, slugs, labels, and target data do not appear in the serialized rejection. Optional invalid comment relations reveal only the already-validated same-scope event post and exclude both invalid comment IDs and foreign/wrong-post titles.

The public notification slice still contains no `entityId`, component changelog ID, or changelog entry ID. Browser bindings accept no target authority. React notification hooks contain no target construction, component query, or entity-ID resolution. Inbox pagination remains indexed and has no target `.collect()` scan; consumers issue no per-notification referent read.

No `TBD`, `FIXME`, `XXX`, `TODO`, `HACK`, placeholder, unimplemented marker, or debug logging appears in the two closure-owned files. `git diff --check` passes. The unrelated dirty files remain outside the phase changes and retain the exact protected fingerprint.

## Requirements Coverage

| Requirement | Status | Evidence |
| --- | --- | --- |
| NOTF-08 | SATISFIED | Closed/versioned public targets have destination-accurate accessible labels, no public polymorphic identifier, server-only scope/relation resolution, executable no-leak behavior, and no client-side target resolution. |

NOTF-08 is mapped only to Phase 02.2; no orphan requirement exists.

## Disconfirmation Pass

| Required check | Investigation | Result |
| --- | --- | --- |
| Previously partial requirement | Re-ran the exact legacy, foreign-required, and invalid-optional paths through the public query | Rejected: every named gap now has exact executable proof. |
| Misleading passing test | Compared the legacy fallback assertion and valid-event labels directly to D-03/D-07 and the production branch | Rejected: the old inverted expectation is corrected; full-object assertions now distinguish anchorless and anchored labels. |
| Uncovered error path | Checked both required referent failures and both independent optional relation failures for exact result and leakage | Rejected: post/changelog failures and scope/post comment mismatches all execute. |
| Required-error metadata leak | Serialized each rejection and searched for foreign IDs, titles, slug, and target data | Rejected by the passing adversarial assertions. |
| Optional-relation redirect or metadata leak | Compared the returned post ID/label to the event post and searched serialized rows for invalid referents | Rejected: only the safe event-post destination remains. |
| Valid-event regression | Re-ran all five event mappings and labels after the conditional change | Rejected: all exact valid targets pass. |
| Merge identity regression | Re-ran atomic/resumable component coverage and the real backend | Rejected: type-aware identity behavior remains green. |
| Client join, authority, version, or packed regression | Rechecked original artifacts/links and ran static, React, headless, packed, and full gates | Rejected: all original four passing truths remain verified. |
| Scope expansion | Diffed closure implementation/test commits and documentation commit separately | Rejected: implementation/test scope is exactly two files; closeout changes are planning-only. |
| Unrelated-worktree drift | Recomputed the protected fingerprint after all fresh gates | Rejected: exact fingerprint preserved. |

## Human Verification

None. All behavior-dependent truths have fresh executable evidence.

---

_Verified: 2026-07-21T20:02:04Z_

_Verifier: independent generic-agent fallback using the installed gsd-verifier role_
